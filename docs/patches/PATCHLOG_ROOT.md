- Patch 641: Refactor-Durchlauf 3 (CI-Lite helper-first) — `useCiLiteWorkflow.ts` nutzt fuer die Lookup-Diagnose-Konsolidierung jetzt den neuen pure Helper `mergeWorkflowRunLookupDiagnosis(...)` aus `useCiLiteWorkflowHelpers.ts` statt lokaler Inline-Merge-Logik; Verhalten/Vertragslogik unveraendert, fokussierte Tests in `__tests__/useCiLiteWorkflowHelpers.test.ts` erweitert.
- Patch 640: Refactor-Durchlauf 2 (helper-first + PR-Review-Fixes) — in `screens/ConnectionsScreen/hooks/useConnectionsScreen.ts` wurde die EAS-Project-Verifikationsableitung in den neuen pure Helper `resolveEasProjectVerification(...)` (`useConnectionsScreenHelpers.ts`) ausgelagert; zusaetzlich wurden die PR-Review-Hinweise umgesetzt: historische Patch-IDs in bereits abgeschlossenen TODO-Eintraegen nicht mehr retaggt (zurueck auf 636/637/638) und der `as any`-Vollscanwert in `docs/04-risk-hotspots.md` auf den reproduzierbaren Wert 341 korrigiert.
- Patch 639: Refactor-Durchlauf 1 (helper-first, ohne Verhaltens-/Vertragsaenderung) — `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` enthaelt kein lokales Artifact-Parse mehr; stattdessen zentraler Helper `parseCiLiteArtifactJson(...)` in `useCiLiteWorkflowHelpers.ts`. Zusaetzlich wurde in `screens/DiagnosticScreen/hooks/useDiagnosticFixRunner.ts` die Preview-Entry-Bildung in den pure Helper `buildFixPreviewEntries(...)` aus `useDiagnosticFixRunnerHelpers.ts` ausgelagert. Fokussierte Regressionen in `__tests__/useCiLiteWorkflowHelpers.test.ts` und `__tests__/useDiagnosticFixRunnerHelpers.test.ts` wurden erweitert.
- Patch 638: kritischen Deep-Scan auf Branchstand nachgezogen und echte Dokument-Drifts bereinigt — Kern-Header (`docs/INDEX.md`, `docs/TESTING_GUIDE.md`, `docs/FRESH_CHECKOUT_GREEN_PATH.md`) auf denselben Stand gezogen, `docs/04-risk-hotspots.md` um frische `as any`/`: any`-Inventarzahlen samt Runtime-vs-Test-Einordnung erweitert, und `docs/reviews/deep-scan-review-2026-03-30.md` um ein zweites Addendum mit bestaetigten Restpunkten/Non-Issues ergaenzt; keine Runtime-/Contract-Aenderung.
- Patch 637: dokumentarischen/organisatorischen Drift auf PR-506-Branch konsolidiert — Stand-Header von `docs/INDEX.md`, `docs/TESTING_GUIDE.md` und `docs/FRESH_CHECKOUT_GREEN_PATH.md` auf Patch 636 harmonisiert; `docs/reviews/deep-scan-review-2026-03-30.md` im Hauptteil auf den aktuellen TODO-Stand nachgezogen (frueher als offen markierte Punkte wie Trust-Follow-up/CS-REST-001 jetzt konsistent als geschlossen/erledigt); `AGENTS.md` um verbindliche Kern-MD-SoT-Pruefung und Pflichtangaben fuer den Abschlussbericht erweitert.
- Patch 636: finaler Test-Audit-Nachzug mit kleinem deterministischem Fixbeispiel — `__tests__/buildReadinessGate.ciLiteFreshness.test.ts` verwendet jetzt eine feste Uhrbasis (`FIXED_NOW`) und `jest.spyOn(Date, "now")`, statt mehrere ungebundene `Date.now()`-Werte in Fixture-/Freshness-Pfaden zu mischen. Dadurch bleibt der Freshness-Vertrag reproduzierbarer. `docs/reviews/deep-scan-review-2026-03-30.md` enthaelt zusaetzlich einen dedizierten Test-Audit-Abschnitt mit P1/P2-Verbesserungsmoeglichkeiten (Begruendung + Fixoptionen).
- Patch 635: letzter kleiner Follow-up aus dem Helper-Refactor-Block und begleitender Deep-Scan-/Docs-Sync — `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts::refreshSyncStatus(...)` setzt bei `resolveSyncStatusPrecheck(...).status === "invalid_repo"` nun bewusst einen frischen Fehlerstatus (`error: 1`, `checkedAt`) statt still/ohne Update zu returnen; damit bleibt die Sync-Anzeige bei ungueltigem Repo-Slug nicht stale auf alten Vergleichswerten. `__tests__/useGitHubReposScreenHelpers.test.ts` deckt den `invalid_repo`-Zweig explizit ab. Der Deep-Scan-Bericht (`docs/reviews/deep-scan-review-2026-03-30.md`) enthaelt zusaetzlich ein aktuelles Addendum mit Ampel-Priorisierung (P0/P1/P2), konkreten Refactoring-/Dead-Code-Kandidaten (Datei+Stelle) und jeweils moeglichen Fixwegen.
- Patch 634: PR-504-Review-Follow-up fix fuer einen echten Materializer-Regressionpfad — `lib/projectMaterializer.ts::materializeProjectFiles(...)` prueft `project.files`-Eintraege jetzt zuerst auf brauchbare Objektkandidaten und ruft `readProjectFileContent(...)` erst danach auf; damit werden `null`/primitive/malformed Hydration-Eintraege wieder fail-safe ignoriert statt bei `file.content` zu crashen. Neuer Regressionstest `__tests__/projectMaterializer.failSafe.regression.test.ts` deckt den Crash-/Skip-Vertrag explizit ab; README/INDEX/TODO/Checklog auf denselben Patchstand synchronisiert.

- Patch 633: kleiner `as any`-Durchlauf im Test-Scope (`tokenEstimator`/`validators`) mit typkonformen Literalen statt Any-Casts; codefokussierter Scan ohne Doku/README/PROJECT_CHECKLOG von 165 auf 150.

- Patch 632: Durchlauf 6 entfernt die letzten `as any`-Fragmente in produktionsnahen UI-Hilfsskripten (`scripts/ui/*`); codefokussierter Scan ohne Doku/README und ohne `PROJECT_CHECKLOG.md` von 167 auf 165.

- Patch 631: `as any`-Durchlauf 5 reduziert verbleibende kleine UI-/Style-/Interop-Casts (Glow-/Icon-/Template-/WebView-Glue); codefokussierter Scan ohne Doku/README von 187 auf 178.

- Patch 630: weiterer `as any`-Durchlauf reduziert kleine UI-/Interop-Casts (Navigation-/Icon-/Diff-/WebView-/Template-Glue) ohne Refactor; codefokussierter Scan ohne Doku/README von 197 auf 187.

- Patch 629: `as any`-Durchlauf 3 reduziert kleine UI-/Glue-Restcasts (`polyfills`, Wizard/Diagnostic new-arch-Flags, WorkflowRunDetailModal, ApiKeysSection, LocalRemoteDiffSection`) ohne Architekturumbau; codefokussierter Scan ohne Doku/README von 208 auf 197.

- Patch 628: `as any`-Durchlauf 2 reduziert weitere Runtime-/Edge-Restpunkte (`notificationService`, `github-workflow-logs`, `create_codesandbox`) mit engen Guards/Narrowings; codefokussierter Scan ohne Doku/README von 212 auf 208.

- Patch 627: erster konservativer `as any`-Abbau auf aktuellem Stand mit Fokus auf Runtime-/Edge-Helper (`k1w1-handler`, `android-keystore-generate`, Template-Diagnostics, Project-Materializer, Repo-Utils); Netto-Reduktion im Scan von 291 auf 285 ohne Vertragsumbau.

- Patch 626: kleiner, evidenzbasierter Cleanup-/Hardening-Nachzug ohne Broad-Refactor — `docs/PROJECT_TODO.md` ist jetzt explizit als historische Liste markiert (aktive Restpunkt-SoT bleibt `docs/TODO.md`), `docs/TODO.md` schliesst den Restpunkt CS-REST-001 mit dem bereits vorhandenen `testEas`-Busy-Guard-/Feedback-Stand final, und `lib/secretRedaction.ts` entfernt den verbleibenden Runtime-`as any`-Cast in `replaceAllSafe(...)` bei unveraenderter Redaction-Semantik; Risk-Hotspots-/README-/INDEX-/Checklog-/Patchnote-Stand sind synchron.
- Patch 625: Deep-Scan-P1-Restpunkte mit kleinem Scope geschlossen — zentraler Fresh-Checkout-Green-Path als neue SoT (`docs/FRESH_CHECKOUT_GREEN_PATH.md`) eingefuehrt und in README/INDEX/TESTING_GUIDE verankert, `docs/TESTING_GUIDE.md` auf aktuellen realen Verify-Flow ohne historische Umgebungsartefakte umgestellt, `testEas` im Connections-Hook auf dieselbe Busy-Guard-Feedback-Semantik wie die anderen Test-/Save-Aktionen gehoben (kein stilles Busy-Return mehr), und der naechste selektive Runtime-`as any`-Hotspot in `lib/diagnostics/ciAutoFix.ts` ueber `unknown`+Narrowing (`toErrorMessage`) abgebaut; Risiko-/TODO-Doku auf den neuen Stand synchronisiert.
- Patch 624: live-vs-repo-Drift zum bereits ausgefuehrten P0-RLS-/Policy-Hotfix geschlossen — neue Migration `20260330000000_p0_rls_policy_fix_lint_and_native_sync.sql` kanonisiert exakt den Live-Stand fuer genau drei Tabellen: `public.lint_jobs` (fehlerhafte PUBLIC-Policy entfernt, deny fuer `anon`/`authenticated`) sowie `public.native_sync_jobs`/`public.native_sync_reports` (RLS aktiviert + jeweilige deny-Policy fuer `anon`/`authenticated`). Kein weiterer Scope.
- Patch 623: berechtigtes Review-Feedback zu Patch 622 am UTF-8-Regressionstest nachgeschaerft — der bisherige Test platzierte Non-ASCII nur in `user_metadata`, waehrend die Role-Quellen (`role`, `app_metadata.role`) ASCII blieben und dadurch den alten Decoderpfad nicht zwingend rot machten. Der Test in `__tests__/auth.failClosedAndDurableRateLimit.test.ts` nutzt jetzt eine Unicode-Rollenclaim (`build_ädmin`) im echten Shared-Auth-Pfad und prueft sowohl dekodierte Claim-Auslese (`getJwtPayload(req)?.role`) als auch den finalen Allowlist-Match via `requireJwtRole(...)`; damit scheitert der Legacy-Decoder fachlich am Role-Match, waehrend der aktuelle UTF-8-Decoder gruen ist. Kein Runtime-Code geaendert.
- Patch 622: verbleibenden Live-RBAC-Drift im finalen JWT-Role-Match geschlossen — in `_shared/auth.ts::decodeJwtPayload(...)` wurde der JWT-Body bisher nach `atob(...)` direkt als JSON geparst; bei Non-ASCII-Zeichen in beliebigen Claims konnte dieser Parse scheitern und der Rollenpfad fiel auf `auth/v1/user.role` (`authenticated`) zurueck, obwohl der verifizierte Token-Claim `role=build_admin` war. Der Payload wird jetzt UTF-8-sicher (`Uint8Array` + `TextDecoder`) dekodiert, sodass `requireJwtRole(...)` den verifizierten `build_admin`-Claim stabil in den finalen Allowlist-Check (`service_role|build_admin`) uebernimmt. Regressionstest mit Unicode-Claim deckt den Drift ab; kein Security-Downscope.
- Patch 621: verbliebenen Timing-/Flaky-Testrestpunkt rund um `retryWithBackoff` robust geschlossen — die Runtime in `lib/retryWithBackoff.ts` war korrekt (deterministische exponentielle Delays), die Flake-Ursache lag im Test `lib/__tests__/retryWithBackoff.test.ts`, der mit Real-Timern echte Wandzeit (`Date.now()`) als Proxy fuer berechneten Backoff nutzte. Unter Last konnte der erste Retry-Abstand daher sporadisch >1500ms werden. Der Test ist jetzt deterministisch auf Fake-Timer + Aufrufzeitpunkt-Assertions umgestellt und prueft den Vertragskern explizit (1. Retry nach 1000ms, 2. Retry nach weiteren 2000ms), ohne inhaltliche Aufweichung oder Runtime-Downscope.
- Patch 620: verbleibenden serverseitigen JWT-/RBAC-Drift im Shared-Auth-Role-Read-Pfad behoben — Ursache war ein Priorisierungsfehler im verifizierten Rollen-Read (`user.role` wurde vor JWT-Claim gelesen); bei korrekt verifiziertem Operator-JWT (`role=build_admin`) kam ueber `auth/v1/user` oft `role=authenticated` zurueck und fuehrte zu faelschlichem `Forbidden ... role is not allowed`. `requireJwtRole(...)` liest jetzt nach erfolgreicher JWT-Verifikation zuerst den verifizierten Token-Claim (`role`, danach `app_metadata.role`) und nutzt `auth/v1/user` nur noch defensiv als Fallback, weiterhin fail-closed auf `service_role|build_admin`; zusaetzlich wurden die zwei bekannten Edge-Typecheck-Restpunkte in `_shared/auth.ts` sauber typisiert und eine Regression im Auth-Test ergänzt.
- Patch 619: strukturierter `as any`-Audit mit Risikoklassifikation und gezieltem Minimal-Abbau — repo-weites Inventar trennt Runtime-/Domain-/Validation-/Config-/Normalizer-/Auth-/Network-nahe Treffer (A/B) von UI-/Test-/Interop-Casts (C/D). Ohne Grossrefactor wurden nur die gefaehrlichsten klein/sicher behebbaren Stellen ersetzt: `lib/validators.ts`, `lib/supabase.ts`, `lib/supabaseEdge.ts`, `lib/normalizer.ts`, `lib/diagnostics/buildPipelineDiagnostics.ts`, `project/services/projectArchiveService.ts`, `screens/ConnectionsScreen/utils/validation.ts`, `config.ts`. `docs/04-risk-hotspots.md` dokumentiert priorisierte offene Restpunkte und die naechste Reihenfolge.
- Patch 618: strukturierter Hook-Refactoring-Audit ohne Grossumbau — `docs/04-risk-hotspots.md` enthaelt jetzt ein repo-weites Hook-Inventar mit A/B/C/D-Klassifikation (groessere Hooks vs. wirklich gekoppelte Risiko-Hooks), konkrete Schnittvorschlaege pro Top-Hook und eine priorisierte Reihenfolge. Besonders hoch eingestuft sind `useGitHubReposScreen`, `useCiLiteWorkflow`, `useConnectionsScreen`, `useCredentialsWizardScreen` und `useDiagnosticFixRunner`; bewusst **nicht jetzt** gross angefasst bleiben die sensiblen, juengst gehaerteten Vertrags-Hooks (`usePreview`, `useEnhancedBuildScreen`, `useGitHubActionsLogs`). README/TODO/Checklog wurden auf denselben Audit-Stand gezogen.
- Patch 617: offenen Supabase-/Operator-Runbook-Restpunkt operativ geschlossen — `docs/TODO.md` fuehrt den Punkt nicht mehr als unklare TODO-Zone, sondern trennt jetzt klar zwischen repo-seitigem Runbook-Vertrag und bewusst externem Provisioning-Rest. `docs/06-build-readiness.md` dokumentiert eine verbindliche Reihenfolge fuer Operator-Claim, scoped Keys, Secrets, DB-/Storage-/Function-Preconditions, Preview-/Signing-/Workflow-Kanten und Live-Test, inklusive Troubleshooting-Matrix (Symptom/Ursache/Schritt). `docs/EDGE_FUNCTIONS_STATUS.md`, `docs/04-risk-hotspots.md` und `docs/01-state-contract.md` wurden auf denselben Betriebsvertrag synchronisiert; `scripts/check_workflow_edge_contracts.sh` prueft die neuen Runbook-Kernanker regressionsfest.
- Patch 616: globale LogBox-Warnungsunterdrueckung in `App.tsx` auf Minimum reduziert — die breiten Ignore-Strings `Require cycle:` und `VirtualizedLists should never be nested` wurden entfernt, damit Architektur-/Render-/Listenwarnungen im Dev-Betrieb wieder sichtbar sind. Als einzige bewusst verbleibende globale Ausnahme bleibt die eng begrenzte Reanimated-Dev-Noise-Warnung `Sending `onAnimatedValueUpdate` with no listeners registered.` (inkl. klarer Entfernungsbedingung im Kommentar). Neue Invariant `__tests__/patch616.logboxWarningVisibility.invariants.test.ts` blockt Rueckdrift (`ignoreAllLogs`/breite Ignore-Strings).
- Patch 615: Preview-Operatorgrenze fuer Legacy-`save_preview` explizit gezogen — `hooks/usePreview.ts` nutzt `getLegacyEdgeAdminKey()` im normalen Supabase-Clientflow nicht mehr still, sondern nur noch bei bewusst aktivierter Operator-/Maintenance-Flag `EXPO_PUBLIC_ENABLE_LEGACY_PREVIEW_OPERATOR_MODE=true`; Standardpfad bleibt ohne Legacy-Privileg fail-closed mit ehrlicher Blocker-Meldung, und der lokale HTML-Fallback bleibt separat nur im expliziten `preferredPreviewMode: "local"`-Pfad erreichbar. Neue Invariant `patch615.previewLegacyOperatorBoundary` sowie aktualisierte Preview-Contract-Tests und Workflow-Contract-Checks sichern diese Trennung regressionsfest.
- Patch 614: Build-Screen-Filter-Truthfulness fuer Workflow-Runs korrigiert — `screens/EnhancedBuildScreen/hooks/useEnhancedBuildScreen.ts` nutzt keinen irrefuehrenden Fallback mehr, der bei aktivem Profilfilter ohne Treffer wieder alle Runs zeigte (`return list.length > 0 ? list : runs`). Die Filterung liefert jetzt im aktiven Profilfall strikt nur echte Matches (`[]` bei null Treffern), ein neuer Empty-State-Text macht den Nulltreffer ehrlich sichtbar, und fokussierte Tests/Invariants (`__tests__/runFilterState.test.ts`, `__tests__/invariants.selection.test.ts`) sichern den No-Fallback-Vertrag regressionsfest.
- Patch 613: Workflow-Dispatch und Workflow-Bootstrap/Repair semantisch entkoppelt — `infra/github/workflows.ts::triggerWorkflow(...)` sowie `supabase/functions/github-workflow-dispatch` dispatchen jetzt fail-closed nur noch den Workflow und fuehren bei `404` keine stillen Repo-Mutationen/Template-Writes mehr aus. Fehlender Workflow wird klar als `missing_workflow` signalisiert; explizite Repair-/Provisioning-Pfade bleiben separat. Neue Contracts/Invariants (`githubBranchRefHardening`, `patch613`) plus aktualisierter `check_workflow_edge_contracts.sh` sichern den mutation-free Dispatch-Vertrag regressionsfest ab.
- Patch 612: Build-Start fail-closed bei Repo-Sync/Push-Kante gehaertet — im `out_of_sync`-Pfad von `startBuildJob(...)` wird ein fehlgeschlagenes `pushFilesToRepo(...)` nicht mehr als Best-Effort behandelt. Stattdessen erfolgt ein harter Abbruch mit klarer Fehlermeldung (`Build abgebrochen: Lokale Aenderungen konnten nicht erfolgreich ins Ziel-Repo gepusht werden.`), und nach diesem Fehler werden weder Workflow-Autofix/Bootstrap (`autoFixCIWorkflows`) noch Dispatch/Run gestartet. Der Integrationsvertrag ist ueber Tests abgesichert (kein Autofix, kein Dispatch bei Push-Fail).
- Patch 611: finaler Audit-/Preflight-/Runbook-Schritt fuer den extern provisionierten `build_admin`-Vertrag — App-/Wizard-/CI-Lite-Callertexte benennen jetzt explizit fail-closed, dass normale eingeloggte Nutzer ohne externen `build_admin`-Claim nicht fuer Operatorpfade ausreichen; `runBuildPipelineDiagnostics` und `docs/06-build-readiness.md` enthalten einen operativen Operator-Preflight inkl. Troubleshooting-Checkliste, und `scripts/check_workflow_edge_contracts.sh` plus neue Invariants (`patch611`) sichern die Aussage regressionsfest ohne Repo-seitige Claim-Simulation.
- Patch 610: Merge-Readiness-Integrationssync fuer PR 480-484 — `docs/EDGE_FUNCTIONS_STATUS.md` hatte nach Patch 609 einen veralteten Stand-Header `(Patch 608)`, obwohl die Inhalte bereits den Patch-609-Endvertrag abbildeten; Header auf `(Patch 609)` korrigiert, ohne Aenderung am operativen Auth-/Runtime-Vertrag.
- Patch 609: finaler Legacy-Edge-Admin-Sunset fuer produktionsnahe Clientpfade — `useCredentialsWizardScreen` und `signingKeyGate` lesen Keystore-Readiness jetzt scoped-only ueber `getAndroidKeystoreExportAdminKey()` (kein stiller Legacy-Fallback mehr), `SecretsSection` behandelt den Legacy-Key nicht mehr als lokalen Pflichtblocker, und verbleibende `getLegacyEdgeAdminKey(...)`-Nutzung ist bewusst auf Compat-/Migration-/Altpfade begrenzt (`usePreview`, `k1w1-handler`, AppInfo/Backup/Restore, optionaler Secret-Sync). Neue Invariant `patch609.legacyEdgeAdminSunsetBoundaries` und erweiterte Contract-Checks erzwingen diese Grenzen.
- Patch 608: finaler Config-SoT-Cleanup fuer `android-keystore-export` — die redundante funktionslokale Datei `supabase/functions/android-keystore-export/config.toml` wurde entfernt, sodass `supabase/config.toml` jetzt fuer alle gehaerteten Keystore-Routen (`android-keystore-export`/`android-keystore-generate`/`android-keystore-status`) die einzige fail-closed `verify_jwt=true`-Quelle ist. `scripts/check_workflow_edge_contracts.sh` und die Keystore-Invariants (`patch599`, `patch549`) pruefen jetzt explizit, dass keine lokale Export-Config mehr existiert und kein Split-Brain-Vertrag rueckkehrt.
- Patch 607: finaler Shared-Auth-Cleanup fuer obsoleten CI-bearer-Vertrag — nach Audit ohne verbleibende produktive CI-bearer-Nutzung wurden in `supabase/functions/_shared/auth.ts` die toten CI-bearer-Reste entfernt (`isScopedCiBearerRequest(...)`, `allowCiBearer`/`ciBearerSecretEnv` in `ScopedEdgeAuthConfig` und `requireScopedEdgeAuth(...)`, plus `requireServiceRoleBearer(...)`, `requireAdminKeyOrServiceRoleBearer(...)`, `hasServiceRoleSecretConfigured()`). Alle betroffenen Edge-Callsites nutzen jetzt den kleineren Scoped-Admin-Vertrag; Operatorrouten bleiben bei `verify_jwt=true` auf JWT+`x-k1w1-admin-key`. Invariants/Contract-Checks (`patch415`, `patch553`, `patch603`, `patch410`, `edgeHelperVisibility`, `check_workflow_edge_contracts.sh`) und aktuelle Status-/Readiness-/Risk-Doku wurden konsistent nachgezogen.
- Patch 606: CI-bearer-Vertragsdrift fuer workflow-/build-/artifact-nahe Edge-Routen konsequent entfernt — unter `verify_jwt=true` war `K1W1_EDGE_WORKFLOW_CI_BEARER` kein eigenstaendiger operativer Pfad mehr, daher laufen `trigger-eas-build`, `check-eas-build`, `github-workflow-dispatch`, `github-workflow-runs`, `github-workflow-logs` und `github-run-artifact-json` jetzt einheitlich mit `allowCiBearer: false` (ohne `ciBearerSecretEnv`/`isScopedCiBearerRequest(...)`) plus verpflichtendem Operator-JWT-Guard. `scripts/check_workflow_edge_contracts.sh`, `__tests__/patch415.edgeAuthGuards.invariants.test.ts`, `__tests__/patch553.workflowDispatchJwtRbac.invariants.test.ts` sowie UI-/Ops-/Doku-Hinweise wurden auf denselben JWT+scoped-Workflow-admin-key-Endvertrag synchronisiert; kein Zombie-Dualpfad bleibt bestehen.
- Patch 605: Operator-/RBAC-Vertragsrest rund um `build_admin` operativ geschlossen — Repo-Audit ueber Auth/JWT/Edge/Caller/Docs bestaetigt: kein interner Claim-Mapper/Grant-Pfad im Repo, `build_admin` bleibt ein bewusst externer Supabase-Provisioning-Claim (`role`/`app_metadata.role` ausserhalb dieses Repos). App-/Wizard-Fehltexte nennen den externen Ursprung jetzt explizit, `runBuildPipelineDiagnostics` fuehrt einen dedizierten Operator-Claim-Precondition-Hinweis, und `scripts/check_workflow_edge_contracts.sh` plus neuer Invariant-Test sichern diesen Endvertrag regressionsfest.
# Patchlog Root
- Patch 604: RBAC-/JWT-Vertragswiderspruch fuer app-initiierte workflow-/build-/artifact-/keystore-Calls geschlossen — der echte Endvertrag bleibt bewusst Operator-only (`service_role|build_admin`), App-Caller-/Wizard-Fehltexte nennen nicht mehr `JWT role=authenticated`, relevante Vertrags-Tests nutzen neutrale Operator-JWT-Mocks, und `scripts/check_workflow_edge_contracts.sh` sowie `__tests__/patch604.workflowOperatorCallerContract.invariants.test.ts` sichern den Zustand regressionsfest.
- Patch 603: echter Guard-Fehler der Legacy-Testroute `supabase/functions/test` fail-closed behoben — der `requireScopedEdgeAuth(...)`-Aufruf fuehrt jetzt explizit `scope: "test"`, `adminSecretEnv: "K1W1_EDGE_ADMIN_KEY"`, `allowAdmin: true`, `allowCiBearer: false`; dadurch endet die Route nicht mehr vorzeitig in einer `500`-Auth-Misconfiguration, sondern liefert konsistent den beabsichtigten Disabled-Vertrag `410 legacy_test_route_disabled`. `scripts/check_workflow_edge_contracts.sh`, `__tests__/edgeCorsRequestBound.invariants.test.ts` und neuer Invariant-Test `__tests__/patch603.legacyTestRouteAuthContract.invariants.test.ts` sichern genau diese Fehlstelle regressionsfest.
- Patch 602: verbleibenden JWT-/Ref-Vertragsbruch im workflow-/build-/artifact-nahen Smoke-Scope fail-closed geschlossen — `scripts/ci-lite-smoke.sh` sendet fuer `github-workflow-dispatch`/`github-workflow-runs`/`github-workflow-logs` jetzt zwingend `Authorization: Bearer <jwt>` (`K1W1_EDGE_WORKFLOW_JWT`) plus `x-k1w1-admin-key` (`K1W1_EDGE_WORKFLOW_ADMIN_KEY`), verlangt einen expliziten `<ref>` (kein stilles `main`), und `scripts/ci-lite-env-load.sh` sowie Contract-Checks/Invariants sichern den Zustand regressionsfest.
- Patch 601: finaler Legacy-Compat-Cleanup fuer generische Edge-Admin-Key-Reste — produktive/scoped App-Pfade lesen/schreiben den Compat-Slot nur noch ueber explizite `get/save/deleteLegacyEdgeAdminKey(...)`-APIs (keine generischen Helper-Namen mehr ausser in `tokenStore`-Compat), die stray Route `supabase/functions/test` ist jetzt bewusst fail-closed als legacy Stub (`requireScopedEdgeAuth(... K1W1_EDGE_ADMIN_KEY)` + `410 legacy_test_route_disabled`), und neue Guards (`__tests__/legacyEdgeAdminCompat.invariants.test.ts`, aktualisierte `edgeCorsRequestBound` Invariant, erweiterter `scripts/check_workflow_edge_contracts.sh`) sichern den Zustand regressionsfest.
- Patch 600: CI-/Smoke-Vertrag fuer workflow-/build-/artifact-nahe Skripte auf dedizierten Workflow-Key gehaertet — `scripts/ci-lite-env-load.sh` und `scripts/ci-lite-smoke.sh` akzeptieren jetzt ausschliesslich `K1W1_EDGE_WORKFLOW_ADMIN_KEY` (keine stillen Fallbacks auf `ADMIN_KEY`/`K1W1_EDGE_ADMIN_KEY` mehr), Fehlermeldungen wurden auf den scoped Vertrag umgestellt, und `scripts/check_workflow_edge_contracts.sh` plus `__tests__/patch600.workflowAdminScriptContract.invariants.test.ts` sichern den Zustand regressionsfest.
- Patch 599: Android-Keystore-Config-Split-Brain fuer `android-keystore-generate`/`android-keystore-status` fail-closed aufgeloest — `supabase/config.toml` ist fuer diese Routen jetzt die einzige `verify_jwt`-Source-of-Truth (`true`), die widerspruechlichen lokalen Function-Configs (`verify_jwt=false`) wurden entfernt, und `scripts/check_workflow_edge_contracts.sh` plus `__tests__/patch599.keystoreConfigSot.invariants.test.ts` sichern den konsistenten JWT-Vertrag regressionsfest ab.
- Patch 598: Legacy-Guard-/Surface-Reduction fuer generische Admin-Key-Routen — `requireAdminKey(...)` akzeptiert jetzt nur noch `K1W1_EDGE_ADMIN_KEY` (kein `SIGNING_ADMIN_KEY`-Fallback), `supabase/functions/_shared/auth.ts` enthaelt zusaetzlich einen dedizierten `requireSigningAdminKey(...)` fuer signing-spezifische Pfade, und die verbleibenden Legacy-Routen (`k1w1-handler`, `create_codesandbox`, `save_preview` sowie disabled lint/native-sync Stubs) nutzen explizit `requireScopedEdgeAuth(... adminSecretEnv: "K1W1_EDGE_ADMIN_KEY", allowCiBearer: false)` statt implizitem Allzweck-Guard; betroffene Invariants/Drift-Checks/Status-Docs sind synchronisiert.
- Patch 597: Credentials-Wizard/Keystore-Auth-Vertrag konsolidiert — `useCredentialsWizardScreen` nutzt lokal primär den dedizierten SecureStore-Slot `androidKeystoreExportAdminKey` (Legacy-`edge_admin_key_v1` nur noch als kontrollierter Compat-Read), Keystore-Requests an `android-keystore-status` und `android-keystore-generate` senden jetzt verpflichtend `Authorization: Bearer <Supabase user JWT>` plus `x-k1w1-admin-key`, und die Wizard-/Contract-Tests sowie `scripts/check_workflow_edge_contracts.sh` sichern den kombinierten JWT+Scoped-Key-Vertrag regressionsfest.

- Patch 596: UI-/Diagnostik-Konsolidierung auf den scoped-key-Vertrag — `ConnectionsScreen` zeigt und persistiert lokale Keys jetzt getrennt nach Workflow (`workflowAdminKey`), Android-Keystore-Export (`androidKeystoreExportAdminKey`) und Legacy-Compat (`legacyEdgeAdminKey`), alte Spiegel-Texte eines einzelnen lokalen Edge-Keys sind in Connections/SecretsSection bereinigt, und `buildPipelineDiagnostics` prueft lokale Workflow-/Keystore-Readiness explizit statt false-green ueber reinen Legacy-Key. Betroffene Tests/Docs wurden auf den scoped Vertrag synchronisiert.

- Patch 595: npm-Tooling-Warning-Stabilisierung — die reproduzierbare Meldung `npm warn Unknown env config "http-proxy"` wurde als externer Host-/Runner-Umgebungsrestpunkt klassifiziert (nicht als Repo-Fehlkonfiguration); Repo-Scan bestaetigt keine Injection veralteter `npm_config_http-proxy`/`npm_config_https-proxy`-Namen in Scripts/Workflows, und ein neuer Invariant-Test haelt diese Nicht-Injektion regressionsfest. Parallel bleiben `check_eas_manual_trigger_controls.sh` und `check_eas_strict_lockfile_policy.sh` gruen.
- Patch 594: gezielter Drift-Fix fuer `check_eas_manual_trigger_controls.sh` und `check_eas_strict_lockfile_policy.sh` — beide Scripts pruefen jetzt die echte Workflow-SoT-Kette (Live `.github/workflows/*` + Shared-Templates in `shared/workflows/easBuildReleaseWorkflowTemplates.ts` / `shared/workflows/k1w1TriggeredBuildWorkflowTemplate.ts` + Diagnostics-Wiring) statt eines veralteten direkten String-Matches auf `lib/diagnostics/workflowTemplates.ts`; Sicherheits-/Manual-Trigger-/Strict-Lockfile-Vertraege bleiben unveraendert streng.
- Patch 593: Hardening-/Integrationsschritt fuer lokale scoped Admin-Keys nachgezogen — `tokenStore` fuehrt den Workflow-Key als primaeren lokalen Vertrag mit kontrollierter Einmal-Migration aus `edge_admin_key_v1` (nur wenn `workflow_admin_key_v1` fehlt), markiert Legacy explizit ueber `get/save/deleteLegacyEdgeAdminKey`, und entfernt stilles Legacy-Multi-Mirroring beim Secure-Backup-Import: `edgeAdminKey` fuellt kompatibel nur noch `workflowAdminKey` + `legacyEdgeAdminKey`, nicht mehr automatisch Keystore-/Signing-Slots. Workflow-/Build-/Artifact-Caller bleiben auf dem dedizierten Workflow-Key, AppInfo-/Secret-Sync-Pfade nutzen die expliziten Legacy-APIs nur noch fuer Kompatibilitaet.
- Patch 592: gezielte Stabilisierung driftender Vertrags-Tests nach Hardening 586–591 — `__tests__/ciLiteHeaderWorkflow.invariants.test.ts` referenziert den Header-Running-Vertrag jetzt ueber die echte SoT `deriveCiLiteHeaderState(...)` statt ueber einen veralteten Inline-String in `useCiLiteWorkflow.ts`; `__tests__/patch403.workflowContracts.invariants.test.ts` prueft CI-/EAS-Template-Vertraege jetzt an den Shared-SoTs (`shared/workflows/managedWorkflowTemplates.ts`, `shared/workflows/easBuildReleaseWorkflowTemplates.ts`) und validiert zusaetzlich die Alias-/Import-Verdrahtung in `infra/github/workflowTemplates.ts`, `supabase/functions/github-workflow-dispatch/index.ts` und `lib/diagnostics/workflowTemplates.ts`. Kein Security-/RBAC-/Branch-Vertrag wird aufgeweicht; nur Testdrift gegen den realen Codevertrag entfernt.
- Patch 591: Letzte SoT-/Retention-/Vertragsbereinigung — `projectData.linkedRepo`/`linkedBranch` sind jetzt die einzige persistente Repo-/Branch-Wahrheit (GitHubContext leitet `activeRepo`/`activeBranch` nur noch daraus ab und schreibt keine `k1w1_github_active_*`-Keys mehr), Repo-/Branch-Write-Pfade in RepoScreen + AppInfo-Import wurden auf `setLinkedRepo(...)` konsolidiert, Secure-Backup migriert Legacy-`github.activeRepo`/`activeBranch` kontrolliert auf `github.linkedRepo`/`linkedBranch`, `edge_rate_limit_events` bekommt echte Retention via `public.prune_edge_rate_limit_events(interval)` plus nightly `pg_cron`-Schedule, und `android-keystore-generate` fuehrt keinen irrefuehrenden branch-Request/-Response-Vertrag mehr (Route bleibt fachlich `repo + mode`).
- Patch 590: Hardening der verbleibenden Branch-/Ref-Fallbacks in tieferen Shared-Layern — `infra/github/workflows.ts` fordert fuer Workflow-Dispatch jetzt einen expliziten non-empty `ref` (kein `ref = "main"` mehr), `infra/github/files.ts` entfernt stille `"main"`-Fallbacks in branch-/ref-gebundenen Helfern (`resolveTargetBranch`, `createOrUpdateFile`, `deleteRepoFile`, `listRepoBlobEntries`), `infra/github/branchOps.ts::getDefaultBranch()` rät bei fehlendem `default_branch` nicht mehr auf `"main"` (`Repository default_branch is missing.`), und `android-keystore-generate` nutzt keinen impliziten `safeString(body?.branch) || "main"`-Fallback mehr. Betroffene Tests/Checks/Docs wurden konsistent nachgezogen.

- Patch 589: Edge-Branch-Contract fuer `trigger-eas-build` fail-closed gehaertet — `validateTriggerBuildRequest(...)` verlangt fuer den Trigger jetzt einen expliziten non-empty `branch` und normalisiert auf `branch: string` (fehlend/leer/Whitespace -> 400), `isAllowedRef(...)` akzeptiert leere Refs nicht mehr, und die Route entfernt ihre bisherige `branch ?? null`-Toleranz beim Insert/Dispatch/Response. Tiefere Shared-Layer-`main`-Fallbacks sind in diesem Schritt bewusst noch nicht entfernt.

- Patch 588: Keystore-Auth-Scope-Hardening fuer `android-keystore-generate` und `android-keystore-status` auf denselben scoped Vertrag wie `android-keystore-export` gezogen — beide Routen nutzen jetzt `requireScopedEdgeAuth(... adminSecretEnv: "K1W1_EDGE_ANDROID_KEYSTORE_EXPORT_ADMIN_KEY", allowCiBearer: false, allowJwtAuthHeaderWithAdmin: true)` statt generischem `requireAdminKey(...)`, erzwingen anschliessend fail-closed JWT-RBAC nur fuer `service_role|build_admin` ueber den neuen neutralen Shared-Helper `requirePrivilegedOperatorJwtRole(...)`, und `supabase/config.toml` setzt `verify_jwt=true` gezielt fuer diese zwei Funktionen. Betroffene Invariants/Contract-Checks/Doku wurden konsistent nachgezogen.

- Patch 587: Secret-Splitting fuer lokale Edge-Admin-Werte umgesetzt — SecureStore fuehrt jetzt dedizierte Zonen fuer Workflow-, Android-Keystore-Export-, Legacy-Edge- und Signing-Admin-Keys, `autoSyncRepoSecrets` mappt diese vier Werte bewusst getrennt auf `K1W1_EDGE_WORKFLOW_ADMIN_KEY`, `K1W1_EDGE_ANDROID_KEYSTORE_EXPORT_ADMIN_KEY`, `K1W1_EDGE_ADMIN_KEY` und `SIGNING_ADMIN_KEY` (kein stilles Multi-Mirroring eines Einzelwerts mehr), und workflow-/build-/artifact-seitige Clientcaller greifen nur noch auf den dedizierten Workflow-Key zu.

- Patch 586: erster Hardening-Schritt fuer privilegierte workflow-/build-bezogene Edge-Routen — zentraler Shared-Helper `WORKFLOW_OPERATOR_ALLOWED_ROLES` + `requireWorkflowOperatorJwtRole(...)` in `supabase/functions/_shared/auth.ts` eingefuehrt und fuer `trigger-eas-build`, `check-eas-build`, `github-workflow-dispatch`, `github-workflow-runs`, `github-workflow-logs`, `github-run-artifact-json` verdrahtet; JWT-RBAC auf Non-CI-Bearer-Pfaden ist jetzt fail-closed auf `service_role|build_admin` (kein `authenticated`), waehrend CI-Bearer- und scoped-admin-secret-Vertrag unveraendert bleibt. Checks/Invariants und Doku wurden auf den neuen Vertrag synchronisiert.

- Patch 585: letzter kleiner Doppel-Schritt mit zwei pure-logic-Extracts ohne Hook-/Context-Redesign — `hooks/useChatAIFlow.ts` nutzt fuer das bestehende Input-Validation-Warning-Mapping nun `getInputValidationMessage(...)` aus `hooks/chatAIFlowNoticeHelpers.ts`, und `contexts/GitHubContext.tsx` nutzt fuer den linked repo/branch Mirror-Guard nun `getLinkedMirrorUpdates(...)` aus `contexts/githubContextHelpers.ts`; Async-/Request-/Persistenz-/Mirror-Orchestrierung sowie Warning-/Notice-/Fallback-Semantik bleiben bewusst unveraendert. Abgesichert durch erweiterte fokussierte Helper-Tests plus ChatAIFlow-Input/Validator-Regressionen.

- Patch 584: zwei letzte kleine pure-logic-Extracts ohne Hook-Redesign — `hooks/useChatAIFlow.ts` zieht die Builder-Error-/Fallback-Textbildung in `hooks/chatAIFlowNoticeHelpers.ts` (`getBuilderFailureDetails`, `getBuilderFailureMessage`), und `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` zieht das Success-/Result-Text-Mapping fuer create/rename/delete in `screens/GitHubReposScreen/hooks/githubReposScreenNoticeHelpers.ts` (`getRepoSuccessNotice`); Async-/Dialog-/Request-Orchestrierung und sichtbare Semantik bleiben bewusst unveraendert. Abgesichert durch neue fokussierte Helper-Tests plus bestehende Chat-/GitHub-Regressionen.

- Patch 583: zwei kleine pure-logic-Extracts ohne Context-Redesign — `contexts/ProjectContext.tsx` zieht den Delete-/Filter-Block fuer Projektdateien in `contexts/projectContextHelpers.ts` (`removeProjectFilesByPaths`), und `contexts/AIContext/index.tsx` zieht die wiederholte Provider-/Mode-Default-Ableitung in `contexts/AIContext/helpers.ts` (`buildProviderSelectionPatch`); Rehydration-/Persistenz-/Context-Orchestrierung und aussen sichtbare Semantik bleiben bewusst unveraendert. Abgesichert durch erweiterte Helper-Vertragstests plus bestehende AIContext-Persistenz-/Quality-Regressionen.

- Patch 582: zwei kleine pure-logic-Extracts ohne Hook-Umbau — `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` zieht die Delete-Confirm-Textbildung in `screens/GitHubReposScreen/hooks/githubReposScreenDialogHelpers.ts` (`getDeleteRepoConfirmDialog`, `getDeleteBranchConfirmDialog`), und `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` zieht die Header-Lampenableitung in `components/CiLiteHeaderButton/hooks/useCiLiteWorkflowStatusHelpers.ts` (`deriveCiLiteHeaderState`); Async-/Polling-/Dispatch-/Dialog-Orchestrierung bleibt bewusst unveraendert. Abgesichert durch neue fokussierte Helper-Tests plus bestehende Hook-Behavior-/Semantiktests.


- Patch 581: kleiner Entflechtungsschritt im Regressions-Hotspot `hooks/useChatAIFlow.ts` — wiederholtes Stage-/Result-Mapping fuer Validator-Fallback-Warnings und Source-Summary ist in den neuen lokalen pure Helper `hooks/chatAIFlowStageHelpers.ts` extrahiert (`getValidatorFallbackWarning`, `getSourceSummaryText`); Hook bleibt Orchestrator fuer Planner/Builder/Validator/Explain/Apply ohne beabsichtigten Verhaltenswechsel bei `validatorState`, Warning-/Summary-Texten oder Chat-Flow-Semantik. Abgesichert durch neue fokussierte Helper-Tests plus nachgezogene Validator-/Explain-Invariants.

- Patch 580: kleiner Entflechtungsschritt im Fix-Runner-Hotspot `screens/DiagnosticScreen/hooks/useDiagnosticFixRunner.ts` — wiederholtes Failure-/Result-Mapping wurde in den neuen lokalen pure Helper `screens/DiagnosticScreen/hooks/fixRunnerResultHelpers.ts` extrahiert (`buildFailedStepPatch`, `buildApplyFailureResult`, `getErrorMessage`, `getFixRuntimeMeta`), sodass Step-Fehler-Patches (inkl. `safeTruncateText(..., 160)`) und Apply-Fehler-Result-Inputs nicht mehr mehrfach inline gebaut werden; Hook-Orchestrierung (Apply/Sync/Rerun/Modal) und Status-Semantik (`failed`/`blocked`/`pending_recheck`/`patch_applied`) bleiben bewusst unveraendert, abgesichert durch neue Helper-Vertragstests plus bestehenden Hook-Semantiktest.

- Patch 579: zwei kleine Entflechtungsschritte ohne Orchestrierungsumbau — in `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` wurde der pure Artifact-Fetch-Kontextblock (Completed-Eligibility + stabiler Context-Key) in `buildArtifactFetchContextKey(...)` nach `components/CiLiteHeaderButton/hooks/useCiLiteWorkflowHelpers.ts` extrahiert; in `contexts/GitHubContext.tsx` wurde die gespeicherte Recent-Repos-Rehydration (trim/string-only/filter-empty/dedupe/limit) in `normalizeStoredRecentRepos(...)` nach `contexts/githubContextHelpers.ts` extrahiert. Polling-/Artifact-Loop-Guard und GitHubContext-Rehydration/Persistenz-Flow bleiben bewusst unveraendert; abgesichert durch neue fokussierte Helper-Tests plus bestehende Workflow-/Mirror-Regressionen.


- Patch 578: kleiner Entflechtungsschritt im userkritischen Hook-Hotspot `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` — die bisher inline verteilte Result-/Notice-/Success-Textbildung fuer EAS-Link-Write-Outcomes und Secrets-Sync-Results wurde in den neuen lokalen pure Helper `screens/GitHubReposScreen/hooks/githubReposScreenNoticeHelpers.ts` (`getEasLinkWriteNotice`, `getSecretsSyncNotice`) extrahiert; der Hook bleibt Orchestrator fuer Repo-/Async-/Busy-/Dialog-Flow ohne beabsichtigten Verhaltenswechsel. Abgesichert durch neue Helper-Vertragstests plus bestehende GitHubReposScreen-Regressionen.

- Patch 577: kleiner Entflechtungsschritt im zentralen Zustands-Hotspot `contexts/GitHubContext.tsx` — die doppelte pure Recent-Repo-Update-Logik (dedupe + move-to-front + limit) und die Linked-Repo/Branch-trim-null-Normalisierung wurden in den neuen lokalen Helper `contexts/githubContextHelpers.ts` (`mergeRecentRepo`, `normalizeLinkedGitHubValue`) extrahiert; der Context bleibt Orchestrator fuer Rehydration, Persistenz und Mirror-Flow ohne beabsichtigten Verhaltenswechsel. Abgesichert durch neue Helper-Vertragstests plus bestehenden Mirror-Regressionstest.

- Patch 576: kleiner Entflechtungsschritt im userkritischen Hook-Hotspot `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` — die pure Error-/Fallback-Message-Logik (`extractErrorMessage`, `getErrorMessage`) wurde in den neuen lokalen Helper `screens/GitHubReposScreen/hooks/githubReposScreenErrorHelpers.ts` extrahiert; Hook bleibt Orchestrator fuer Repo-Aktionen, Busy-/Dialog-/Async-Flow, ohne beabsichtigten Verhaltenswechsel bei Alerts/Notice-Catches. Abgesichert durch neue Helper-Vertragstests plus bestehende GitHubReposScreen-Regressionen fuer Pull/Push und EAS-Status-Flow.

- Patch 575: kleiner Entflechtungsschritt im Hook-Hotspot `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` — die pure Artifact-/Notice-Textbildung (`sanitizeArtifactDetail`, `getArtifactUiMessage`) ist in den neuen lokalen Helper `components/CiLiteHeaderButton/hooks/ciLiteWorkflowNoticeHelpers.ts` extrahiert; Hook bleibt Orchestrator, Polling-/Timer-/Dispatch-/Artifact-Flow bleibt bewusst unveraendert, abgesichert durch neue Helper-Vertragstests plus bestehende Workflow-Behavior-Regressionen.

- Patch 574: kleiner Entflechtungsschritt im Regressions-Hotspot `contexts/AIContext/index.tsx` — der Inline-Block fuer API-Key-Rehydration (SecureStore-vs.-Legacy-Migration) plus wiederholte API-Key-Listen-Normalisierung wurde in kleine pure Helper `normalizeApiKeys`, `hasAnyApiKeys` und `resolveRehydratedApiKeys` in `contexts/AIContext/helpers.ts` extrahiert; `AIContext` bleibt Orchestrator, Persistenz-/Fallback-Semantik bleibt bewusst unveraendert, abgesichert durch neue Helper-Vertragstests plus bestehende AIContext-Persistenztests.

- Patch 573: kleiner Entflechtungsschritt im Regressions-Hotspot `hooks/useChatAIFlow.ts` — der gemischte Builder-/Validator-Result- und Fallback-Normalisierungsblock wurde in den neuen lokalen pure Helper `hooks/chatAIFlowResultHelpers.ts` (`normalizeResultFiles`, `readBuilderFilesOrThrow`) extrahiert; `useChatAIFlow` bleibt Orchestrator, Chat-/Plan-/Status-/Error-Semantik bleibt bewusst unveraendert, abgesichert durch neue Helper-Vertragstests plus bestehende Chat-Flow-Invariants.

- Patch 572: kleiner Entflechtungsschritt im Regressions-Hotspot `contexts/ProjectContext.tsx` — die wiederholte Rehydration-/Fallback-Normalisierung und der wiederholte Basisaufbau neuer Projektobjekte wurden in den neuen lokalen pure Helper `contexts/projectContextHelpers.ts` (`normalizeLoadedProjectData`, `buildProjectForCreation`) extrahiert; `ProjectContext` bleibt Orchestrator, Persistenz-/Storage-Flow und aussen sichtbare Context-Semantik bleiben bewusst unveraendert, abgesichert durch neue Helper-Vertragstests plus bestehenden `createNewProject`-Regressionstest.

- Patch 571: kleiner Entflechtungsschritt im Hook-Hotspot `screens/DiagnosticScreen/hooks/useDiagnosticFixRunner.ts` — Step-Plan-Erzeugung fuer Issue/Single/Batch (`buildIssueFixSteps`, `buildSingleFixSteps`, `buildBatchFixSteps`) und wiederholtes Step-Status-Mapping (`setStepStatusAtIndex`) wurden in den neuen lokalen pure Helper `screens/DiagnosticScreen/hooks/fixRunnerHelpers.ts` extrahiert; Orchestrierung, Status-/Toast-Semantik (`patch_applied`, `pending_recheck`, `failed`, `blocked`) und Modal-Abfolge bleiben bewusst unveraendert, abgesichert durch neue Helper-Vertragstests plus bestehenden Hook-Semantiktest.

- Patch 570: kleine Typ-/Fehlervertrags-Haertung in zwei eng begrenzten Hooks — `screens/AppInfoScreen/hooks/useAppInfoScreen.ts` entfernt den weichen Root-Cast `projectData as any`, liest Projektdateien/Assets ueber kleine lokale Guards (`toProjectFiles`, `isRecord`) und nutzt in Save-/Import-/Export-/Backup-Catches `unknown` + `getErrorMessage(...)`/`isAbortLikeError(...)` statt unsicherer `error.message`-Direktzugriffe; `components/CiLiteHeaderButton/hooks/useCiLitePatch.ts` haertet Patch-JSON-/Error-Pfade mit `parsed: unknown` plus lokalen Record-/Error-Guards und entfernt riskante `any`-Catch-/Parse-Ketten, bei unveraenderter Patch-/Alert-Fallback-Semantik.

- Patch 569: kleine Typ-/Payload-Haertung in zwei kompakten Hotspots — `components/ciLite/ciLiteUtils.ts` ersetzt riskante `any`-Grenzen in `findWorkflowRunByJobId(...)` und `normalizePreflightPatch(...)` durch kleine Run-/Record-Guards (`unknown` + Narrowing), sodass unvollstaendige Workflow-Run-/Patch-Payloads fail-safe gelesen werden, ohne Matching-/Fallback-Semantik zu aendern; `lib/appInfoBackup.ts` entfernt weiche `as any`-Objektzugriffe beim Backup-Import, validiert `config`/`apiKeys` strikt als Objektgrenzen und baut `validateApiBackupJson(...)` explizit ohne Blind-Cast auf, bei unveraenderter toleranter Provider-/mode-/quality-Fallback-Semantik.

- Patch 568: zwei naechste riskante Typ-/Fehlervertragsgrenzen gezielt gehaertet — `infra/github/files.ts` nutzt fuer GitHub-IO jetzt kleine Response-/Message-Typen plus `unknown`-sichere JSON-/Error-Guards statt unscharfer `any`-Payload-Zugriffe (u.a. Contents-/Branch-/Commit-/Tree-Responses), und `screens/DiagnosticScreen/hooks/useDiagnosticFixRunner.ts` ersetzt die zentralen Runner-Step-Catches auf `unknown` mit lokalem `getErrorMessage(...)`/Runtime-Meta-Narrowing fuer `partial`/`localChangeApplied`; kein beabsichtigter Flow-Umbau, nur defensive Vertrags-Haertung an den riskantesten Hotspot-Grenzen.

- Patch 567: verbleibende riskante `any`-Fehlervertraege in zwei High-Impact-Hotspots gezielt reduziert — `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` und `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` nutzen in zentralen Repo-/Branch-/Pull-/Push-/EAS-/CI-Lite-Dispatch-/Run-Lookup-Catches jetzt `unknown` plus kleine lokale Error-Guards statt direkter `any`-Property-Zugriffe; kein beabsichtigter Flow-/API-Vertragswechsel, nur defensive Typ-Haertung an Netzwerk-/Action-Grenzen.

- Patch 566: verbleibenden Triggered-Build-Template-Driftblock auf kleinere Shared-SoT gezogen — neue gemeinsame Quelle `shared/workflows/k1w1TriggeredBuildWorkflowTemplate.ts` enthaelt `WORKFLOW_K1W1_TRIGGERED_BUILD_TEMPLATE` als byte-aligned String-SoT zu `.github/workflows/k1w1-triggered-build.yml`; `lib/diagnostics/workflowTemplates.ts` exportiert `WORKFLOW_K1W1_TRIGGERED_BUILD` nur noch als Alias auf Shared, und `scripts/check_workflow_template_drift.sh` sowie `scripts/check_managed_workflows.sh` pruefen jetzt explizit Shared==Live plus Diagnostics-Alias auch fuer Triggered Build (kein beabsichtigter Workflow-Semantikwechsel).

- Patch 565: naechsten grossen Build/Release-Template-Driftblock auf kleinere Shared-SoT gezogen — neue gemeinsame Quelle `shared/workflows/easBuildReleaseWorkflowTemplates.ts` enthaelt `WORKFLOW_EAS_BUILD_TEMPLATE` und `WORKFLOW_RELEASE_BUILD_TEMPLATE` als byte-aligned String-SoT zu `.github/workflows/eas-build.yml` und `.github/workflows/release-build.yml`; `lib/diagnostics/workflowTemplates.ts` exportiert beide nur noch als Alias-Verdrahtung, und `scripts/check_workflow_template_drift.sh` sowie `scripts/check_managed_workflows.sh` pruefen jetzt explizit Shared==Live plus Diagnostics-Alias (kein beabsichtigter Workflow-Semantikwechsel, `WORKFLOW_K1W1_TRIGGERED_BUILD` bleibt bewusst lokal in Diagnostics).

- Patch 564: naechsten EAS-Link-Template-Driftblock auf kleinere SoT gezogen — neue Shared-Quelle `shared/workflows/easLinkWorkflowTemplate.ts` enthaelt den EAS-Link-Workflow-String als gemeinsame Quelle fuer Diagnostics (`lib/diagnostics/workflowTemplates.ts` re-exportet nur noch), waehrend `scripts/check_workflow_template_drift.sh` und `scripts/check_managed_workflows.sh` den finalen Vertrag jetzt auf Shared-SoT + Live-Workflow + Base-Template absichern; bewusst unveraendert bleiben die getrennten Live-/Base-Artefakte selbst (kein Workflow-Semantikwechsel).
- Patch 563: Managed-Workflow-Templates fuer CI Lite/Autofix auf eine gemeinsame kleine SoT gezogen — neue Shared-Quelle `shared/workflows/managedWorkflowTemplates.ts`, `infra/github/workflowTemplates.ts` und `supabase/functions/github-workflow-dispatch/index.ts` referenzieren dieselbe Exportquelle statt doppelter Inline-Bloecke; bestehende Drift-/Managed-Workflow-Guards und fokussierte Invariant-Tests wurden auf den Shared-SoT-Pfad aktualisiert, ohne beabsichtigten Workflow-Verhaltenswechsel.
- Patch 562: Preview-Vertrag fail-closed auf den produktiven Remote-SoT nachgeschaerft — `usePreview` kippt bei Remote-/Admin-Key-/Payload-Problemen im Standardpfad nicht mehr still auf lokalen HTML-/Eval-Fallback, lokaler Fallback ist nur noch im expliziten `preferredPreviewMode === "local"` erreichbar, und die bisherige stille `setPreferredPreviewMode("local")`-Promotion wurde entfernt; Builder-/UI-Texte markieren den lokalen `new Function(...)`-Pfad konsistent als expliziten Dev-/Best-Effort-Mode.
- Patch 561: offenen Edge-Auth-/JWT-/Rate-Limit-Block final ehrlich gezogen — `requireJwtRole(...)` verifiziert JWT-Rollen weiter fail-closed ueber Supabase Auth, unterscheidet jetzt aber klar `401 invalid/unverifiable` vs `500 server_misconfigured`; `android-keystore-generate` bleibt bewusst admin-only (mit durable + local rate-limit), waehrend die workflow-/build-/artifact-Routen den scoped Admin+CI-Bearer-Dualpfad wieder explizit enthalten und JWT-Rollenchecks auf dem Non-CI-Bearer-Pfad erzwingen.
- Patch 560: Prompt-/Validator-Kontext minimal gehaertet — `collectFocusTerms(...)` in `lib/promptEngine.ts` und `lib/aiContextBudget.ts` filtert jetzt zusaetzlich kleine englische Stopwords, und `buildValidatorMessages(...)` sendet neben dem budgetierten AI-JSON-Entwurf ein kompaktes Vollpfad-Manifest aller AI-Zielpfade, inklusive expliziter Kennzeichnung, wenn einzelne Pfade wegen Budget nicht vollstaendig inline im JSON enthalten sind; damit bleibt Budgetierung erhalten, aber stille AI-Pfad-Drops fuer den Validator entfallen.
- Patch 559: ZIP-Import-/Persistence-Vertrag minimal gehaertet und widerspruchsfrei gemacht — `importProjectFromZipFile(...)` arbeitet jetzt strikt all-or-nothing (keine Teiluebernahme/kein Skip-Pfad), `validateZipImport(...)` und Import-Fehlertexte sprechen dieselbe Wahrheit (inkl. leeres ZIP als harter Fehler), und `readDirectoryRecursive(...)` erzwingt das Dateilimit global ueber die gesamte Rekursion via gemeinsamem Zaehler statt pro Teilbaum; zusaetzlich blockiert ein kleiner Pre-unzip-Guard in `importProjectFromZipFile(...)` leere/unlesbare bzw. >25MB ZIP-Archive bereits vor `unzip(...)` (best-effort nur ueber Archivgroesse, keine Entry-Inspektion). Zu grosse Dateien und Limit-Ueberlaeufe brechen weiterhin frueh und ehrlich ab. Neue Regressionen decken den globalen Nested-Limit-Guard, den strikten Import-Error-Vertrag, den Pre-unzip-Guard und den Validator-Vertrag fuer invalid/leer ab.
- Patch 558: CI-Lite-Artifact-/Polling-Restpunkte im Header-Hook minimal gehaertet — `getArtifactUiMessage(...)` behaelt die klare sichere Hauptmeldung, zeigt jetzt aber zusaetzlich einen gekuerzten/sanitized `artifactError`-Detailhinweis (inkl. Redaction fuer Bearer/Admin-Key/gh*-Token- und `github_pat_...`-Muster), sodass Artifact-/Nachzug-Ursachen ehrlicher sichtbar bleiben; das starre `setInterval(..., 2500)` fuer Dispatch- und Autofix-Chain-Run-Lookups ist durch einen kleinen lokalen Timeout-Backoff (`1200 -> 1800 -> 2600 -> 3500 -> 4500ms`, gedeckelt) plus Generation-Guard gegen Stop/Unmount-Reschedule-Races ersetzt, und ein lokaler Artifact-Kontext-Guard verhindert Endlos-Refetch-Loops nach failed completed-run Artifact-Reads.
- Patch 556: verbleibenden Workflow-Restblock `github-run-artifact-json` auf denselben kontrollierten JWT+Admin-Key-Vertrag gezogen — Route laeuft jetzt mit `verify_jwt=true`, `requireScopedEdgeAuth` ohne CI-Bearer (`allowCiBearer: false`, `allowJwtAuthHeaderWithAdmin: true`) und serverseitigem JWT-Claim-Guard (`role in [service_role, authenticated]`); der direkte Artifact-Caller (`useCiLiteWorkflow`) sendet dafuer JWT + `x-k1w1-admin-key`, und `useGitHubActionsLogs` blockiert Runs/Logs lokal fail-fast statt Requests mit optionalem Admin-Key abzuschicken.
- Patch 555: naechsten eng angrenzenden JWT-/RBAC-Rollout fuer Build-Start/-Status umgesetzt — `trigger-eas-build` und `check-eas-build` laufen jetzt mit `verify_jwt=true`, serverseitigem Claim-Guard (`role in [service_role, authenticated]`) und scoped Workflow-Admin-Key (`K1W1_EDGE_WORKFLOW_ADMIN_KEY`) als zweitem Gate; der fruehere CI-Bearer-Pfad ist auf genau diesen beiden Routen deaktiviert, die App-Caller senden dafuer JWT + Admin-Key, und `github-run-artifact-json` bleibt in diesem Schritt bewusst unveraendert.
- Patch 554: naechsten eng angrenzenden JWT-/RBAC-Rollout fuer die Read-/Support-Routen `github-workflow-runs` und `github-workflow-logs` umgesetzt — beide Routen laufen jetzt mit `verify_jwt=true`, serverseitigem Claim-Guard (`role in [service_role, authenticated]`) und scoped Workflow-Admin-Key (`K1W1_EDGE_WORKFLOW_ADMIN_KEY`) als zweitem Gate; der bisherige CI-Bearer-Pfad ist auf diesen zwei Routen bewusst deaktiviert, die restliche Workflow-Familie bleibt unveraendert secret-basiert.
- Patch 553: `github-workflow-dispatch` als naechsten JWT-/RBAC-Pilot gehaertet — Route auf `verify_jwt=true` gehoben, serverseitigen Claim-Guard auf `role in [service_role, authenticated]` verengt (kein `anon`) und den Dispatch-Pfad auf die enge Kombination aus JWT + scoped Workflow-Admin-Key (`K1W1_EDGE_WORKFLOW_ADMIN_KEY`) umgestellt; der fruehere Dispatch-CI-Bearer-Pfad ist bewusst deaktiviert, waehrend andere Workflow-Routen unveraendert secret-basiert bleiben.
- Patch 552: gezielte JWT-/RBAC-Pilot-Haertung fuer `android-keystore-export` umgesetzt — Route laeuft jetzt mit `verify_jwt=true`, serverseitigem `role=service_role`-Claim-Guard und weiterhin scoped Admin-Key als zweiter Route-Gate, waehrend `github-workflow-dispatch` bewusst unveraendert secret-basiert bleibt; EAS-/Release-Caller senden fuer den Keystore-Export zusaetzlich `Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}`.
- Patch 551: verbleibende Doku-Drift nach den Modell-/Runtime-Merges minimal bereinigt — Summary-Dokumente verweisen jetzt wieder auf den aktuellen Katalog-/Default-Stand (u.a. OpenAI `gpt-5.4*`, Gemini `gemini-3.1-*`) statt auf den zwischenzeitlichen `gpt-5-mini`/`gpt-4.1-nano`-Text, und `patch_429.md` ist als historisch ueberholt markiert, damit alte Defaults nicht als heutige SoT gelesen werden.
- Patch 549: dokumentierte einen damaligen Zwischenstand des OpenAI-/Groq-Katalogs; dieser Stand wurde durch spaetere Runtime-/Katalog-Merges erneut konsolidiert. Fuer den aktuellen Branch-Stand sind `contexts/AIContext/models.ts` und `shared/ai/providerDefaults.ts` die massgebliche Modell-/Default-Wahrheit.
- Patch 548: produktiven KI-/CI-Lite-Runtime-Vertrag konsolidiert — `AIContext` und `k1w1-handler` teilen jetzt aktuelle runtime-unterstuetzte Default-Modelle, `runOrchestrator(...)` nutzt ehrliche same-provider-/cross-provider-Fallbacks inkl. sichtbarem Runtime-Hinweis und geschlossenem Abort-Race, und `k1w1-ci-lite`/Workflow-Templates/Dispatch verlangen `workflow_dispatch.inputs.ref` plus strikten `job_id`-Marker wieder durchgaengig; fokussierte Orchestrator-/CI-Lite-Regressionen und der volle Repo-Check sind gruen.
- Patch 547: `localRemoteDiffSection.truthfulness` im Default-Jest-Lauf wieder parallel-stabil gemacht und Doku-Sync fuer den aktuellen Modellkatalog-Stand nachgezogen — der flakige Context-Switch-Test in `__tests__/localRemoteDiffSection.truthfulness.test.tsx` wartet jetzt auf den tatsaechlich gewechselten Repo-/Branch-Labelzustand plus wieder aktivierten Refresh-Button statt nur auf das oft schon vorhandene `Push (0)`, wodurch der zweite Refresh nicht mehr gegen den noch laufenden alten Request verloren geht; README, `PROJECT_CHECKLOG.md` und `patch_547.md` dokumentieren zusaetzlich den bereits gelandeten Modellkatalog-/Settings-Metadaten-Stand konsistent nach.
- Patch 546: roten PR-407-CI-Lauf auf den echten Root Cause reduziert und lokal behoben — nicht die Jest-Suite war rot, sondern `workflow-lint.yml` via `check_patch_docs_sync.sh`, weil `README.md` noch Patch 544 meldete, waehrend `PROJECT_CHECKLOG.md` und `PATCHLOG_ROOT.md` schon auf 545 standen; README-/Patch-Doku sind jetzt wieder synchron, und `jest.setup.js` faengt zusaetzlich unhandled Promise Rejections testweit als Fail-fast-Guard ab.
- Patch 545: repo-weite Testhygiene fuer parallele Jest-Laeufe nachgeschaerft — `jest.setup.js` setzt blockierte Netzwerk-Globals jetzt pro Test neu auf, raeumt nach jedem Test zusaetzlich Spies/Timer/DOM konsistent auf, zwei Netzwerk-Helper-Tests initialisieren `global.fetch` nicht mehr nur implizit auf Dateiebene, lokale Fake-Timer werden in den betroffenen Regressionen explizit auf Real-Timer zurueckgefuehrt, und ein Preview-Hook-Test stellt prozessweiten Supabase-Env-Zustand nach jedem Fall wieder her.
- Patch 544: LocalRemoteDiffSection-Truthfulness-Test parallel-stabil gemacht — die Flake lag nicht in Produktlogik, sondern im Test selbst: nach einem Repo-/Branch-Wechsel wurde der zweite Refresh teils noch vor dem effect-getriebenen Reset des alten `loading`-Zustands ausgelöst. Der Test wartet jetzt explizit auf `Push (0)` als Reset-Signal und raeumt Cleanup/Timer/Mocks sauber zwischen den Faellen auf; der Default-Jest-Lauf ist wieder gruen.
- Patch 543: letzten bestaetigten App-Timeout-Rest geschlossen — `buildPollingService.ts` verwendet fuer `check-eas-build` jetzt denselben gemeinsamen `fetchWithTimeout(...)`-Helper wie die uebrigen App-Fetches statt lokaler Timer-/Abort-Duplikation, der tote `preview_page/helpers.ts`-`withTimeout(...)`-Rest ist entfernt, und `EnhancedBuildScreen` dokumentiert/typisiert seinen bewusst lokalen Gesamt-Deadline-Guard jetzt explizit als Wrapper ueber bereits helper-geschuetzte GitHub-Requests; typecheck/lint/diff-check sind gruen, waehrend der lokale Default-Jest-Lauf einmal am isoliert gruenen `localRemoteDiffSection.truthfulness`-Timeout haengenblieb.
- Patch 542: letzte Timeout-/Leak-Follow-ups nach Patch 541 konsolidiert — `invokeSavePreview(...)`, `invokeEdgeJson(...)`, `useGitHubActionsLogs` und `preview_page` nutzen jetzt ebenfalls den gemeinsamen `fetchWithTimeout(...)`-Vertrag statt lokaler Timer-/Abort-Duplikate, der `github-workflow-logs`-Timeout nennt keine signierte Download-URL mehr, und `k1w1-handler` verwendet fuer echte Provider-Upstreams ein gemeinsames 45s-Limit; fokussierte Regressionen sichern Preview-, Wizard-, Logs- und Leak-Vertrag ab.
- Patch 541: verbleibende Netzwerk-Haertung fuer App-/Edge-Fetch-Pfade konsolidiert — gemeinsame AbortController-basierte Timeout-Helper decken die verbleibenden Client-/Edge-Fetch-Pfade in `infra/github/*`, `useCiLiteWorkflow`, `useConnectionsScreen`, `retryWithBackoff` sowie den betroffenen GitHub-/CodeSandbox-/Provider-Functions ab, echte Aborts werden nicht mehr weg-retried, und `github-run-artifact-json` plus der deploybare `test`-Endpoint haengen jetzt ebenfalls am bestehenden Rate-Limiter.
- Patch 540: verbliebenen `useGitHubActionsLogs`-Pending-Guard-Race nach Patch 539 minimal geschlossen — der Pending-Guard ist jetzt an die konkret aktive Request-Version gebunden statt an einen globalen Boolean, sodass ein spaetes `finally` eines alten aborteten Requests den neuen laufenden Selection-Fetch nicht mehr freigeben kann; fokussierte Hook-Regression deckt aborteten Alt-Request, neuen Selection-Request und den blockierten Parallel-Refresh fuer dieselbe Auswahl ab.
- Patch 539: zwei echte `useGitHubActionsLogs`-Follow-up-Regressionen nach Patch 538 minimal geschlossen — `fetchLogs()` erhoeht `requestVersionRef` erst noch, wenn nach dem Pending-Guard wirklich ein neuer Request startet, damit ein zweiter `refreshLogs()`-Aufruf laufende Antworten nicht still invalide macht oder `isLoading` haengen laesst, und der Selection-Reset fuer `[githubRepo, runId, workflowId]` laeuft vor dem Auto-Refresh-Effekt, damit der erste legitime Fetch nach einem Auswahlwechsel nicht vom eigenen Reset abortet wird.
- Patch 537: verbleibenden `preview_page`-Timer-/Cleanup-Restpunkt nach PR 399 minimal geschlossen — `fetchPreviewRecord()` prueft den `supabaseHeaders()`-/Partial-Env-Fall jetzt vor `withTimeout(8000)`, sodass in fehlkonfigurierten Preview-Envs kein unnoetiger Pending-Timer/Abort-Callback offen bleibt, waehrend `preview_env_missing` und der browser-facing HTML-Fehlerpfad unveraendert bleiben.
- Patch 536: `preview_page`-Follow-up nach PR 398 minimal geschlossen — der Partial-Env-Fall (`PREVIEW_SUPABASE_URL` vorhanden, `PREVIEW_SERVICE_ROLE_KEY` fehlt) wird im Lookup jetzt explizit als `preview_env_missing` statt `preview_db_error` klassifiziert, und browser-/QR-seitige Env-/DB-/Lookup-Fehler bleiben konsequent auf dem sicheren HTML-Fehlerpfad mit erhaltenem Header-/Marker-/Status-Vertrag statt rohem JSON.
- Patch 535: Preview-Edge-Fehlervertrag gehaertet — `save_preview` und `preview_page` klassifizieren jetzt bekannte Env-/Payload-/DB-/NotFound-/Expired-/Response-/Runtime-Faelle ueber strukturierte Preview-Fehlercodes statt blindem `Internal Server Error`; der Client liest diese Codes in `invokeSavePreview(...)`/`describeRemotePreviewFailure(...)` stabil aus und meldet ehrlichere, aber sichere Ursachen.

- Patch 534: PreviewScreen-Layout-Restpunkte nach PR 396 geschlossen — der Scroll-Hauptinhalt nutzt wieder eine explizite Fill-Struktur statt nur intrinsischer Hoehe, die BottomBar wird ueber denselben Scroll-Vertrag sauber am unteren Bereich gehalten, und auf iOS bleibt genau ein Bottom-Safe-Area-Pfad aktiv, sodass kein doppelter Leerraum hinter den Action-Buttons entsteht.

- Patch 533: PreviewScreen auf schmalen Mobilgeraeten stabilisiert — der bestehende Drawer-Preview-Pfad ist gegen alte Duplikate verifiziert, Toolbar und Statusleiste wechseln fuer enge Breiten in einen kompakten Wrap-/Mehrzeilenmodus statt Actions in eine einzige kippelige Zeile zu pressen, die Hauptstruktur verankert DeviceFrame/Footer klarer, ein kleiner Runtime-Hinweis markiert den aktiven Renderpfad, und fokussierte Preview-Regressionen decken Narrow-Toolbar, strukturelle Stabilitaet und sichtbare Fehler-/Fallback-Zustaende ab.

- Patch 532: produktiven k1w1-handler-Fehlervertrag fuer Provider-/Env-/Modellfehler gehaertet — `helpers.ts` klassifiziert bestehende interne Provider-/Env-/HTTP-Fehler jetzt in sichere strukturierte Codes (`provider_env_missing`, `provider_http_401/403/404/429`, `provider_model_not_found`, `provider_upstream_error`, `invalid_request_payload`, `unsupported_provider`, `unknown_internal_error`), `index.ts` liefert diese Payloads statt generischem `Internal Server Error`, und `lib/orchestrator/k1w1Edge.ts` uebernimmt die strukturierten Ursachen in ehrliche Chat-Meldungen ohne rohe Upstream-Bodies oder Secret-Leaks.

- Patch 531: Runtime-Dependency-Diagnose kann fehlende Runtime-Pakete jetzt nicht mehr nur ueber harte Expo-Safe-Mappings auto-fixen, sondern bevorzugt exakte bereits im `package-lock.json`/`npm-shrinkwrap.json` aufgeloeste Versionen wiederverwenden; dadurch erscheinen reale Runtime-Import-Fehler deutlich haeufiger mit ehrlichem lokalem Auto-Fix statt nur als KI-/Manual-Follow-up.

- Patch 530: lokale Preflight-Diagnose erkennt jetzt offensichtliche Runtime-Import→Dependency-Mismatches vor CI Lite; strukturierte `findings` benennen Paket, Fundstellen, Kategorie/Fixbarkeit und sichere Expo-SDK-54-AutoFixes ergaenzen gezielt `expo-linear-gradient` bzw. `expo-blur` in `package.json`, waehrend unsichere Faelle bewusst diagnose-only bleiben.

- Patch 528: CI-Lite-/Build-Readiness-Contracts klein konsolidiert — `ciLitePersistence` exportiert jetzt kanonische Reason-/Workflow-Konstanten fuer den repo-/branch-scoped Snapshot-Vertrag, `buildReadiness` mappt CI-Lite-Blocker darueber statt ueber verstreute Stringvergleiche, und eng ueberschneidende Jest-Faelle fuer Mismatch bzw. stale/SHA-Mismatch wurden per `it.each(...)` ohne Schutzverlust zusammengezogen.

- Patch 526: Remote-Preview-Payload im echten App-Pfad minimal repariert — `usePreview` sendet `save_preview` jetzt ueber einen expliziten JSON-`fetch` mit `x-k1w1-admin-key` und validiertem `files`-Payload statt ueber den vorherigen fehleranfaelligen Invoke-Weg, normale Projekte liefern dadurch wieder nicht-leere Remote-Dateien an den Server, und leer/weggefilterte Dateimengen werden in `previewHelpers` ehrlich als Datei-/Payload-Ursache kommuniziert statt mit Auth vermischt.

- Patch 525: Repo-Secrets-Refresh-/Load-Loop in `SecretsSection` minimal entkoppelt — der automatische Initial-Load liest den fuer Refresh-/stale-Entscheidungen benoetigten „es gab bereits eine verifizierte Liste“-Status jetzt ueber einen kleinen `hasVerifiedNamesRef` statt ueber die `names`-Hook-Dependency; dadurch feuert `useEffect(() => load(), [load])` nicht mehr nach jedem erfolgreichen `setNames(...)` erneut, waehrend manueller Refresh, Repo-Wechsel-Invalidierung und die bisherige ehrliche Fehler-/Last-known-list-Semantik erhalten bleiben.

- Patch 524: CI-Lite-Chain-Run-Korrelation deterministisch und final konsolidiert — `useCiLiteWorkflow` verlangt fuer den Autofix→CI-Lite-Folgepfad denselben expliziten `job_id`-Marker wie fuer manuelle `workflow_dispatch`-Runs, behandelt `sourceHeadSha` nur noch als zusaetzlichen Guard/Sorter innerhalb marker-passender Kandidaten und haelt die pure Matching-Logik im finalen Branch-Stand in `workflowRunMatching.ts` statt als Test-Only-Export am Hook; fokussierte Regressionen sichern denselben Chain-/Manual-Contract weiter ab.

- Patch 523: GitHubRepoScreen-/Infra-Push-/Pull-Pfad minimal gehaertet — `normalizeRepoPath()` blockiert jetzt `..`-Traversal-Segmente statt sie still durchzulassen, `pushFilesToRepoAdvanced(...)` lehnt solche lokalen Repo-Pfade mit klarer Fehlermeldung vor dem GitHub-Write ab, und `pullFromRepo()` bricht bei mehr als 200 erlaubten Textdateien ehrlich vor dem Blob-Load ab statt sehr grosse Repos ungebremst in den App-Memory zu ziehen.

- Patch 522: lokalen Edge-Admin-Key-Save-Flow im Credentials Wizard minimal gehaertet — `onSaveAdminKey()` nutzt jetzt dieselbe formale `isLikelyValidAdminKey(...)`-Pruefung wie die restliche lokale Key-Diagnose, behandelt leer/Whitespace weiter als Loeschpfad, blockiert nicht-leere formal ungueltige Werte mit klarer UI-Meldung vor dem Persistieren und laesst nur formal gueltige Keys in SecureStore; fokussierte Regressionen decken Delete/Invalid/Valid-Save ab.

- Patch 521: lokaler Edge-Admin-Key-HTTP-Vertrag im Wizard-/Preview-/CI-Lite-Scope bereinigt — `invokeEdgeJson(...)` sendet fuer lokale Admin-Key-Edge-Calls keinen semantisch falschen `Authorization: Bearer ...`-Header mehr, sondern nur noch `x-k1w1-admin-key`; gezielte Regressionstests sichern den Header-Vertrag fuer Wizard, Remote-Preview und CI Lite, waehrend die bestehende ehrliche `missing`/`invalid`/`rejected`-Diagnose sowie Fresh-vs-Persist-Truthfulness regressionsfest bleiben.

- Patch 520: lokale Edge-Admin-Key-Diagnose in Wizard, Preview und CI Lite ehrlich nachgezogen — `missing` gilt nur noch fuer wirklich leere lokale App-Keys, formal kaputte Werte bleiben `invalid`, vorhandene aber serverseitig mit 401/403 bzw. `missing or invalid admin` abgelehnte Keys werden als `rejected` kommuniziert, und fokussierte Regressionen sichern die konsistente Nutzerkommunikation ohne Secret-Leaks.
- Patch 519: AppInfo-Backup-/Import-Pfad fuer Secrets/Tokens ehrlich getrennt und gehaertet — der bisherige Klartext-"Full-Backup"-Pfad ist durch passwortgeschuetzte AES-GCM-Backups fuer `Secrets/Tokens/Connections` bzw. optional `AI-/KI-Konfiguration + Secrets/Connections` ersetzt, API-/KI-Konfiguration bleibt separat exportierbar, und neue Regressionen sichern Verschluesselung, falsche Passphrase, Legacy-Klartext-Ablehnung sowie die Abwesenheit von Projektdateien/Chat-Inhalten.

- Patch 518: lokaler Edge-Admin-Key-Vertrag fuer Wizard/Preview/Build ehrlich und konsistent gezogen — `useCredentialsWizardScreen` rehydriert und persistiert den lokalen Key-/Status jetzt nach Save/Fokus projektbezogen, `signingKeyGate` gibt Build-Readiness und One-Click-Deploy denselben praezisen lokalen Blockergrund, und `previewHelpers` benennt fehlende/abgelehnte `x-k1w1-admin-key`-Faelle offen, waehrend der lokale HTML-Fallback klar sekundaer bleibt.

- Patch 517: Preview-Vertrag auf den vorhandenen Expo-/WebView-Remote-Pfad als klare Produkt-SoT geschaerft — `usePreview`/`usePreviewScreen` markieren den vertrauenswuerdigen Supabase-URL-Pfad jetzt noch expliziter als primaeren Weg, der lokale HTML-/Eval-Pfad bleibt im UI, im Builder und in den Status-Texten klar als Dev-/Best-Effort-Fallback eingegrenzt, und gezielte Preview-Regressionen sichern Remote-vor-Fallback sowie die neue Sekundaer-Semantik ab.

- Patch 516: finalen unbenutzten Legacy-Key-Manager-Rest entfernt — `lib/SecureKeyManager.ts` samt Test ist geloescht, der bestehende Key-Manager-Invariant blockiert die Rueckkehr jetzt dateiweit, und `docs/SYSTEM_README.md` beschreibt den produktiven KI-Vertrag wieder ohne veraltete Key-/Token-Manager-Hinweise.

- Patch 515: verbliebenen GitHub-PAT-Passthrough im produktiven Client→Edge-Workflow-/Run-/Log-Pfad entfernt — `useGitHubActionsLogs` und `useCiLiteWorkflow` senden kein `githubToken` mehr an `github-workflow-runs`, `github-workflow-logs` bzw. `github-workflow-dispatch`, die drei Edge-Functions lesen den GitHub-Token konsistent nur noch ueber `_shared/github.ts`, und neue Vertrags-/Invariant-Tests blockieren Rueckfaelle bei Token-Body-Passthrough sowie unveraenderten Admin-/Workflow-Guards.

- Patch 514: verbliebenen produktiven Build-/Preview-Env-Restpfad auf die gemeinsame Shared-Helper-Linie gezogen — `check-eas-build`, `preview_page/helpers.ts` und `save_preview` bleiben auf `_shared/auth.ts`, `trigger-eas-build` liest das GitHub-Token ueber `_shared/github.ts` nun ebenfalls via `getRuntimeEnv(...)` statt via direktem `Deno.env.get(...)`, und ein erweiterter Invariant-Test blockiert Rueckfaelle bei Helper-Nutzung und unveraenderten Guard-Vertraegen.

- Patch 513: Key-/Token-Manager-Restpfade nach der Edge-Proxy-Umstellung bereinigt — `AIContext` spiegelt keine Provider-API-Keys mehr in `SecureKeyManager`, `SecureTokenManager` ist aus dem Repo entfernt, `SecureKeyManager` bleibt nur noch als expliziter Legacy-/Test-Helper dokumentiert, und ein neuer Invariant-Test blockiert direkte Runtime-Nutzung bzw. Token-Manager-Drift.

- Patch 512: verbliebene direkte Client-Provider-Altpfade im Orchestrator entfernt — `lib/orchestrator/providers/*` und die daran haengenden clientseitigen Provider-Tests sind geloescht, `SecureKeyManager` bleibt explizit als lokaler Key-/Rotations-State fuer `contexts/AIContext` eingegrenzt, und ein neuer Invariant-Test blockiert die Wiedereinbindung direkter Client-Provider-Imports im produktiven Runtime-Scope.

- Patch 511: verbleibende produktive Supabase-Edge-Entry-Points auf native `Deno.serve(...)`-Bootstrap-Linie vereinheitlicht — `check-eas-build`, `trigger-eas-build`, `github-workflow-dispatch`, `github-workflow-runs`, `github-workflow-logs`, `k1w1-handler`, `save_preview`, `preview_page` und `create_codesandbox` nutzen keinen alten `std/http/server.ts`-Import mehr; bereinigte Helper schleppen keine toten `serve`-Reexports/-Imports weiter, und ein gezielter Invariant-Test blockiert neue Serve-Drift.

- Patch 510: verbleibende Android-Keystore-Export-/Status-Secret-Lesepfade auf die bestehende Shared-Secret-/Auth-Linie gezogen — `_shared/auth.ts` liefert jetzt kleine Getter fuer `SUPABASE_URL` und `SIGNING_MASTER_KEY`, die drei Keystore-Pfade `android-keystore-generate`, `android-keystore-export` und `android-keystore-status` nutzen dadurch keine parallelen direkten `Deno.env.get(...)`-Reads mehr, und gezielte Invariants halten Helper-Reexports sowie unveraenderte Admin-/CI-Guards regressionsfest.

- Patch 509: verbleibende repo-weite Edge-Restpfade ohne Produktlogik-Änderung auf request-gebundene Shared-CORS-Header gezogen — die disabled Legacy-Functions `check-lint`, `trigger-lint`, `check-native-sync`, `trigger-native-sync`, `native-sync-report`, `native-sync-report-ingest` senden lokal keine `Access-Control-Allow-Origin: *`-Antworten mehr und halten fuer die disabled Stubs den sichtbaren `status: 410`-Vertragsanker ueber `corsHeadersForRequest(req)`; `test` bleibt fuer die 200-Antwort bei request-gebundenem `jsonResponse({ ok: true }, req)`; ein erweiterter Invariant-Test haelt Restpfade, Preflight und die bereits gehaerteten Kernpfade regressionsfest.

- Patch 508: `LocalRemoteDiffSection` invalidiert laufende Diff-/Preview-Asyncs jetzt auch beim Unmount, damit spaete Promise-Ruecklaeufer nach Teardown keinen alten UI-State mehr committen; der Truthfulness-Test deckt den Unmount-/Late-Resolve-Fall zusaetzlich als Regression ab.

- Patch 507: repo-seitige DB-Referenz fuer Public-Schema-/Diagnostics-Vertraege ehrlich gezogen — `supabase/schema.public.sql` ist jetzt eine explizit sekundäre Snapshot-Referenz statt leerer Schein-SoT, zeigt nur die aktuell relevanten Public-Contracts (`diagnostic_uploads`, `diagnostics_reports`, `insert_diagnostic_upload(jsonb)`), und ein neuer Invariant-Test haelt Snapshot, Migrations-SoT sowie finalen bigint-/Grant-Stand widerspruchsfrei.

- Patch 506: Diagnostic-Upload-Auth-Vertrag explizit auf den echten Clientpfad ausgerichtet — direkter Client-RPC auf `insert_diagnostic_upload(jsonb)` bleibt der finale Vertrag, eine kleine Folgemigration gibt `anon` bewusst wieder frei, und gezielte Regressionen/Invariants sichern RPC-Pfad, Grant-Stand sowie unveraenderte Payload-/ID-Haertungen ab.

- Patch 505: irrefuehrende Service-Role-Secret-Pfade bereinigt — `android-keystore-generate` nutzt fuer den serverseitigen Service-Role-Key jetzt denselben Shared-Auth-Helper wie angrenzende Signing-Pfade, und der tote Client-Storage-/Backup-Cleanup-Hinweis fuer `SUPABASE_SERVICE_ROLE_KEY` wurde auf einen expliziten Legacy-Cleanup-Helper ohne normalen `STORAGE_KEYS`-Slot zurueckgebaut.
- Patch 504: verbleibende Wildcard-CORS-Nutzung in geschuetzten produktiven Edge-Functions entfernt — `k1w1-handler`, `github-workflow-runs` und der eng angrenzende `github-workflow-logs` nutzen fuer lokale Antwortpfade jetzt request-spezifische Header aus `_shared/cors`, `handleCors(req)` behaelt das bestehende Preflight-Verhalten, und neue Invariants sichern Origin-Reflection sowie das Entfernen von `Access-Control-Allow-Origin: *`.
- Patch 503: Confirm-/Review-UX fuer KI-Aenderungen diff-orientierter gemacht — `ConfirmChangesModal` zeigt vorhandene `changePreviews` jetzt dateibezogen als kompakte Review-Karten fuer neue/geaenderte Dateien, stellt Builder-vs-Validator-Provenance ueber `finalFileSource`/`validatorState` sichtbar advisory dar, und blendet uebersprungene sowie geblockte Hinweise klar ausserhalb der langen Summary ein; fokussierte Jest-Regressionen sichern Vorschau, Delta-Ausschnitte und den bestehenden Accept/Reject-Flow ab.
- Patch 502: Prompt-/Write-Contract auf echte Chat-Ownership-Policy synchronisiert — `effectiveWritePolicy` arbeitet jetzt mit realen Chat-Guard-Probes statt der groben Path-Allowlist, markiert kritische/manual-only und baseline-verwaltete Pfade explizit als nicht normal schreibbar, und Prompttexte fuer Planner/Builder/Validator halten den priorisierten Snapshot statt Vollrepo-Sicht ehrlich sichtbar; fokussierte Jest-Regressionen sichern Prompt-Policy-Sync und unveraenderte Guard-Haerte.
- Patch 501: produktiven Chat-Sendepfad gegen lokale Preview-/Secret-Leaks gehaertet — `useChatAIFlow` validiert/sanitizt echte KI-Requests jetzt nochmals direkt vor Planner/Builder/Validator, lokale Meta-/Preview-Nachrichten werden mit klaren Flags sichtbar aber provider-exkludiert gehalten, und Prompt-Redaction deckt zusaetzlich Cookie-/Credential-Muster ab.
- Patch 500: produktiven AI-Flow auf den vorhandenen Supabase-Edge-Proxy `k1w1-handler` umgestellt — der Client sendet fuer Groq/OpenAI/Anthropic/Gemini/HuggingFace keine lokalen Provider-Keys mehr direkt an externe Provider-Endpunkte, `runOrchestrator(...)` nutzt stattdessen `supabase.functions.invoke(...)` mit kompatibler Response-/Timeout-/Abort-Normalisierung, und gezielte Orchestrator-Regressionen sichern Invoke-Wiring sowie Result-Mapping ab.
- Patch 499: GitHubReposScreen macht die Source of Truth fuer `EXPO_TOKEN` und `K1W1_EDGE_ADMIN_KEY` ehrlich sichtbar — `SecretsSection` trennt jetzt `Repo Secret` und `Lokaler App-Wert`, warnt bei bestaetigten Repo-Secrets aber fehlendem lokalem SecureStore-Wert explizit vor false-green CI-Lite-/Dispatch-Annahmen, und der CI-Lite-Modalhinweis sagt klar, dass App-Dispatch den lokalen Edge Admin Key braucht.
- Patch 498: GitHubReposScreen `SecretsSection` auf Diagnostics-gleiche Repo-Secret-Semantik gezogen — kleiner Shared-Helper mappt bestätigte GitHub-Secret-Namenslisten jetzt auf `verified`/`missing`, 401/403 auf `auth_error`, generische Fetch-Fehler auf `unknown` und Recheck-Fehler nach bereits bestätigter Liste auf `stale`; die Section rendert dadurch keine fake-missing UI mehr aus `names=[]`, und fokussierte Jest-Regressionen decken verified/missing/auth/unknown weiter ab.
- Patch 497: Pipeline-Diagnostics Repo-Secret-Contract / Title-Truthfulness — erfolgreich geladene Repo-Secret-Namenslisten werden für `EXPO_TOKEN`, `SUPABASE_URL` und `SUPABASE_SERVICE_ROLE_KEY` jetzt explizit als `verified` oder `missing` in den bestehenden Verification-Contract eingespeist; `unknown`/`auth_error` bleiben auf echte Fehlerpfade beschränkt, Secret-Checks nutzen zustandsabhängige Titel/Fix-Hinweise statt pauschalem `Repo Secret vorhanden`, und gezielte Jest-Regressionen decken verified/missing/auth/title-semantics ab.

Append-only Überblick über Patch-Notizen.

## Recent (kompakt)
- Patch 620: verbleibenden serverseitigen JWT-/RBAC-Drift im Shared-Auth-Role-Read-Pfad behoben — Ursache war ein Priorisierungsfehler im verifizierten Rollen-Read (`user.role` wurde vor JWT-Claim gelesen); bei korrekt verifiziertem Operator-JWT (`role=build_admin`) kam ueber `auth/v1/user` oft `role=authenticated` zurueck und fuehrte zu faelschlichem `Forbidden ... role is not allowed`. `requireJwtRole(...)` liest jetzt nach erfolgreicher JWT-Verifikation zuerst den verifizierten Token-Claim (`role`, danach `app_metadata.role`) und nutzt `auth/v1/user` nur noch defensiv als Fallback, weiterhin fail-closed auf `service_role|build_admin`; zusaetzlich wurden die zwei bekannten Edge-Typecheck-Restpunkte in `_shared/auth.ts` sauber typisiert und eine Regression im Auth-Test ergänzt.
- 2026-03-23: Patch 540: verbliebenen `useGitHubActionsLogs`-Pending-Guard-Race nach Patch 539 minimal geschlossen — der Pending-Guard ist jetzt request-versionsgebunden, sodass der Selection-Reset alte Requests weiter korrekt abortet, deren spaetes `finally` aber keinen neueren laufenden Request fuer die neue Auswahl mehr freigeben kann; fokussierte Hook-Regression deckt genau diesen Abort→Neu-Request→stale-`finally`-Race ab.
- 2026-03-22: Patch 539: `useGitHubActionsLogs` schliesst die zwei echten Follow-up-Regressionen aus Patch 538 minimal — `requestVersionRef` wird erst beim echten Request-Start erhoeht, sodass ein zweiter `refreshLogs()`-Aufruf den aktiven Fetch nicht mehr selbst invalide macht, und der Selection-Reset laeuft vor dem Auto-Refresh-Effekt, damit der erste neue Fetch nach Repo-/Run-Wechsel nicht sofort wieder abortet wird.
- 2026-03-22: Patch 534: PreviewScreen-Layout-Restpunkte nach PR 396 geschlossen — `PreviewScreen.tsx` fuehrt fuer den Scroll-Hauptpfad wieder eine explizite Fill-Struktur ein, `PreviewScreen.styles.ts` verankert BottomBar und Hauptinhalt ueber flex-/auto-margin-orientierte Regeln, `contentInsetAdjustmentBehavior` bleibt bewusst auf `never`, und fokussierte Preview-Layout-Tests sichern Fill-Vertrag plus Single-Safe-Area-Vertrag regressionsfest.
- 2026-03-22: Patch 533: PreviewScreen auf schmalen Mobilgeraeten stabilisiert — der bestehende Drawer-Preview-Pfad ist gegen alte Duplikate verifiziert, Toolbar und Statusleiste wechseln fuer enge Breiten in einen kompakten Wrap-/Mehrzeilenmodus statt Actions in eine einzige kippelige Zeile zu pressen, die Hauptstruktur verankert DeviceFrame/Footer klarer, ein kleiner Runtime-Hinweis markiert den aktiven Renderpfad, und fokussierte Preview-Regressionen decken Narrow-Toolbar, strukturelle Stabilitaet und sichtbare Fehler-/Fallback-Zustaende ab.
- 2026-03-21: Patch 528: kleiner Stabilitaets-/Cleanup-Durchgang nach den CI-Lite-/Build-Readiness-Folgeaenderungen — `lib/ciLitePersistence.ts` haelt Persistenzgruende und das kanonische CI-Lite-Workflow-Id jetzt als kleine gemeinsame Konstanten, `lib/buildReadiness.ts` nutzt dieselbe Quelle fuer ReasonCode-Mapping statt duplizierter Stringlisten, und fokussierte Jest-Regressionen wurden an den klaren Verantwortlichkeiten leicht entdupliziert, ohne CI-Lite-/Build-Verhalten zu aendern.
- Patch 512: alte direkte Client-Provider-Helfer unter `lib/orchestrator/providers/*` sowie deren verbleibende clientseitige Provider-Tests sind entfernt; der produktive Orchestrator bleibt ausschliesslich auf `invokeK1w1Handler(...)` verdrahtet, und ein neuer Invariant-Test blockiert direkte Client-Provider-Imports im Runtime-Scope.
- Patch 509: restliche disabled Legacy-Edge-Stubs halten fuer lokale Disabled-Antworten jetzt den sichtbaren `status: 410`-Vertragsanker und nutzen request-gebundene Shared-CORS-Header via `corsHeadersForRequest(req)` statt `...corsHeaders`; `test` bleibt fuer die lokale 200-Antwort bei `jsonResponse({ ok: true }, req)`; die CORS-Invariants decken diese Restpfade zusaetzlich ab.
- Patch 508: `LocalRemoteDiffSection` invalidiert laufende Diff-/Preview-Asyncs jetzt auch beim Unmount; der Truthfulness-Test deckt den Unmount-/Late-Resolve-Fall explizit als Regression ab.
- Patch 507: `supabase/schema.public.sql` ist als abgeleitete Sekundaerreferenz fuer aktuelle Diagnostics-Public-Contracts nachgezogen; Migrationen + DB-/RPC-Invariants bleiben die kanonische SoT, und der Snapshot behauptet keine Legacy-UUID-/Spaltenwelt mehr.
- Patch 505: Shared-Secret-SoT fuer `android-keystore-generate` auf den bestehenden Auth-Helper gezogen; irrefuehrenden Client-Storage-Slot `STORAGE_KEYS.SUPABASE_SERVICE_ROLE_KEY` entfernt und auf expliziten Legacy-Cleanup ohne normale Backup-/UI-Semantik reduziert.
- Patch 504: request-spezifische Shared-CORS-Header fuer `k1w1-handler`, `github-workflow-runs` und den angrenzenden `github-workflow-logs`; lokale Erfolgs-/Fehlerpfade nutzen keine Wildcard-Origin mehr, Preflight bleibt ueber `handleCors(req)` intakt.
- Patch 499: GitHubReposScreen trennt Repo-Secret-Namen und lokale Laufzeit-Credentials fuer `EXPO_TOKEN` und `K1W1_EDGE_ADMIN_KEY` sichtbar; bestaetigte Repo-Secrets bleiben getrennt von lokalem SecureStore-Status, und CI Lite / Edge Dispatch wird explizit als lokaler App-Key-Pfad benannt.
- Patch 498: GitHubReposScreen `SecretsSection` spricht jetzt dieselbe Repo-Secret-Semantik wie Diagnostics — erfolgreiche Secret-Namenslisten werden explizit als `verified`/`missing` ausgewertet, 401/403 bleiben `auth_error`, generische Fetch-Fehler bleiben `unknown`, und ein Recheck-Fehler nach bereits bestaetigter Liste wird als `stale` statt als fehlend angezeigt.
- Patch 496: GitHubReposScreen EAS-Link-Statuschecks gegen stale Async und Race-Zustände gehärtet — ein kleiner Request-/Generation-Guard bindet jeden Check an `repo@@branch`, Repo-/Branch-Wechsel invalidieren laufende Checks und resetten den Screen bewusst auf neutral/unknown, ältere Requests dürfen weder neuere Statusläufe noch den Write→Recheck-Pfad überschreiben, und manuelle Checks wie Rechecks teilen dieselbe Guard-Wahrheit.
- Patch 495: `LocalRemoteDiffSection` Same-Context-Truthfulness für lokale Dateiänderungen gehärtet — ein kleiner lokaler Fingerprint bindet die geladene Diff-/Preview-Wahrheit jetzt an den aktiven lokalen Dateistand; Änderungen im selben Repo-/Branch-Kontext invalidieren Liste, Push-Selection, Inline-/Modal-Preview und Preview-Cache ehrlich statt alte Diff-Wahrheit still weiterzuzeigen.
- Patch 494: `LocalRemoteDiffSection` stale-async-/Cache-/Reset-Wahrheit gehärtet — Repo-/Branch-Wechsel invalidieren laufende Diff-/Preview-Requests jetzt hart, resetten Liste/Selection/Inline-/Modal-Preview ehrlich auf neutral und binden den Preview-Cache an den aktiven Kontext statt alte Datei-/Diff-Wahrheit mitzuschleppen.
- Patch 493: GitHubReposScreen EAS-Link-/Repo-Semantik gehärtet — kleiner gemeinsamer `easLinkContract` trennt jetzt `verified`, `workflow_missing`, `project_missing`, `project_invalid`, `project_mismatch`, `auth_error`, `unknown` und `pending_recheck`; der Repo-Check prueft Workflow und erwartete `projectId` ehrlich getrennt, und der Write-Pfad meldet nur noch echte Verifikation oder klares Re-Check-Pending statt false-green `linked/ok`.
- Patch 492: GitHubReposScreen Pull-/Push-/Sync-Wahrheit gehärtet — Pull-Apply wartet jetzt echte lokale Writes vor Sync-Signatur/Refresh/Success ab, no-op/teilweise angewendet/fehlgeschlagen werden ehrlich getrennt, `skipConflicts` erzeugt keinen false-green Sync mehr, und Push meldet ohne Branch oder echte Dateiauswahl keinen Erfolg.
- Patch 491: Diagnostics-Fix-Engine-/UX-Semantik gehärtet — kleiner gemeinsamer `fixResultContract` trennt jetzt Advisory, lokalen Patch-Apply, Workflow-Dispatch, Blocker, Fehlversuch und Re-Check-Pending; leere/no-op oder guard-blockierte Patches enden nicht mehr als „behoben“, teilweise angewendete Fixes bleiben explizit teilweise/fehlgeschlagen, Workflow-only-Fixes erscheinen als gestartet/recheck-pending statt lokal repariert, und Badge-/Action-/Toast-Texte sprechen dieselbe Wahrheit.
- Patch 490: CredentialsWizard-Trust-/Safety-/Status-Härtung — Wizard-Zustände unterscheiden jetzt ehrlich zwischen `verified`, `missing`, `unknown`, `auth_error`, `generated_pending_verification` und laufenden Busy-Aktionen; Generate bleibt bis zur echten Nachverifikation pending, Refresh/Recheck verfälscht Auth-/temporäre Fehler nicht mehr zu "fehlt", und ein gemeinsamer Busy-Guard blockiert widersprüchliche Parallelaktionen.
- Patch 489: Preview-/Server-Contract minimal gehärtet — Remote-Preview, lokaler HTML-Fallback, unavailable/loading/failure und unsichere/ungültige Remote-URLs werden jetzt über einen gemeinsamen Preview-Status ehrlich unterschieden; lokale Fallbacks bleiben nutzbar, erscheinen aber nicht mehr als „grüne“ Server-Preview, und die Preview-WebViews laufen mit engerem Mixed-Content-Setting.
- Patch 488: gemeinsamer `verificationContract` vereinheitlicht Secret-/EAS-/Readiness-Semantik über ConnectionsScreen, Build-Readiness und Diagnostics; Auth/Permission/Unknown/Stale werden nicht mehr als „fehlt“ verfälscht, inklusive gezielter Jest-Regressionen.
- Patch 487: zweite AI-Flow-Welle konservativ abgeschlossen — produktive Chat-Input-Validierung mit Sanitizing aktiviert, deterministisches Prompt-Budget/Context-Trimming für Planner/Builder/Validator eingeführt, Confirm-Modal auf kompakte Inhaltsdeltas umgestellt und Validator-Herkunft/Fallback im UX als advisory transparent gemacht; fokussierte Jest-Regressionen ergänzt.
- Patch 486: CI-Lite-Header hydriert jetzt konservativ aus passender Persistenz (Repo/Branch/SHA/Freshness geprüft), bleibt bei fachlich fremden/veralteten Ständen neutral und trennt Workflow-/Run-Fehler im Modal sichtbar von Artifact-/Nachzug-Problemen; Reopen zeigt den letzten plausiblen Abschluss ohne Redispatch.
- Patch 485: CI-Lite-Tracking/Status-UI sauber entkoppelt — Header-Tap öffnet nur noch das Modal, neue CI-Lite-/Autofix-Runs starten ausschließlich über explizite Modal-CTAs, laufendes Polling/Log-Following bleibt bei geschlossenem Modal aktiv und Reopen zeigt den bestehenden Zustand ohne Redispatch.
- Patch 476: UX-/Flow-Consistency zwischen Connections/Repo/Build konservativ nachgeschärft — Build-Gates zeigen konsistent auf den GitHub-Repos-Screen, Secret-Sync-Kommunikation spiegelt den real app-verwalteten Umfang inkl. manueller Production-Grenze (Service-Role-Key) ehrlich wider, und die EAS-Rollen sind sprachlich klar getrennt (Connections: Token/ID, Repo: EAS-Link/Workflow im Ziel-Repo).
- Patch 475: Persistenz-/ProjectContext-Restpunkte konservativ gehärtet — `saveProjectToStorage` prüft Payloadgröße jetzt vor dem Schreiben mit klarem Soft-/Hard-Limit-Guard (kein stilles Oversize-Fail), und `ProjectContext.messages` nutzt eine stabile memoized Referenz aus `projectData.chatHistory`, um unnötige Re-Renders durch Array-Neureferenzen zu reduzieren.
- Patch 474: Timeout-Hauptrestpunkt regressionsfest abgesichert — neue Invariants stellen sicher, dass Planner/Builder/Validator/Explain im Chat-AI-Flow weiterhin ausschließlich über `runOrchestratorWithHardTimeout(...)` laufen und keine direkte `runOrchestrator(...)`-Nutzung in `processAIRequest(...)` zurückkehrt.
- Patch 473: Chat-AI-Flow-Härtung vervollständigt — alle Planner/Builder/Validator/Explain-`runOrchestrator(...)`-Aufrufe laufen jetzt über einen echten harten Stage-Timeout (45s) mit aktivem Abort des hängenden Calls und sauberer Timeout-vs-Abort-Trennung; gezielte Timeout/Abort-Regressionen ergänzt.
- Patch 472: AI-/Request-Robustheit-Follow-up — Orchestrator unterscheidet Timeout jetzt explizit von externem Abort (deterministische Timeout-Fehlermeldung bei hartem 45s-Request-Limit), ohne Änderung am bestehenden Retry-/Backoff-/Provider-Design; Regressionen decken Timeout-vs-Abort-Trennung ab.
- Patch 471: AI-/Request-Robustheitsreste minimal nachgezogen — harter Request-Timeout im Orchestrator pro Provider-Call (45s, abort-konsistent), kleiner Rotation-Backoff (350ms) vor Key-Retry bei 429/Rate-Limit und konservativer Builder-Retry-Backoff (700ms) in `useChatAIFlow`; gezielte Orchestrator-Regressionstests für Backoff/Timeout ergänzt.
- Patch 470: Follow-up auf Patch 469 — `k1w1-handler` gibt auch im frühen `parseJsonBody(...)`-Fehlerpfad keine rohen Parse-/Validierungsdetails mehr aus, sondern konsistente generische Client-Fehler (`Invalid request payload.`/`Request too large.`); kleiner Catch-Typing-Rest auf `unknown` + Narrowing ergänzt, Invariants entsprechend erweitert.
- Patch 469: verbleibende Edge-/Preview-Security-Exposure-Restpunkte konservativ geschlossen — `preview_page` entfernt raw Fehler-/Stack-Interpolation aus HTML-Pfaden (sicherer Overlay-Render via `textContent`), `k1w1-handler` leaked keine rohen internen `err.message`-Details mehr an Clients und antwortet mit generischen sicheren Fehlertexten; gezielte Invariant-Tests ergänzt.
- Patch 468: GitHubReposScreen-Architekturblock konservativ beruhigt — Sync-Status-Vergleich läuft zentral über Tree-SHA-Abgleich (`compareLocalFilesWithRepo`) statt teurem per-file Contents-Loop, und Multi-File-Push nutzt jetzt den Git-Data-Pfad (Tree/Commit/Ref) als konsolidierten Commit statt N Datei-Commits; bestehende Repo-/Branch-Stale-Guards bleiben intakt.
- Patch 467: Allgemeiner flow-naher Maintenance-/Typing-Block konservativ nachgezogen — `useChatAIFlow` entfernt einen verbliebenen Validator-`any`-Cast, `useGitHubActionsLogs` bereinigt toten Secret-Import und nutzt `unknown` im Error-Pfad, `actionsLogsTypes.describeEdgeFailure` parst Edge-Fehlerpayload ohne `any`, und `ensureChatHistoryHasIds` läuft defensiv über `unknown[]` + lokalen Type-Guard.
- Patch 466: CodeScreen/File-Editor/File-Actions-Restpunkte konservativ geschlossen — WebCodeEditor nutzt jetzt die bestehende Shared-WebView-Crash-Recovery-Verdrahtung (inkl. Process-Gone-Handler), Folder-Delete läuft gebatcht über `deleteFiles(...)` statt sequentieller Einzel-Deletes, der Delete-Handler hat einen expliziten fehlendes-Target-Guard, flow-nahe tote Imports wurden entfernt und gezielte Regressionstests für Recovery-Wiring + Delete/Batching ergänzt.
- Patch 465: SettingsScreen/AIContext-Restpunkte final konservativ geschlossen — leere Retention-Eingaben werden im Save-Pfad klar invalidiert (kein stilles `0` via Leerstring), Retention-Hydration im `ProjectContext` überschreibt keine frisch gesetzten Runtime-Werte mehr, kleiner `moveKeyToFront`-Fallback ohne Funktionsänderung bereinigt und Regressionstests für beide Kernfälle ergänzt.
- Patch 464: GitHubReposScreen-Defensiv-Nachfix für malformed/legacy `project.files` — lokale Dateien werden zentral über einen getypten Normalizer gefiltert (u. a. `null`/invalid Einträge), sodass Push/Pull/Sync-nahe Iterationen nicht crashen; gezielte Regression für Legacy-Einträge ergänzt.
- Patch 463: SettingsScreen/AIContext-Restpunkte konservativ geschlossen — Quality-Mode setzt nun die effektiven Chat-/Agent-Modelle mit (kein kosmetischer Toggle), Retention-Limit ist in Privacy sinnvoll editierbar/persistierbar **und wird nach Save sofort in der aktiven Chat-Runtime wirksam**, Gemini-Key-Prefix-Validation (`AIza`) ergänzt, unnötige `any`-Hotspots im Settings-Hook/Provider-Status entfernt und gezielte Tests inkl. Retention-Runtime-Absicherung ergänzt.
- Patch 462: GitHubReposScreen-Restpunkte konservativ geschlossen — Root-`projectFiles` jetzt sauber als `ProjectFile[]` (kein `any[]`-Root-Cast), `refreshSyncStatus` ignoriert stale Async-Rückläufer via Run-Guard, `handleCreateRepo` übernimmt GitHub-`default_branch` direkt, und angrenzende Pull-/Push-Casts wurden minimal reduziert; keine neue GitHub-Architektur.
- Patch 461: Chat-Flow-Nachfix aus PR #272/#273 zusammengeführt — Meta-/lokale Kommandos bleiben strikt auf unverändertem Raw-Input, der Attachment-Hinweis bleibt auf den normalen AI-Pfad begrenzt, und Attachment-only-Details gehen auch im Pending-Plan-Handoff nicht verloren (`aiContent || userContent`).
- Patch 460: Rest-Regression nach PR #273 geschlossen — `handleSendWithMeta(...)` akzeptiert Attachment-only-Sendefälle wieder korrekt (Abort nur noch wenn Raw+AI leer), Meta-/lokale Kommandos bleiben strikt auf unverändertem Raw-Input, und der normale AI-Pfad wird bei Attachment-only deterministisch ausgelöst.
- Patch 459: Rest-Regression aus PR #272 geschlossen — Attachment-Hinweis wird nicht mehr vor Meta-Command-Routing in den User-Input injiziert; lokale/full-line Kommandos (`cat ...`, `zeige datei ...`) laufen wieder auf unverändertem Raw-Input, während der Hinweis nur im normalen AI-Request-Payload landet; gezielte Invariant-Regression ergänzt.
- Patch 458: ChatScreen/Chat-Flow konservativ gehärtet — Attachment-Kommunikation ist jetzt explizit ehrlich (Dateiname/Metadaten statt impliziter Inhaltsanalyse), Chat-History wird bereits beim Append per Retention begrenzt, Focus-Cleanup räumt Pending-/Modal-/Streaming-State auf, und chatAIFlow-Typing-/Import-Artefakte wurden bereinigt.
- Patch 457: Offener Connections-Busy-Guard-UX-Bug geschlossen — dedizierter `BusyGuardActiveError` trennt Busy-Kollisionen von echten Save/Test-Fehlern, sodass der Hinweis „Ein anderer Save/Test-Lauf ist noch aktiv.“ nur noch bei realer Konkurrenz erscheint; `useChatAIFlow`-Pending-Plan-Guard wurde gezielt geprüft und per Invariant gegen versehentlichen Logikdrift abgesichert.
- Patch 456: Chat-Drift-Digest gegen RN-Runtime-Regression abgesichert — `lib/chatFlowStateGuards.ts` dokumentiert nun explizit Node-`crypto`-Importverbot im App-Pfad (`useChatAIFlow`), Hashing bleibt runtime-safe ohne Metro-Polyfill-Abhängigkeit.
- Patch 455: ConnectionsScreen konservativ gehärtet — Supabase-ANON-Key von AsyncStorage auf SecureStore mit Legacy-Migration umgestellt, Busy-/Hydration-Guards gegen parallele Save/Test-Runs ergänzt, `testExpo`-Persistenz-Side-Effect entfernt, Connection-Light-Writes teilweise gebündelt und EAS-Link-Lampe nicht mehr optimistisch auf grün gesetzt; inklusive gezielter Flow-/Storage-Regressionstests.
- Patch 454: flakigen Timeout-Restpunkt in `__tests__/oneClickDeploy.test.tsx` konservativ stabilisiert — deterministischer Press-Start via `act` + Microtask-Flush, AsyncStorage-Default-Resolves pro Test und striktes Cleanup (`cleanup` + Timer-Clear) gegen runInBand-Rennen; kein Produktcode-Umbau.
- Patch 453: KI-/Chat-Nachaudit (misstrauisch): echte Restlücke im Builder-NonJSON-Pfad geschlossen (`output_text` → nutzerlesbare Fehler-Preview), Regressionstests für `normalizer` gezielt verschärft.
- Patch 452: KI-/Chat-/Prompting-Block konservativ gehärtet; projektrelevante Snapshot-Priorisierung im Prompt-Context, verständlicher Builder-NonJSON-Fehlerpfad mit KI-Text-Preview, SHA-256 State-Drift-Digest (statt Länge), transparentere Ownership-/Validator-/Explain-Hinweise im Chat-Feedback; keine neue KI-Architektur, `k1w1-handler` nur eingeordnet.
- Patch 451: Preview-Restpunkte konservativ geschlossen — `filesFingerprint` erkennt nun auch Same-Length-Content-Änderungen (content-hash-basiert), Crash-Recovery (`onContentProcessDidTerminate`/`onRenderProcessGone`) ist auch im normalen PreviewScreen verdrahtet, abgelaufene Supabase-URLs werden im PreviewScreen nicht mehr blind geladen, und flow-nahe Helper-/Typing-/Dependency-Duplikate im Preview-Block wurden ohne Broad-Refactor konsolidiert.
- Patch 450: CustomHeader-/CI-Lite-Restfix konservativ geschlossen — `useGitHubActionsLogs` resetet stale Run/Log-State bei Input-Wechsel und schützt vor verspäteten Responses; CI-Lite-Persistenz schreibt nur für den aktiven CI-Lite-Run-Kontext (kein Autofix-Fehlpersist), Dispatch-Guard verhindert Doppeltap-Doppelstart, plus flow-nahe Typing-/Artifact-/Patch-Sync-Härtung ohne Broad-Refactor.
- Patch 449: EnhancedBuildScreen-Restpunkte konservativ nachgezogen — OneClickDeploy-Vorab-Push entfernt (SHA-sichere Reihenfolge über `startBuildJob`), redundanter Doppel-Push eliminiert, `canStartBuildUi` gegen Ref-Drift stabilisiert und build-/logs-nahe `WorkflowRun`-Typing inkl. `event`-Feld vereinheitlicht.
- Patch 448: DiagnosticScreen-Restpunkte konservativ geschlossen — progressive Preflight-Severity wieder sichtbar (`stage` korrekt), KI-Fix-Hinweis für `pass`-Items entfernt und flow-nahe Typing-/Hook-Cleanups in Runner/Screen-Hook ergänzt; gezielte Regressionen für Stage-Text und Fix-Hinweis hinzugefügt.
- Patch 447: kleiner Edge-/Deno-Typing-Follow-up in `_shared` — `globalThis as any` in Auth/CORS durch getypte Runtime-Globals ersetzt, `validation`-Any-Hotspots für Trigger/Workflow-Dispatch auf enge Union-/Record-Typen umgestellt und `parseJsonBody` gegen nicht-Objekt-JSON regressionsgesichert.
- Patch 446: selektiver Type-Safety-Restpunkt im Build-Start-Flow geschlossen — `buildStartService` ersetzt riskante `as any`-Zugriffe auf Edge-Invoke-Payload durch lokales Narrowing, entfernt unnötigen Push-Cast und ergänzt gezielte Regressionen für `job.id`-/Error-Payload-Shape.
- Patch 445: `save_preview`-Konsistenz zusätzlich gegen Auth-/Rate-Limit-Fehlerpfade abgesichert und kleiner Deno/Node-Typecheck-Restpunkt in `_shared/auth` per runtime-kompatiblem Env-Lookup behoben; Typecheck wieder grün.
- Patch 444: `save_preview` CORS-/Response-Konsistenz konservativ gehärtet — lokaler Erfolgs-/Fehlerpfad nutzt jetzt denselben `_shared/cors`-Header-Stack wie Auth/Rate-Limit-Guards; gezielte Invariants sichern Header-Gleichlauf inkl. Security-Headern.
- Patch 443: k1w1-handler Provider-Randfälle gehärtet — Anthropic sendet bei reinen `system`-Prompts kein leeres `messages`-Array mehr (konservativer Fallback-Turn), Gemini behandelt `system` explizit via `systemInstruction` + nicht-leerer `contents`-Fallback; doppeltes Nullish-Coalescing bereinigt, Invariants ergänzt.
- Patch 442: Build-Status/Phasenanzeige im EnhancedBuildScreen gezielt beruhigt — aktiver Lauf vs. letzter bekannter Stand klarer getrennt, aktive Phase explizit markiert, Run/Artifact/Download-Aktionen als aktueller vs. letzter Build-Kontext verständlicher benannt (ohne Backend-/Flow-Umbau).
- Patch 441: gezieltes UX-Feintuning nur für BuildScreen/DiagnosisScreen/Preview — verständlichere CTA-/Aktionssprache, ruhigere und konsistentere Statusbegriffe (inkl. Live/Fallback/Preview-Fehler), ohne Architekturumbau
- Patch 440: konservatives UX-/Flow-Feintuning in Kernpfaden (Build/Diagnosis/Preview/Connections/Credentials/Chat-Menü) — missverständliche/zu technische Status- und Hinweistexte geschärft, gespeicherter vs. letzter bekannter Zustand klarer getrennt, gezielte Preview-Status-Regression ergänzt
- Patch 439: zusätzliche Invariant-Härtung für `insert_diagnostic_upload`-Historie; UUID-Drift bleibt auf zwei bekannte Altmigrationen begrenzt und Legacy-Spalten-Drift (`repo/branch/mode/platform/report/meta`) wird im finalen Vertrag regressionssicher geblockt
- Patch 438: Supabase-Edge Import-Hygiene — produktive Workflow-Edges von fragilem App-Pfadimport (`../../../shared/constants/github.ts`) entkoppelt; `GITHUB_API_BASE` edge-nah in `_shared/github.ts` verankert, Invariant-Guard ergänzt.
- Patch 437: Doppelte Preview-Migration (`20251226140000`/`20251226160000`) als byte-identische Redundanz eingeordnet; spätere Datei konservativ auf explizites Legacy-No-op umgestellt (keine riskante History-Löschung).
- Patch 436: `insert_diagnostic_upload`-Vertrag migrationsseitig finalisiert (`jsonb -> bigint`), historischen UUID-/Spalten-Drift explizit dokumentiert und per Invariant-Tests gegen Regression abgesichert
- Patch 435: Supabase E2E contract close-out — audited Preview/Workflow/Signing/AI edge contracts end-to-end, documented explicit remaining operator dependencies, and fixed `github-run-artifact-json` ZIP path normalization for backslash-separated entries.
- Patch 434: Abschluss des großen Supabase-E2E-Contract-Audits; zentrale Edge-Function-SoT um produktive Signing/Preview/AI-Endpunkte ergänzt und Credentials-Wizard auf Constants statt Hardcodes umgestellt (inkl. Invariant-Tests).
- Patch 433: Supabase-Edge E2E-Contract-Audit; Credentials-Wizard Edge-200/Error-Mapping zentral gehärtet und per Jest-Regressionstest abgesichert.
- Patch 432: Ownership-/Permission-Audit; zentrale Guard-Matrix für Template/Baseline, Chat-Writeback und Diagnosis/Autofix eingeführt, inklusive konservativer Konfliktbehandlung und Regressionstests.
- Patch 431: System-Audit-Fix — Diagnostic-Status auf Repo/Branch scoped gemacht (mit Legacy-Fallback), damit Diagnosis→Build-Readiness keine Cross-Repo/Cross-Branch-Freigabe mehr erzeugt.
- Patch 430: Live-Reachability-Audit für 5 KI-Provider mit realen Smoke-Requests; in dieser Umgebung alle Checks als missing_secret klassifiziert; k1w1-Handler-HTTP-Fehler enthalten jetzt konsistent provider+model-Kontext, Groq-Fallback liefert resolved model zurück
- Patch 429: KI-Provider-/Modell-Audit; k1w1-Edge-Defaults auf aktuelle app-konsistente Modelle gehärtet, Groq-Prefix-Fallback ergänzt, Invariants erweitert
- Patch 428: Zweiter Korrektheits-Check; flakiges One-Click-Deploy-Timeout im Test stabilisiert (kein Produktionscode-Drift)
- Patch 427: Supabase-Function-Flow-Audit; Credentials-Wizard meldet Edge-HTTP-/Timeout-Fehler jetzt vertragstreu statt generisch
- Patch 426: Chat/KI-Confirm→Apply-Pfad gegen Zustandsdrift gehärtet (Rebase auf latest project state + transparente Drift-Meldung + Guard-Regressionstests)
- Patch 425: One-Click-Deploy um hartes Readiness-Gate (Diagnostik + CI-Lite + Repo/Branch-Frische) erweitert; mode-abhängige Pipeline-Filterlogik testbar extrahiert
- Patch 424: Supabase-Preview als offiziellen Browser-/QR-Preview-Weg im Produkt klarer markiert (URL-/Expiry-/Fallback-Transparenz, QR-Aktion ohne Architekturumbau)
- Patch 423: konservatives Machbarkeits-Audit für Expo Web / QR-Web-Preview (ohne Umbau); Empfehlung B mit minimalen Vorarbeiten
- Patch 422: job-gebundene History-Selektion für Repo/Branch/Profil im Build-Polling gehärtet (E2E-Traceability gegen Drift zwischen UI-Zustand und gestarteter Build-Wahrheit)
- Patch 421: Build-Traceability für gestartete Jobs gehärtet (Repo/Branch/Profil konsistent in CurrentBuild/History/UI sichtbar, inkl. CSV-/Status-Transparenz)
- Patch 420: managed-workflow guardrails gegen implizite ref/default-branch Fallbacks gehärtet; CI/CI-Lite-Ausnahme explizit als Vertrag abgesichert
- Patch 419: follow-up auf PR #206; `EDGE_FUNCTIONS_STATUS`-Indexlink in `docs/INDEX.md` wiederhergestellt und Patch-Benennung konsistent gehalten
- Patch 418 V1: trust/docs consolidation sweep; core architecture docs auf Post-417-Stand gezogen, offene Follow-ups zentral in TODO gesammelt, MD-/Notes-Cleanup als nächster Schritt verankert
- Patch 417 V18: V17-Verträge beibehalten, versehentlich committetes Repo-Root-Patch-Artefakt entfernt, Patch-Bundles/-Dateien gegen Re-Commit ignoriert
- Patch 416: retired lint/native-sync edge functions in `supabase/config.toml` deaktiviert, 410-legacy stubs explizit belassen, Guard-/Invariant-Coverage ergänzt
- Patch 415 V3: workflow-/CI-edge paths auf gemeinsamen admin-or-CI-bearer guard ausgerichtet; wizard/keystore setup admin-only belassen
- Patch 414 V13: explizite Ref-SoT-Invariants für Workflow-Templates gehärtet; dokumentierte branch-basierte CI-Lite-Ausnahme bewusst erhalten
- Patch 413: restliche stille repo/config + branch/main fallbacks entfernt; Repo/Branch-SoT-Regression-Coverage ergänzt
- Patch 412: `_diagnostic_upload_guard()` mit explizitem `search_path`; versehentliche `PUBLIC` execute-Rechte entzogen; Guard-/Invariant-Coverage ergänzt
- Patch 411: Supabase deploy workflow auf `workflow_dispatch` + required `ref` gehärtet; single-function/_shared guards + migrations policy + metadata synchronisiert
- Patch 410 / 410B: Edge admin auth vom service-role lookup getrennt, CI bearer guard ergänzt, service-role handling aus Client-Pfaden entfernt
- Patch 409 / 408: diagnostics upload ids als opaque strings im Client stabilisiert; build job id contract auf positive numerische IDs ausgerichtet
- Patch 407–403: Repo/Branch-SoT-Härtung, Workflow-Governance, Contract-/Drift-Guards und Doku-Sync konsolidiert
- Patch 401–389: Provider-/Helper-Invariants, CI-Lite-SHA-/Template-Drift-Härtung, Dispatch-/Trigger-Polish

- Patch 527: CI-Lite-Persistenz auf repo-/branch-scoped Snapshot-Vertrag umgestellt; `readPersistedCiLiteSelection(...)` bevorzugt jetzt `ci_lite_snapshot::<repo>::<branch>`, globale `CI_LITE_LAST_*`-Keys bleiben nur noch Legacy-Fallback/Migration, der Header schreibt denselben scoped Snapshot und Build-Readiness liest dieselbe gemeinsame Quelle.

- Patch 529: CI-Lite-Run-Lookup gegen Legacy-Workflow-Drift gehaertet; exakter `job_id`-Marker bleibt Prioritaet, `workflow_dispatch` bekommt nur noch sichere Fallback-Tiers inkl. Ambiguitaets-/Contract-Diagnose statt irrefuehrendem Generic-Timeout.

- Patch 538: kleiner Maintenance-Block fuer verbleibende Robustheitsreste — GitHubContext spiegelt `projectData.linkedRepo` / `linkedBranch` jetzt ohne redundante Mirror-Re-Set-Pfade, `useGitHubActionsLogs` hat einen echten AbortController-/Timeout-Vertrag fuer Runs und Logs inklusive Request-Invalidierung bei Input-Wechsel, und AIContext coalesced die redacted AsyncStorage-Persistenz per kleinem Debounce; fokussierte Regressionen decken alle drei Pfade ab.

## Historical (selected)
- Patch 388: workflow drift validator + stronger patch artifact discipline
- Patch 387: managed workflow drift hardening + CI-lite template SHA metadata + cleanup
- Patch 386: SHA hardening phase 2 + workflow marker completion + patch artifact discipline
- Patch 385: Project-context SoT correction + CI Lite artifact/autofix hardening
- Patch 383: Workflow hardening for explicit ref handling + manual-path transparency
- Patch 382: Build screen CI Lite gating + stale transparency
- Patch 381: CI Lite SoT hardening + build gate metadata persistence
- Patch 380: CI Lite dispatch robustness (ref input + auto-bootstrap on 422)
- Patch 379: Guard against flattened workflow YAML/templates + invariant test to catch newline loss
- Patch 374: CI-Lite workflows produce deterministic result JSON artifact + checkout input ref
- Patch 373: Metro blockList CI-lite env overlay + remove private metro-config import
- Patch 372: Expo/Metro ignores CI-Lite env overlay files (fix dev bundler parse error)
- Patch 371: CI-Lite workflow templates pin ESLint@8 fallback for legacy configs
- Patch 370: Patchlog restore patch_337 dual references (invariant I10)
- Patch 369: CI-Lite in-app status uses GitHub truth (fix green/rot mismatch)
- Patch 368: CI-Lite smoke diagnostics + gitignore

## Legacy patch-337 references
- docs/patches/patch_337.md
- docs/patches/PATCH_337_NOTES.md

- Patch 550: PR-408-Follow-up auf aktuellem Branch-Zustand geschlossen — gemeinsame Provider-Default-Quelle fuer App/Edge eingefuehrt, Anthropic-Quality-Default auf `claude-sonnet-4-20250514` aktualisiert und Runtime-Fallback-Hinweise per explizitem `runtimeNote`-Meta-Filter aus spaeterer LLM-Historie ausgeschlossen.

- 2026-03-28: Patch 557: Notification-Push-Flow konservativ repariert und nachgezogen — `lib/notificationService.ts` ermittelt `projectId` jetzt ueber echte Expo/EAS-Pfade (`easConfig` -> `expoConfig.extra.eas` -> `manifest2.expoClient.extra.eas`) statt owner/placeholder, loggt keine Token-Werte mehr und haelt im gesamten `initialize()`-Verlauf `permissionGranted`/`expoPushToken` konsistent; explizit getestete Fehlerpfade sind denied, outer-error, token-fetch-error und Android-FCM-skip. `hooks/useNotifications.ts` synchronisiert weiter `isInitialized` + `hasPermissions` + `pushToken`, und `lib/__tests__/notificationService.test.ts` deckt die projectId-Prioritaet jetzt explizit regressionsfest ab.
