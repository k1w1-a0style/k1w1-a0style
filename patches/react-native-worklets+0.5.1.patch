diff --git a/node_modules/react-native-worklets/android/CMakeLists.txt b/node_modules/react-native-worklets/android/CMakeLists.txt
index 1d949d6..c160b41 100644
--- a/node_modules/react-native-worklets/android/CMakeLists.txt
+++ b/node_modules/react-native-worklets/android/CMakeLists.txt
@@ -31,7 +31,7 @@ if(NOT ${CMAKE_BUILD_TYPE} MATCHES "Debug")
   string(APPEND CMAKE_CXX_FLAGS " -DNDEBUG")
 endif()
 
-if(${JS_RUNTIME} STREQUAL "hermes")
+if(TRUE) # force Hermes runtime on Expo SDK 54
   string(APPEND CMAKE_CXX_FLAGS " -DJS_RUNTIME_HERMES=1")
 elseif(${JS_RUNTIME} STREQUAL "jsc")
   string(APPEND CMAKE_CXX_FLAGS " -DJS_RUNTIME_JSC=1")
@@ -52,7 +52,7 @@ file(GLOB_RECURSE WORKLETS_ANDROID_CPP_SOURCES CONFIGURE_DEPENDS
 find_package(fbjni REQUIRED CONFIG)
 find_package(ReactAndroid REQUIRED CONFIG)
 
-if(${JS_RUNTIME} STREQUAL "hermes")
+if(TRUE) # force Hermes runtime on Expo SDK 54
   find_package(hermes-engine REQUIRED CONFIG)
 endif()
 
@@ -104,7 +104,7 @@ else()
     ReactAndroid::glog ReactAndroid::reactnativejni)
 endif()
 
-if(${JS_RUNTIME} STREQUAL "hermes")
+if(TRUE) # force Hermes runtime on Expo SDK 54
   target_link_libraries(worklets hermes-engine::libhermes)
 
   if(${HERMES_ENABLE_DEBUGGER})
@@ -117,7 +117,12 @@ if(${JS_RUNTIME} STREQUAL "hermes")
   endif()
 elseif(${JS_RUNTIME} STREQUAL "jsc")
   if(ReactAndroid_VERSION_MINOR GREATER_EQUAL 76)
-    target_link_libraries(worklets ReactAndroid::jsctooling)
+    if(TARGET ReactAndroid::jsctooling)
+      message(STATUS "Linking worklets with ReactAndroid::jsctooling")
+      target_link_libraries(worklets ReactAndroid::jsctooling)
+    else()
+      message(WARNING "ReactAndroid::jsctooling target not found, continuing without it")
+    endif()
   else()
     target_link_libraries(worklets ReactAndroid::jscexecutor)
   endif()
