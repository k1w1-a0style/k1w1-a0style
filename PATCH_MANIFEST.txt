PATCH19 - Diagnostics reliability: persist fixes immediately for re-scan

Changed files:
- screens/DiagnosticScreen.tsx

Why:
- After applying a fix, some users can immediately re-run diagnostics and still see the old warnings.
- Root cause: run() reads projectRef.current.files; React state propagation can lag briefly after updateProjectFiles().
- Fix: update projectRef.current.files immediately with nextFiles before awaiting context persistence.

