PATCH13 - Allow arbitrary safe branches for in-app builds

Changed files:
- .github/workflows/k1w1-triggered-build.yml

Why:
- The workflow previously only allowed a small fixed set of branches (main/work/develop/etc).
- Your app lets you select any branch in-app; blocking it in CI breaks the end-to-end build flow.
- New regex allows typical Git refs safely (letters/digits/._/-), blocks whitespace/newlines and odd characters.

