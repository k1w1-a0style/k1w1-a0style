PATCH3 Manifest – APK Download URL (Supabase Storage) + error_message Fix

Enthaltene Dateien:
1) .github/workflows/k1w1-triggered-build.yml
   - Upload der gebauten APK nach Supabase Storage (Bucket: apk-builds oder secrets.SUPABASE_APK_BUCKET)
   - Erzeugt signed download_url (7 Tage) und schreibt sie in build_jobs.download_url
   - Failure schreibt jetzt build_jobs.error_message statt error

2) supabase/functions/check-eas-build/index.ts
   - Liefert download_url + build_url konsistent (top-level + job)
   - Nutzt error_message (fallback error), entfernt double CORS call

3) screens/EnhancedBuildScreen.tsx
   - Zeigt Buttons: “📦 Artifacts öffnen” und “⬇️ APK Download” / “🔗 Build Ergebnis”
   - Nutzt currentBuild.urls.* (aus check-eas-build / Polling)

Voraussetzungen:
- Supabase Storage Bucket “apk-builds” existiert (private empfohlen).
- Optional GitHub Secret SUPABASE_APK_BUCKET (ansonsten default apk-builds).
