PATCH7 - Fix diagnostics TypeScript error + UUID regex

Changed files:
- lib/diagnostics/buildPipelineDiagnostics.ts

Why:
- Fixes TS2554: getRepoFileText expects a single params object, not 4 positional args.
- Fixes corrupted regex (literal '...') so projectId UUID detection works reliably.
