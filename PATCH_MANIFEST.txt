PATCH20 - Diagnostics: ensure applied fixes persist on immediate re-scan

Changed files:
- screens/DiagnosticScreen.tsx

Why:
- You previously observed: Fix shows 'applied' but warnings reappear on next scan.
- Root cause: projectRef.current.files can lag behind updateProjectFiles() (async context/state).
- This patch updates projectRef.current.files immediately after applying patches, so a re-run reads the new files.

