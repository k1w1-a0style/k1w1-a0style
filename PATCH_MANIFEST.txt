Mobile-APK-Builder PATCH28a

Changed files:
- contexts/ProjectContext.tsx
- screens/GitHubReposScreen.tsx
- .github/workflows/k1w1-triggered-build.yml

Purpose:
- Prevent pushing partial native folders (android/ios) into linked repos.
- Add CI safeguard to strip incomplete native folders before EAS build.
- Ensure devDependencies are installed in CI to avoid expo-dev-client missing checks.
