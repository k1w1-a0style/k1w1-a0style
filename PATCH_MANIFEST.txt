PATCH16 - CI hardening: auto-initialize EAS project in workflow

Changed files:
- .github/workflows/k1w1-triggered-build.yml

Why:
- You require zero manual terminal steps. EAS can fail in CI with:
  'EAS project not configured. Must configure EAS project by running eas init ...'
- This adds 'eas project:init --force --non-interactive' before the build step so fresh repos/branches still build.

