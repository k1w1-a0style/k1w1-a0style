PATCH18 - Diagnostics polish: avoid duplicate check id

Changed files:
- lib/diagnostics/buildPipelineDiagnostics.ts

Why:
- The expo-dev-client dependency check used the same id in both try and catch blocks.
- This can cause duplicate rows / confusing Autofix selection in the UI.
- Catch case now uses id 'repo.dep.expoDevClient.read'.

