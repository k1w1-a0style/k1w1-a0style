PATCH11 - Fix diagnostics typecheck: support ProjectFile[] in app config lookup

Changed files:
- lib/diagnostics/preflightChecks.ts

Why:
- In some preflight checks, `files` is a ProjectFile[] array, but getText() expects a Map.
- Add getTextAny() to handle both Map and array, and use it for app config lookup.

