PATCH15 - Final hardening: allow arbitrary safe branches + APK-only template for Full flow

Changed files:
1) .github/workflows/k1w1-triggered-build.yml
   - Relax ALLOWED_REF_REGEX to a safe generic pattern so in-app branch selection can't be blocked by an arbitrary whitelist.
2) lib/diagnostics/preflightChecks.ts
   - Fix generated eas.json template to keep production (Full) APK-only (no AAB).
   - Add cli.appVersionSource: 'remote' in template to avoid future EAS warnings.

Why:
- Requirements: repo/branch selection happens in-app, so CI must not reject valid branch names.
- App is APK-only, including the 'Full' flow (production profile). Diagnostics template must not generate AAB.

