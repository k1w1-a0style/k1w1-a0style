PATCH: Mobile-APK-Builder-PATCH30
Changes:
- .github/workflows/k1w1-triggered-build.yml: add normalization step before EAS init/build
- .github/workflows/eas-build.yml: add normalization step before EAS init/build

Why:
- Fixes EAS failures when a partial android/ directory exists but android/app/build.gradle is missing.
- Ensures expo-dev-client is installed when declared (prevents EAS preflight error).
- Enforces APK-only managed defaults during CI runs.
