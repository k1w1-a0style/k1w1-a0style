PATCH24 - Diagnostics + GitHub writeback stability

Changed files:
- lib/diagnostics/buildPipelineDiagnostics.ts
- contexts/githubService.ts

Why:
1) TypeScript fix: DiagnosticScreen expects pipeline checks to optionally include 'fix'.
   buildPipelineDiagnostics.DiagnosticCheck type now includes optional fix {label, patch}.
2) Repo writeback fix: createOrUpdateFile could fail with 409 SHA mismatch during concurrent repo sync.
   Added one retry with refetch SHA to make diagnostics autofix writeback reliable.

