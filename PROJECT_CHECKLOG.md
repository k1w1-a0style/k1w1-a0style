- 2026-04-09: Patch 768: Residual-Hotspot-Finalscan + WeakFallback-Hygiene abgeschlossen — die verbleibenden A1/A2/A3-Residual-Hotspots wurden erneut im engen Scope geprueft und als fachlich schlanke Orchestratoren bestaetigt (kein erzwungener Grossumbau ohne klaren Mehrwert). Direkt im produktnahen Scope wurde ein stiller AsyncStorage-Fallback in `screens/GitHubReposScreen/hooks/useGitHubReposScreenBootstrap.ts` entfernt (`.catch(() => "")` -> explizites `null`-Sentinel + `logger.warn(...)`), sodass Storage-Fehler sichtbar bleiben bei unveraenderter EAS-Project-ID-Fallback-Semantik.
- 2026-04-09: Patch 767: Release-Readiness-Tooling robust + Live-Variable-SoT finalisiert — `scripts/check_release_readiness.sh` ruft strict/edge TypeScript jetzt robust repo-lokal ueber `./node_modules/.bin/tsc` mit `npx tsc`-Fallback auf (kein Fragilitaetsbruch durch fehlendes globales `tsc`). Runbooks/Testing-SoT dokumentieren den sicheren Variablenbezug fuer Live-Checks klar (`EDGE_BASE_URL`, `EDGE_OPERATOR_JWT`; bevorzugt masked runner secrets, lokaler URL-Fallback ueber Projekt-Ref `xfgnzpcljsuqqdjlxgul`) ohne Secret-Leaks. Der fruehere `preview_page`-Legacy-Drift (`Missing ?secret=...`) ist als realer Deploy-Fall im SoT jetzt als geschlossen dokumentiert (Redeploy `preview_page` + `save_preview`, danach Live-Contract erneut OK); lokale Verifikation in dieser Shell bleibt weiterhin ehrlich env-gebunden (`OK_WITH_SKIPS`, kein Fake-`OK_FULL`).
- 2026-04-09: Patch 766: Scope-/Live-Contract-Truthfulness nachgezogen fuer den breiten PR-Stand — `scripts/check_edge_live_contracts.sh` prueft jetzt zusaetzlich `save_preview` live auf den modernen Fragment-/Header-Transport (`preview_page?transport=fragment#secret=...`, kein Query-`?secret=`) und klassifiziert den bekannten Legacy-Drift bei `preview_page` explizit mit klarer Redeploy-Anweisung (`Missing ?secret=...` => deploy drift). Abschluss-Checks lokal voll gruen; `check_release_readiness` bleibt in dieser Shell ehrlich `OK_WITH_SKIPS`, weil `EDGE_BASE_URL`/`EDGE_OPERATOR_JWT` hier nicht gesetzt waren und daher kein Fake-Live-Signoff erfolgen konnte.
- 2026-04-09: Patch 765: verbleibende Hook-Resthotspots finalisiert — `useGitHubRepos.ts` delegiert Pull-/Blob-/GraphQL-Fallback-Orchestrierung jetzt an `useGitHubReposPull.ts`, `useCredentialsWizardScreen.ts` delegiert UI-nahe Action-/Clipboard-/Status-Meta-Logik an `useCredentialsWizardActions.ts`, `useChatScreen.ts` nutzt fuer Thinking-/Typing-Feedback den neuen Hook `useChatScreenAnimations.ts`, und `useEnhancedBuildScreen.ts` lagert Build-Readiness-/Filter-/Checklist-/Logs-Derivation in `useEnhancedBuildDerivedState.ts` aus. Public Hook Shapes bleiben stabil; Vollsuite + Release-Checks lokal gruen (`OK_WITH_SKIPS` nur wegen fehlender Live-Env-Variablen).
- 2026-04-09: Patch 764: Hook-Hotspot-Decomposition-Wave im sicheren Scope abgeschlossen — `hooks/useGitHubRepos.ts` entmischt zwischen Hook-Fassade und dedizierten Repo-Request-Helpers (`useGitHubReposRequests.ts`), `useCredentialsWizardScreen.ts` kapselt UI-/Debug-/Error-State jetzt in `useCredentialsWizardUiState.ts`, `useChatScreen.ts` delegiert Attachment-Picker-IO an `chatScreenDocumentPicker.ts`, und `useEnhancedBuildScreen.ts` nutzt ausgelagerte Action-Orchestrierung (`enhancedBuildScreenActions.ts`). Oeffentliche APIs und fail-closed Semantik bleiben stabil; Vollsuite + Release-Checks lokal gruen (`OK_WITH_SKIPS` nur wegen fehlender Live-Env-Variablen).
- 2026-04-09: Patch 763: enger Abschluss fuer WorkflowContractFragility + RefactorSoTDrift-Follow-up — `scripts/check_workflow_edge_contracts.sh` wurde von fragilen Vollsatz-Pruefungen auf semantische Markerbuendel nachgeschaerft (`require_operator_claim_contract`), wodurch Auth-/RBAC-Contracts weiterhin strikt fail-closed bleiben, aber nicht an kosmetischer Copy-Drift haengen. Parallel wurden die fuehrenden SoT-Dateien (`README`, `TODO`, `Review`, `INDEX`, `TESTING_GUIDE`, `FRESH_CHECKOUT_GREEN_PATH`, `EDGE_FUNCTIONS_STATUS`) auf Patch 763 synchronisiert. Produktiver Runtime-Scope wurde erneut auf direkte `console.log`-Reste geprueft (kein offensichtlicher Treffer ausser `lib/logger.ts` als zentrale Fassade).
- 2026-04-08: Patch 762: enger Hygiene-/Drift-Follow-up ohne neuen Grossumbau — `check_workflow_edge_contracts.sh` prueft Auth-Contracts jetzt robust auf die modularen Dateien (`auth/jwt.ts`, `auth/scoped.ts`, `auth/runtime.ts`, `auth/admin.ts`) statt auf fragilen Monolith-Text in `auth.ts`; die temporary Marker-Kommentare in `auth.ts` sind entfernt. Zusaetzlich neuer Check `scripts/check_verify_jwt_visibility.sh` (inkl. Einbindung in `check_release_readiness.sh`) fuer klare Sichtbarkeit der kritischen `verify_jwt`-Flags und zugehoeriger Status-Doku. Produktive `console.log`-Reste in `k1w1-handler` und `preview_page` wurden auf `console.info` gehoben; SilentCatch-Re-Scan zeigt im produktiven Scope keine verbliebenen stillen Catch-Reste.
- 2026-04-08: Patch 761: Abschluss des verbleibenden Hotspot-Blocks ohne Verhaltensdrift — `lib/diagnostics/buildPipelineDiagnostics.ts` auf Orchestrator reduziert und in `buildPipelineDiagnostics.checks.ts`/`.constants.ts`/`.helpers.ts` zerlegt; `supabase/functions/_shared/auth.ts` in Facade + `auth/{runtime,jwt,admin,scoped,rateLimit}.ts` aufgeteilt; `screens/AppInfoScreen/hooks/useAppInfoScreen.ts` als duenne Fassade belassen und API-/Secure-Backup-Flows nach `useAppInfoApiConfigFlow.ts` und `useAppInfoSecureBackupFlow.ts` verschoben. Source-marker-Invariants wurden auf modulare Zielstruktur nachgezogen. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent`, plus fokussierte Hotspot-Tests (diagnostics/auth/appinfo/invariants) und DocsSync-Check.
- 2026-04-08: Patch 760: `LocalRemoteDiffSectionRefactor` und `RefactorSoTDrift` in einem Block abgeschlossen — der Diff-Hotspot `screens/GitHubReposScreen/components/LocalRemoteDiffSection.tsx` wurde vollstaendig in Container (`LocalRemoteDiffSection/index.tsx`), Async-/State-Modell (`useLocalRemoteDiffModel.ts`), pure Diff-Algorithmen (`diffAlgorithms.ts`), Fingerprint-Helper (`fingerprint.ts`), Listen-UI (`LocalRemoteDiffList.tsx`), Modal-UI (`DiffPreviewModal.tsx`) und lokale Typen (`types.ts`) zerlegt, ohne Diff-/Cache-/Stale-Request-/Preview-Semantik zu aendern; zusaetzlich sichern neue Regressionen (`__tests__/localRemoteDiffSection.diffAlgorithms.test.ts`) die Diff-Contracts. Parallel wurde SoT-Drift geschlossen: README/TODO/Review/INDEX/TESTING_GUIDE/FRESH_CHECKOUT sowie Patchlog/Checklog auf den echten Refactor-Stand synchronisiert. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, gezielte GitHubRepos-/Diff-Tests inkl. `localRemoteDiffSection.truthfulness`, `githubReposScreen.list`, `githubReposScreen.pullPushSemantics`, `patch462`, `patch483`, plus `bash scripts/check_patch_docs_sync.sh` und `npm run -s docs:lint`.
- 2026-04-07: Patch 759: Analyse-only Master-Plan fuer verbleibende Refactor-/Wartbarkeits-Hotspots erstellt (kein Runtime-Umbau). Neue Plan-SoT: `docs/reviews/hotspot_master_plan_2026-04-07.md` mit konkreten Split-Zielstrukturen, Orchestrator-Zielbild, Risikoanalyse, Testschutz und Etappenreihenfolge fuer `ConnectionsScreenRefactor`, `ProjectContextRefactor`, `LocalRemoteDiffSectionRefactor`, `SecretsSectionRefactor`, `CiLiteWorkflowFurtherSplit`, `DiagnosticScreenFurtherSplit` plus echte Zusatz-Hotspots `useChatAIFlow` und `managedWorkflowTemplates`. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s docs:lint`, `bash scripts/check_patch_docs_sync.sh`.
- **Hinweis zur Einordnung:** Dieser Checklog ist bewusst append-only Historie und **keine** alleinige aktuelle Release-/Freigabe-Wahrheit.
- Fuer den aktuellen Ist-Stand gelten die jeweils letzten gruenen Ausfuehrungen von `verify:release`, den fokussierten Runtime-/Contract-Tests sowie die aktiven SoT-Dokumente (`README.md`, `docs/TODO.md`, `docs/reviews/Review.md`).
- 2026-04-07: Patch 758: Offene Restpunkte aus PR-574 sauber abgeschlossen — `scripts/check_patch_docs_sync.sh` prueft den echten Kern-SoT-Scope jetzt hart auf Patch-Konsistenz (README/Checklog/Patchlog plus TODO/Review/INDEX/TESTING_GUIDE/FRESH_CHECKOUT/EDGE_FUNCTIONS_STATUS), damit offensichtlicher Docs-Drift nicht mehr still gruen durchgeht. Legacy-Keystore-Crypto wurde enger als read-only Compat markiert (`deriveLegacyCompatAesKeyBytesSha256`, `decryptWithAesCbcLegacyCompatOnly`; neue Writes bleiben v3/PBKDF2+GCM), verbleibende Silent-Catches in `WebCodeEditor` und `ConfirmChangesModal` loggen jetzt sichtbar fail-safe Warnungen, und der Preview-/Release-Tradeoff bleibt ehrlich dokumentiert (`unsafe-eval`/`esm.sh` bewusst steuerbar; `OK_WITH_SKIPS` weiterhin klar nicht `OK_FULL`). Validation: `bash scripts/check_patch_docs_sync.sh`, `npm run -s test:silent -- --runInBand __tests__/androidKeystoreCrypto.contract.test.ts __tests__/releaseReadiness.execution.contract.test.ts __tests__/previewEdgeErrorContract.test.ts`, `npm run -s lint:ci`, `npm run -s typecheck`.
- 2026-04-07: Patch 757: Preview-Tradeoff-Block konsistent eingegrenzt (ohne Funktionsumbau) — `preview_page/helpers.ts` dokumentiert den verbleibenden CSP-Sandbox-Tradeoff jetzt explizit im Code (`unsafe-eval` + `https://esm.sh` sind weiterhin bewusst moeglich, kein voll risikofreier Modus), und fuehrt einen kleinen Low-Risk-Haertungshebel ein: `PREVIEW_STRICT_CSP=true` deaktiviert `unsafe-eval` zusaetzlich zu `TEST_STRICT_CSP`/`PREVIEW_ALLOW_UNSAFE_EVAL=false`; `PREVIEW_ALLOW_ESM_SH_CDN=false` kann den CDN-Import in `script-src` bewusst abschalten. Secret-Gate-SoT wurde konsistent nachgeschaerft (`supabase/config.toml` + `docs/EDGE_FUNCTIONS_STATUS.md`: `preview_page` ist absichtlich `verify_jwt=false` und kein normaler JWT-Endpunkt). Validation: `npm run -s test:silent -- --runInBand __tests__/previewEdgeErrorContract.test.ts __tests__/patch514.buildPreviewEnvSharedHelpers.invariants.test.ts`, `npm run -s typecheck`, `npm run -s lint:ci`.
- 2026-04-07: Patch 756: Device-ID-/UUID-Throw-Restpunkt robust geschlossen — `useDiagnosticUpload` hat jetzt einen echten non-throwing Device-ID-Fallback-Stack (`expo-crypto` -> `globalThis.crypto` -> `uuidv4` -> finaler timestamp+counter-Rettungsanker) und einen analogen non-throwing Fallback fuer `clientRequestId`, damit Upload-/Copy-Flow auch bei kaputtem RNG/UUID nicht mehr am ID-Pfad scheitern. Der Release-Partial-Hinweis wurde in den Operator-Runbooks nachgeschaerft (`OK_FULL` vs `OK_WITH_SKIPS`), damit `OK_WITH_SKIPS` nicht als Voll-Sign-off missverstanden wird. Validation: `npm run -s test:silent -- --runInBand __tests__/useDiagnosticUpload.silentCatchFollowup.test.tsx lib/__tests__/sandpackBuilder.test.ts __tests__/releaseReadiness.execution.contract.test.ts __tests__/previewEdgeErrorContract.test.ts`, `npm run -s typecheck`, `npm run -s lint:ci`.
- 2026-04-07: Patch 755: PR-574-Restpunkte final nachgezogen — `useDiagnosticUpload` hat keinen stillen Persisted-Cooldown-Load-Catch mehr (`console.warn` statt swallow), und neuer Runtime-Test `__tests__/useDiagnosticUpload.silentCatchFollowup.test.tsx` prueft die Warnpfade fuer Persisted-Cooldown sowie Device-ID-/RNG-/Persist-Fallbacks ausfuehrbar. Fuer Disabled-Legacy-Edges wurde zusaetzlich ein execution-naher Contract-Test `__tests__/legacyDisabledEdges.execution.contract.test.ts` eingefuehrt, der `scripts/check_legacy_disabled_edges.sh` in isolierten Fixtures auf pass/fail Semantik prueft (inkl. Drift-Fall bei fehlendem `status: 410`). Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/useDiagnosticUpload.silentCatchFollowup.test.tsx __tests__/legacyDisabledEdges.execution.contract.test.ts __tests__/previewSecretCandidates.runtime.test.ts __tests__/previewEdgeErrorContract.test.ts __tests__/releaseReadiness.execution.contract.test.ts __tests__/silentCatchCriticalPaths.invariants.test.ts __tests__/projectChecklog.truthfulness.invariants.test.ts`, `npm run -s docs:lint`, `npm run -s docs:check:contracts`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_legacy_disabled_edges.sh`.
- 2026-04-07: Patch 754: SilentCatch-/Runtime-Follow-up geschlossen — weiterer stummer Catch im PreviewFullscreen-URL-Parse-Guard wurde auf sichtbares Warn-Logging umgestellt; Diagnostic-Upload Device-ID-/RNG-/Persist-Fallbacks loggen nun ebenfalls explizit statt still zu schlucken. Fuer die Preview-Boundary wurde `isValidPreviewSecretFormat(...)` als shared Runtime-Guard in `preview_page/helpers.ts` gezogen und in `preview_page/index.ts` produktiv genutzt; `__tests__/previewSecretCandidates.runtime.test.ts` prueft missing/invalid/valid Secret-Formate jetzt ausfuehrbar. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/previewEdgeErrorContract.test.ts __tests__/previewSecretCandidates.runtime.test.ts __tests__/releaseReadiness.execution.contract.test.ts __tests__/silentCatchCriticalPaths.invariants.test.ts __tests__/projectChecklog.truthfulness.invariants.test.ts`, `npm run -s docs:lint`, `npm run -s docs:check:contracts`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_legacy_disabled_edges.sh`.
- 2026-04-07: Patch 753: offener Restblock `HistoricalChecklogDrift` + `SilentCatchDebt` + `MaxRuntimeHardening` nachgezogen — `PROJECT_CHECKLOG.md` wurde explizit als historische, nicht alleinige Release-Wahrheit relativiert; neue runtime-nahe Preview-Vertragstests (`__tests__/previewSecretCandidates.runtime.test.ts`) sichern hash-first Lookup + legacy raw fallback + Delete-Kandidatenpfad ausfuehrbar ab; `__tests__/projectChecklog.truthfulness.invariants.test.ts` und `__tests__/silentCatchCriticalPaths.invariants.test.ts` halten Truthfulness-/Silent-Catch-Guards regressionsfest. Kritische stumme Catches in Preview-Navigation, Repo-Meta-Link, Diagnostic-Upload-Cooldown und Build-Preconditions wurden auf sichtbare Warnpfade umgestellt (Fail-safe Verhalten bleibt). Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/previewEdgeErrorContract.test.ts __tests__/previewSecretCandidates.runtime.test.ts __tests__/releaseReadiness.execution.contract.test.ts __tests__/silentCatchCriticalPaths.invariants.test.ts __tests__/projectChecklog.truthfulness.invariants.test.ts`, `npm run -s docs:lint`, `npm run -s docs:check:contracts`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_legacy_disabled_edges.sh`.
- 2026-04-07: Patch 752: Folgefehler aus PR #574 geschlossen — nach Preview-Secret-Hashing wurden Expiry-Deletes noch mit raw Secret ausgefuehrt (`deletePreviewRecord(secret)` => `secret=eq.<raw>`), wodurch neue gehashte Rows beim Expiry-Cleanup nicht geloescht wurden. Fix: `preview_page` nutzt fuer Lookup **und** Delete denselben `buildPreviewSecretCandidates(secret)`-Pfad (hash-first + legacy raw fallback); damit bleiben neue gehashte Rows und alte raw Rows gleichermassen loeschbar. Zusaetzlich Regressionstests erweitert (`previewEdgeErrorContract`) fuer Candidate-Order und Expiry-Delete-Pfad. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/previewEdgeErrorContract.test.ts __tests__/releaseReadiness.execution.contract.test.ts`.
- 2026-04-07: Patch 751: Preview-/Auth-/Release-Haertung ohne Grossumbau — Preview-Secrets werden serverseitig nicht mehr als Klartext gespeichert (`save_preview` persistiert nur noch SHA-256-hashte Tokens mit Prefix), `preview_page` lookuped hash-first und bleibt fuer bereits bestehende Legacy-Klartextdatensaetze bewusst kompatibel (fallback lookup). Zusaetzlich wurde die Preview-Boundary auf GET/HEAD begrenzt und Fragment-Bootstrap-Fehlerpfade sichtbarer geloggt. Fuer sensible Preview-Routen ist durable Rate-Limiting jetzt fail-closed (`enforceDurable: true` -> `503 rate_limit_unavailable` statt stiller local-only Degradation), waehrend nicht-sensitive Routen beim bisherigen best-effort Verhalten bleiben. Local-Preview-Eval wurde weiter begrenzt (`allowUnsafeLocalEval` ignoriert in `NODE_ENV=production`). Release-Readiness-Absicherung wurde von reinen Wiring-Checks auf Ausfuehrungsvertrag erweitert (`__tests__/releaseReadiness.execution.contract.test.ts`: mandatory-check failure breaks run, `OK_WITH_SKIPS` vs `OK_FULL` wird real ausgefuehrt verifiziert). Kleine Silent-Catch-Debt im Chat-Privacy-Storage wurde durch warn-logging ersetzt. Historische Checklog-Drift wurde dokumentativ eingegrenzt: dieser Checklog bleibt append-only Historie und ist kein alleiniger Beweis fuer den aktuellen Release-Stand. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/previewEdgeErrorContract.test.ts __tests__/edgeRateLimitCoverage.invariants.test.ts __tests__/edgeRateLimitHighRiskRoutes.invariants.test.ts __tests__/auth.failClosedAndDurableRateLimit.test.ts lib/__tests__/sandpackBuilder.test.ts __tests__/releaseReadiness.contracts.test.ts __tests__/releaseReadiness.execution.contract.test.ts`.
- 2026-04-07: Patch 750: Preview-/Release-Truth-Block repariert — `preview_page` nutzt beim invaliden Secret wieder den Contract-Code `preview_payload_invalid` (statt Drift `preview_invalid_payload`), wodurch `typecheck:edge` und der Release-Gate wieder passieren; Preview-Contract-Test haertet den exakten Invalid-Code regressionsfest. Zusaetzlich wurde der QR-Leak-Pfad fail-safe geschlossen (kein externer `api.qrserver.com`-Flow mehr, Secret-URL in der Preview-UI maskiert) und `check_docs_contracts.js` gegen reine Marker-Compliance gehaertet (aktive Marker + externe-Offenheits-Disclaimer, Legacy-Kontext-Guard). Validation: `npm run typecheck:edge`, `bash scripts/check_release_readiness.sh`, `npm run test:silent -- --runInBand __tests__/previewEdgeErrorContract.test.ts __tests__/previewHelpers.test.ts __tests__/releaseReadiness.contracts.test.ts`, `node scripts/check_docs_contracts.js`.
- 2026-04-07: Patch 749: Preview-Legacy-Removal finalisiert — `preview_page` akzeptiert Secrets nur noch ueber Header-Handoff (`x-k1w1-preview-secret`), der Legacy-`?secret=`-Bridgepfad wurde entfernt, und der Contract-Test sichert das fail-closed Verhalten inkl. expliziter Missing-Header-Meldung. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/previewEdgeErrorContract.test.ts`.
- 2026-04-07: Patch 748: Mini-Finish fuer PR-572-Abschluss — `main` aus den verbliebenen Writeback-Regexen entfernt (`.github/workflows/eas-link.yml` und `.github/workflows/k1w1-ci-lite-autofix.yml`, inkl. korrespondierender Managed-/Template-Pfade), sodass Writeback nur noch auf `work|codex|dev|develop` erlaubt ist; Invariant-Tests und Doku auf den finalen Scope nachgezogen. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/easBuildWritebackRefGuard.invariants.test.ts __tests__/patch422.templateWorkflowBaseline.invariants.test.ts __tests__/previewEdgeErrorContract.test.ts`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_workflow_template_drift.sh`, `npm run -s docs:lint`, `npm run -s docs:check:contracts`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-07: Patch 747: Abschluss-Follow-up fuer PR-572 Restpunkte ohne erneute Persistenz-Refactors — Doku/Review/TODO auf ehrliche Rest-Risiko-Kommunikation nachgezogen (keine pauschalen „alles sauber abgeschlossen“-Formeln), Preview-Legacy-Kompatibilitaet explizit begrenzt dokumentiert, und Writeback-Scope in Workflows/Managed-Templates weiter verengt (EAS-Build-Autofix: `work|codex|dev|develop`; EAS-Link: `work|codex|main|dev|develop`; keine generischen `feature/*|hotfix/*|release/*`). Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/easBuildWritebackRefGuard.invariants.test.ts __tests__/previewEdgeErrorContract.test.ts`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_workflow_template_drift.sh`, `npm run -s docs:lint`, `npm run -s docs:check:contracts`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-07: Patch 746: Persistenz-/Recovery-Restpunkte aus PR-572 Follow-up geschlossen — Read/Decrypt erzeugt bei fehlendem/ungueltigem SecureStore-Key keinen neuen Key mehr (NoRekeyOnRead), kaputte verschluesselte sowie kaputte unverschluesselte Payloads liefern explizite Recovery-Fehler statt still `null`, und ProjectContext blockiert normale Debounce-/Background-Saves im Recovery-Mode (NoDelayedOverwrite) bis zu einer expliziten Nutzeraktion (`replaceProjectData`). Zusaetzlich EAS-Project-ID-Import nur noch UUID-valide (leer => clear, invalide Werte => skip), Preview-Token-Formatserver-Guard ergaenzt, deaktivierte Legacy-Functions auf `verify_jwt=true` fail-safe gesetzt. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/projectPersistence.encryption.test.ts __tests__/projectContext.persistenceHelpers.test.ts __tests__/easProjectIdImportHelpers.test.ts __tests__/previewEdgeErrorContract.test.ts __tests__/appInfoSecretImportStatusReset.test.ts`, `npm run -s test:silent -- --runInBand __tests__/disabledEdgeVerifyJwtDefaults.invariants.test.ts`.
- 2026-04-07: Patch 745: AppInfo Secret-Import Status-Reset aus `useAppInfoScreen` in den dedizierten Helper `screens/AppInfoScreen/hooks/secretImportStatusReset.ts` verschoben, den test-only Hook-Export entfernt und den zugehoerigen Test auf den produktiven Helper-Import umgestellt. Validation: `npm run -s typecheck`, `npm run -s lint:ci`, `npm run -s test:silent -- --runInBand __tests__/appInfoSecretImportStatusReset.test.ts __tests__/previewEdgeErrorContract.test.ts`, `npm run -s test:silent`.
- 2026-04-06: Patch 744: Release-/Workflow-Trust-Drift im kombinierten Scope ehrlich geschlossen — der reale Gate-Pfad war initial rot (`bash scripts/check_workflow_edge_contracts.sh` meldete fehlenden build_admin-Contract-Marker in `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts`), wurde im selben Durchlauf mit dem geforderten Marker (`JWT role=build_admin (oder service_role fuer Server-Caller)`) repariert und per `bash scripts/check_release_readiness.sh` wieder gruen bestaetigt; zusaetzlich wurde `.github/workflows/eas-link.yml` fuer manuelle Writebacks auf read-default + job-scoped write + sichere explizite Branch-Guards gehaertet. Validation: `bash scripts/check_workflow_edge_contracts.sh`, `npm run typecheck`, `npm run lint:ci`, `bash scripts/check_release_readiness.sh`, `npm run docs:lint`, `npm run docs:check:contracts`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-04: Patch 743: Secret-Hotfixes im AppInfo-Import/Export/Backup-Block — API-Config-Export redaktiert jetzt API-Keys fail-closed (leere Arrays statt Klartext), Import-/Export-Flows fuer API-Config und encrypted scoped backups raeumen temporaere Cache-Dateien idempotent auf, und Secure-Backup reduziert unnoetige Secret-Duplikation (kein neues `ciSecrets`-Mirror im Export; Restore bleibt kompatibel ueber bestehenden Fallback).
- 2026-04-03: Patch 742: Abschluss-Sync fuer SoT-/Backlog-Doku nach operativ erledigtem Supabase-Test-User-Cleanup — Restpunktlage in TODO/Review auf klare Kategorien nachgezogen (kritisch offen: keine; bewusst offen: `diagnostics_reports`; Hygiene: optionale Plattform-/Live-Checks), Kern-MDs auf Patch-742-Header synchronisiert, Patchlog bereinigt. Validation: `npm run typecheck`, `npm run typecheck:edge`, `npm run lint:ci`, `node scripts/docsLint.js`, `node scripts/check_docs_contracts.js`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-03: Patch 741: MD-Review-/Sync-Durchlauf nach abgeschlossenem Supabase-Test-User-Cleanup (`h91874350@gmail.com` / `BlauBeerToni84`) — README/INDEX/TESTING_GUIDE/FRESH_CHECKOUT/TODO/Review auf konsistenten Stand-Header (Patch 741) gezogen, externe Restpunktlage praezisiert (Test-User-Cleanup erledigt, `diagnostics_reports` weiterhin bewusst offene Produktentscheidung), Patchlog-SoT nachgezogen. Validation: `node scripts/docsLint.js`, `node scripts/check_docs_contracts.js`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-03: Patch 740: SoT-Konsolidierung nach Review-Follow-up fuer den bestaetigten Live-Abschluss von `k1w1-handler` — der Edge-Caller-Audit wurde widerspruchsfrei auf den bestaetigten Ist-Stand umgestellt (kein verbleibender kritischer Auth-Driftpunkt, JWT/fail-closed live bestaetigt); TODO/Review/README/Patchlog synchronisiert, `diagnostics_reports` bleibt bewusst offen. Validation: `npm run typecheck`, `npm run typecheck:edge`, `npm run lint:ci`, `npm run docs:check:contracts`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-03: Patch 739: Abschluss-Synchronisierung der SoT-Doku nach extern bestaetigtem Live-Auth-Check fuer `k1w1-handler` (Bearer-JWT => `400 invalid_request_payload`, ohne Token => `401 Unauthorized`); `docs/TODO.md`, `docs/reviews/Review.md` und `docs/reviews/edge_function_caller_audit_2026-04-03.md` markieren damit den kritischen Auth-Migrationspunkt als abgeschlossen, `save_preview` bleibt live/repo-konsistent, `diagnostics_reports` bleibt bewusst offene Produktentscheidung. Validation: `npm run typecheck`, `npm run typecheck:edge`, `npm run lint:ci`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-03: Patch 738: Repo-Trace fuer Edge-Caller-Lage (`k1w1-handler`/`save_preview`) konsolidiert und als Entscheidungsvorlage dokumentiert (`docs/reviews/edge_function_caller_audit_2026-04-03.md`); Ergebnis: `save_preview` live laut bestaetigtem Befund bereits `verify_jwt=true` (kein akuter Flag-Restpunkt), `k1w1-handler` bleibt externer kritischer Driftpunkt (`verify_jwt=false` live vs. JWT-Repo-Contract) und ist nur mit vorgelagerter Caller-Migrations-/Runbook-Klaerung sicher deploybar. Doku-SoT in `docs/TODO.md` und `docs/reviews/Review.md` entsprechend synchronisiert. Validation: `npm run typecheck`, `npm run typecheck:edge`, `npm run lint:ci`.
- 2026-04-03: Patch 737: Kleine Workflow-Hygiene fuer CI-Lite-Autofix — unnoetige Workflow-Permission `actions: write` in `.github/workflows/k1w1-ci-lite-autofix.yml` entfernt und denselben Scope im Shared-Template `shared/workflows/managedWorkflowTemplates.ts` synchronisiert; Writeback/Repository-Dispatch bleiben ueber `contents: write` abgedeckt. Validation: `npm run typecheck`, `npm run typecheck:edge`, `npm run lint:ci`, `npm run test:silent -- --runInBand edgeHelperVisibility.invariants.test.ts`, `npm run test:silent -- --runInBand k1w1Handler.providers.invariants.test.ts`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_eas_manual_trigger_controls.sh`, `bash scripts/check_eas_production_credentials.sh`, `bash scripts/check_eas_strict_lockfile_policy.sh`, `bash scripts/check_edge_helper_visibility.sh`, `bash scripts/check_k1w1_handler_providers.sh`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_supabase_deploy_workflow.sh`, `npm run verify:release`.
- 2026-04-03: Patch 735: `diagnostics_reports`-Policy-Widerspruch explizit geklaert statt blind gefixt (Decision-Note `docs/reviews/diagnostics_reports_policy_decision_2026-04-03.md`, aktuelle Repo-SoT bleibt fail-closed fuer `anon`/`authenticated`) und low-risk `search_path`-Nachzug fuer Trigger-/Cleanup-Helfer als idempotente Migration `supabase/migrations/20260403010000_search_path_followup.sql` ergaenzt; dazu neuer Invariant-Test `__tests__/patch735.diagnosticsReportsAndSearchPathFollowup.invariants.test.ts`.
- 2026-04-03: Patch 734: bestaetigte Supabase-Live-Befunde repo-seitig als idempotente Migration vorbereitet (ohne Live-Deploy) — neue Migration `supabase/migrations/20260403000000_supabase_live_findings_hardening.sql` re-assertet `build_jobs` fail-closed fuer `anon`/`authenticated`, haertet Legacy-`cleanup_old_previews(integer)` bei Existenz auf `service_role`-only mit explizitem `search_path = public, pg_temp`, und setzt fuer `signing_audit_log` explizite deny-policies fuer Read/Write gegen `anon`/`authenticated`; dazu neuer Invariant-Test `__tests__/patch734.supabaseLiveFindingsRepoHardening.invariants.test.ts`. Validation: `npm run typecheck`, `npm run typecheck:edge`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patch734.supabaseLiveFindingsRepoHardening.invariants.test.ts`, `npm run test:silent -- --runInBand edgeHelperVisibility.invariants.test.ts`, `npm run test:silent -- --runInBand k1w1Handler.providers.invariants.test.ts`, `npm run verify:release`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_eas_manual_trigger_controls.sh`, `bash scripts/check_eas_production_credentials.sh`, `bash scripts/check_eas_strict_lockfile_policy.sh`, `bash scripts/check_edge_helper_visibility.sh`, `bash scripts/check_k1w1_handler_providers.sh`, `bash scripts/check_supabase_deploy_workflow.sh`.
- 2026-04-03: Patch 733: Template-Workflow-Paritaet + Persistenz-/RateLimit-Guards nachgezogen — in `templates/expo-sdk54-base.json` und `templates/expo-sdk54-full.json` wurden die eingebetteten `actions/upload-artifact`-Pins auf denselben Full-SHA wie die Live-Workflows (`ea165f8d65b6e75b540449e92b4886f43607fa02`) gezogen, sodass `patch422.templateWorkflowBaseline` wieder gruen ist; `normalizeStoredSupabaseRaw(...)` verwirft jetzt explizit Legacy-Secret-Kompositwerte (`url:::key`), und durable-rate-limit Fallback-Logs markieren den lokalen Ersatzpfad explizit als `local_in_memory_best_effort` + `cluster_safe=false`. Doku-/Release-Hinweise fuer verify_jwt-Live-Drift wurden in Testing/Status/Readiness klarer gemacht (ohne Live-Mutation). Validation: `npm run typecheck`, `npm run typecheck:edge`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patch422.templateWorkflowBaseline.invariants.test.ts __tests__/supabaseRawNormalization.test.ts __tests__/auth.failClosedAndDurableRateLimit.test.ts __tests__/releaseReadiness.contracts.test.ts`, `npm run test:silent`, `bash scripts/check_workflow_template_drift.sh`, `npm run verify:release`.
- 2026-04-03: Patch 732: Security-/SoT-Nachzug fuer fail-closed Allowlist-Guards + lokales Preview-Eval-Hardening — `isAllowedGithubRepo(...)` blockiert jetzt fehlende/leere `K1W1_ALLOWED_GITHUB_REPOS` fail-closed; `trigger-eas-build` und `github-workflow-dispatch` blockieren fehlende/leere `K1W1_ALLOWED_REF_REGEX` fail-closed; `actions/upload-artifact` ist workflow-/template-weit auf einen konsistenten Full-SHA (`ea165f8d65b6e75b540449e92b4886f43607fa02`) vereinheitlicht; `buildSandpackHtml(...)` liefert ohne explizite Dev/Test-Freigabe nur noch einen gesperrten Production-HTML-Pfad statt Eval/Babel/CDN-Execution. Validation: `npm run typecheck`, `npm run typecheck:edge`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/edgeAllowlist.failClosed.test.ts lib/__tests__/sandpackBuilder.test.ts`, `npm run verify:release`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_workflow_template_drift.sh`.
- 2026-04-03: Patch 731: Abschlussrunde-Hardening + nächste 10 Durchläufe dokumentiert — `__tests__/useChatAIFlow.pendingPlan.guard.invariants.test.ts` prüft zusätzlich, dass `isDirectBuildCommand(...)` konsistent sowohl für Scout-Handoff als auch Metrics-Pfad verwendet wird; `docs/TODO.md` enthält jetzt eine klare Definition von "Durchlauf" sowie eine priorisierte Empfehlungsliste für die nächsten 10 Durchläufe. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/useChatAIFlow.pendingPlan.guard.invariants.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-03: Patch 730: Scout-Handoff-Command-Erkennung zentralisiert — `useChatAIFlow` nutzt jetzt den neuen Helper `isDirectBuildCommand(...)`, damit Direct-Build-Trigger (`direkt build`/`build`/`jetzt builden`) konsistent für Metrikzählung und Scout-Handoff ausgewertet werden; Regression in `__tests__/useChatAIFlow.summary.regression.test.ts` deckt die Varianten ab. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/useChatAIFlow.summary.regression.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-03: Patch 729: Scout-Mode-PendingPlan-Invariants abgesichert — `__tests__/useChatAIFlow.pendingPlan.guard.invariants.test.ts` prüft jetzt explizit, dass Scout-Mode nur mit `direkt build` aus der Analysephase in die Builder-Phase wechselt und der Scout-Hinweistext weiterhin vorhanden ist. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/useChatAIFlow.pendingPlan.guard.invariants.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 728: Scout-/Audit-only-Modus im Chat-Flow ergänzt — `looksLikeScoutModeRequest(...)` erkennt explizite Scout/Audit-only-Intents; `useChatAIFlow` setzt den Pending-Plan auf `mode: "scout"` und blockiert die automatische Builder-Phase, bis der User explizit `direkt build` bestätigt. Regression in `__tests__/chatHeuristics.plannerRouting.test.ts` deckt Scout-Erkennung ab. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/chatHeuristics.plannerRouting.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 727: Lokale/anonymisierte Rückfrage-Qualitätsmetriken ergänzt — neues Modul `lib/chatQualityMetrics.ts` erfasst lokal (AsyncStorage) Intent-Bestätigungsereignisse (`prompt`/`planen`/`direkt build`); `useChatAIFlow` protokolliert Prompt-Ausspielung und bestätigende User-Antworten fail-safe im Hintergrund. Regression `lib/__tests__/chatQualityMetrics.test.ts` sichert Counters + malformed-JSON-Resilienz, `__tests__/chatHeuristics.plannerRouting.test.ts` deckt weiterhin Grenzfall-Intentpfad ab. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand lib/__tests__/chatQualityMetrics.test.ts __tests__/chatHeuristics.plannerRouting.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 726: Command-Intent mit Confidence + Grenzfall-Bestätigung gehärtet — neues `classifyChatIntent(...)` liefert Intent/Confidence/Reason und triggert bei Low-Signal-Kommandos eine kurze Bestätigungsfrage (`planen` vs `direkt build`) statt stiller Heuristik; `useChatAIFlow` nutzt diese Entscheidung für das Planner-Routing. Regressionen in `__tests__/chatHeuristics.plannerRouting.test.ts` decken High-Confidence-Builder und Confirmation-Grenzfall ab. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/chatHeuristics.plannerRouting.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 725: Guard-Policy-Hinweis vor Vorschlägen im Planner-Reply — `useChatAIFlow` ergänzt vor dem Planner-Text einen expliziten Pre-Hinweis mit `allowed`/`guarded`-Chips, damit der Guard-Rahmen bereits vor dem eigentlichen Dateivorschlag sichtbar ist; Regression in `__tests__/useChatAIFlow.summary.regression.test.ts` sichert die Hint-Copy. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/useChatAIFlow.summary.regression.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 724: Planner-Rückfragen auf strukturiertes Slot-Format gehärtet — `buildPlannerMessages` fordert fehlende Rückfragen jetzt explizit als Slot-Liste (`[SLOT] <Name>: <Frage>`) statt Fließtext; Regression in `__tests__/promptEngine.contextPriority.test.ts` sichert den Instruktions-Contract. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/promptEngine.contextPriority.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 723: Sichtbarer Kontextkürzungs-Hinweis im Chat-Flow — `useChatAIFlow` extrahiert jetzt den internen `aiContextBudget`-Marker aus Planner-/Builder-Prompts und zeigt ihn einmalig als System-Hinweis (`🏷️ Kontext gekürzt`) im Chat an; Regression in `__tests__/useChatAIFlow.summary.regression.test.ts` prüft Marker-Extraktion und No-Marker-Fall. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/useChatAIFlow.summary.regression.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 722: Strukturierte Pre-Flight-Zusammenfassung vor Datei-Review — `useChatAIFlow` baut jetzt vor der eigentlichen Dateiliste einen expliziten Intro-Block (`Pre-Flight (voraussichtlich)`) ein, der klar zwischen neu/aktualisierten Dateien und manuell verbleibenden Pfaden unterscheidet; neue Regression `__tests__/useChatAIFlow.summary.regression.test.ts` sichert den Intro-Text. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/useChatAIFlow.summary.regression.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 721: Persistenz-Scope-Hinweis in Settings explizit gemacht — `PrivacySection` zeigt jetzt klar „Scope: pro Projekt (kein globales, projektübergreifendes Gedächtnis)“; neue Regression `__tests__/privacySection.scopeLabel.test.tsx` sichert die Sichtbarkeit. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/privacySection.scopeLabel.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 720: User-Vorschlagsliste in TODO aufgenommen — alle angefragten offenen Verbesserungen (Intent-Classifier, strukturierte Planner-Slots, Kontextkürzungs-Badge, strukturierte Pre-Flight-Zusammenfassung, Persistenz-Scope-Label, Guard-Policy-Chips vor Vorschlägen, lokale Rückfragen-Qualitätsmetriken, Großprojekt-/Scout-Modus) sind als neue TODO-Punkte dokumentiert. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 719: Guard-Telemetrie Event-Normalisierung + Payload-Cap — `guardAuditTelemetry` trimmt/filtriert jetzt leere Event-Eintraege und begrenzt verarbeitete Eintraege pro Event auf 50 (defensiver gegen noisy/oversized Inputs); Regression deckt Ignore-Empty + Cap-Verhalten ab. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand lib/__tests__/guardAuditTelemetry.test.ts __tests__/confirmChangesModal.guardAuditFlow.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 718: Guard-Audit-Signatur case-insensitive normalisiert — Dedupe-Signatur im `ConfirmChangesModal` normalisiert jetzt auf lowercase, dedupliziert und begrenzt auf max. 20 Signatur-Eintraege; dadurch fuehren reine Case-Varianten/duplizierte Hinweise nicht zu Extra-Events. Regression deckt Reorder+Case+Duplikat in einem Lauf ab. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/confirmChangesModal.guardAuditFlow.test.tsx __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 717: Guard-Audit-Signatur stabilisiert — Dedupe-Signatur im `ConfirmChangesModal` ist jetzt order-insensitive (trim + unique + sort), sodass identische Guard-Hinweis-Mengen in anderer Reihenfolge kein zusaetzliches Audit-Event erzeugen; Regression sichert das Reorder-Verhalten. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/confirmChangesModal.guardAuditFlow.test.tsx __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 716: Guard-Audit-Dedupe im Confirm-Modal — Telemetrie-Write wird jetzt pro sichtbarer Modal-Session auf Signaturbasis dedupliziert (keine Doppelzaehlung bei unveraendertem Re-Render); beim Close wird die Signatur resetet, Reopen erfasst erneut. Neue Regression `__tests__/confirmChangesModal.guardAuditFlow.test.tsx` sichert das Verhalten. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/confirmChangesModal.guardAuditFlow.test.tsx __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 715: P2-lokale Guard-Audit-Telemetrie — neues Modul `lib/guardAuditTelemetry.ts` erfasst lokal (AsyncStorage) Guard-Event-Haeufigkeit + Marker-Buckets ohne externe Exfiltration; `ConfirmChangesModal` protokolliert Guard-Hinweise fail-safe im Hintergrund, und neue Regressionen sichern Bucketing + malformed-JSON-Resilienz. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand lib/__tests__/guardAuditTelemetry.test.ts __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 714: P2-Policy-Explain im Guard-Hinweis — `ConfirmChangesModal` bietet jetzt einen umschaltbaren Kurz-Erklärblock (`Warum Guard-Regeln?`) mit kompakter Begründung und Beispielen (Secrets/Baseline/Deploy-Infra/Owner-Pfade); Explain-State resetet beim Modal-Close, Tests sichern Toggle + Expand-Content. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 713: P1-Structured-Follow-up fuer Guarded-Pfade — `ConfirmChangesModal` zeigt im Guard-Hinweis jetzt explizite Safe-Optionen (A: nur unkritische Dateien anwenden + manuelle TODO-Liste fuer guarded Pfade; B: sichere Minimal-Variante ohne guarded Pfade), damit das naechste Vorgehen sofort klar ist; Tests decken Show/Hide der Optionen ab. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 712: P1-Path-Chips im Datei-Review — `ConfirmChangesModal` zeigt jetzt pro Datei einen expliziten Pfadstatus-Chip (`wird geändert` fuer new/updated, `manuell nötig` fuer skipped), damit Guard-/Manual-Pfade sofort sichtbar sind; `__tests__/ConfirmChangesModal.review.test.tsx` sichert beide Chip-Zustaende. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/ConfirmChangesModal.review.test.tsx __tests__/chatComposer.guardBadge.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 711: Guard-Hint-Erkennung helper-first zentralisiert — neues Shared-Modul `lib/guardHints.ts` kapselt Marker-Detection (`hasGuardHint`/`extractGuardHints`), `ChatScreen` und `ConfirmChangesModal` nutzen jetzt denselben Pfad statt duplizierter Markerlogik; neue Regression `__tests__/guardHints.test.ts` deckt Erkennung/Filterung ab. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/guardHints.test.ts __tests__/chatComposer.guardBadge.test.tsx __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 710: P0-Durchlauf-2 Chat-Guard-UX — `ChatComposer` zeigt jetzt einen sichtbaren Schreibstatus-Badge (`Normal write` vs. `Guarded path enthalten`), `ChatScreen` verdrahtet den Badge anhand aktueller Guard-/Ownership-Hinweise, und die Planner-Antwort in `useChatAIFlow` enthaelt einen expliziten Guarded-Pfad-Hinweis vor dem Build-Start. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/chatComposer.guardBadge.test.tsx __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 709: P0-Start Chat-Guard-UX — `ConfirmChangesModal` zeigt bei Guard-/Ownership-Hinweisen jetzt einen expliziten manuellen Guard-Block (inkl. Warntext + fokussierte Guard-Liste); bestehende Review-/Accept-/Reject-Flows bleiben unveraendert. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/ConfirmChangesModal.review.test.tsx`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 708: Neues Operator-Ausfuehrungs-Runbook `docs/runbooks/OPERATOR_EXECUTION_CHECKLIST.md` ergänzt (kompakter, abhakbarer extern/Live-Staging-Pfad inkl. Provisioning, Verify, Troubleshooting, Abschluss). `docs/INDEX.md` verlinkt den neuen Einstieg. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 707: TODO-Nachzug fuer spaeteren Chat-Guard-UX-Backlog — die abgestimmten P0/P1/P2-Punkte (Guard-Badge, Confirm-Hinweis, Planner-Info, Path-Chips, Structured Follow-up, Policy-Explain, lokale Audit-Telemetrie) sind explizit als geparkte Folgearbeit in `docs/TODO.md` aufgenommen; kein Produktcode geaendert. Validation: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 706: Release-Readiness-Live-Env-Guard gehaertet — `scripts/check_release_readiness.sh` fuehrt jetzt immer `scripts/check_edge_live_env_readiness.sh` aus, um fehlende Live-Edge-Variablen explizit als SKIP-Hinweis zu melden; env-gated `check_edge_live_contracts.sh` bleibt unveraendert optional. Validation: `npm run test:silent -- --runInBand __tests__/releaseReadiness.contracts.test.ts`.
- 2026-04-02: Patch 705: Durchlauf-15-Release-Meta-Haertung — neuer Contract-Test `__tests__/releaseReadiness.contracts.test.ts` sichert die zentrale Guard-Check-Verdrahtung von `scripts/check_release_readiness.sh` und die explizit env-gated Optionalitaet der Live-Edge-Checks ab. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/releaseReadiness.contracts.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 704: Durchlauf-14-Security-Negativtests fuer `_shared/auth::requireScopedEdgeAuth(...)` ergaenzt — neue Runtime-Regression prueft fail-closed bei fehlender Secret-Config, malformed Authorization, mixed admin+bearer (dual off), bearer-only auf scoped-admin Route und den gueltigen Admin-Success-Pfad. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/edgeAuth.scopedGuard.behavior.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 703: Durchlauf-13-Concurrency-Stresslauf fuer `easLinkStatusRequestGuard` ergaenzt — neuer deterministischer Stresstest mit 200 gemischten Operationen prueft Request-ID-Konsistenz und `isCurrent`-Semantik gegen ein Modell; Produktionscode unveraendert. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/githubReposScreen.easLinkStatusRequestGuard.stress.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 702: Durchlauf-12-Property-Lite fuer PatchEngine ergaenzt — neue Regressionen in `__tests__/patchEngine.propertyLite.test.ts` sichern Empty-Patch-Identitaet, deterministische Ausgabe bei gleichen Inputs und eindeutige Ergebnis-Pfade ohne Duplikate. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patchEngine.propertyLite.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 701: Durchlauf-11-Path-Safety-Follow-up — `lib/diagnostics/patchEngine.ts` blockt jetzt jedes Laufwerks-Praefix `^[A-Za-z]:` (inkl. drive-relativ `C:...`), und `__tests__/patchEngine.pathSafety.test.ts` deckt zusaetzlich UNC-/Backslash-root-/mixed-separator-/Null-Byte-Pfade regressionsfest ab. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patchEngine.pathSafety.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 700: P1-Windows-Pfadkante aus Patch 699 geschlossen — `lib/diagnostics/patchEngine.ts` blockt jetzt zusaetzlich Null-Byte-/UNC-/Backslash-root-/Drive-absolute Pfade und erkennt Traversal nach Separator-Normalisierung (`\` -> `/`); `__tests__/patchEngine.pathSafety.test.ts` deckt `..\secrets.txt` und `C:\temp\secret.txt` regressionsfest ab. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patchEngine.pathSafety.test.ts`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-04-02: Patch 699: `lib/diagnostics/patchEngine.ts` fail-closed gehaertet — Core-Patch-Anwendung blockt jetzt unsichere Pfade (leer/absolut/Traversal `..`) und bricht bei mehr als 200 Gesamtoperationen defensiv ab; neue Regressionen sichern Traversal- und Oversize-Faelle. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`.
- 2026-04-02: Patch 698: Final-Gate-/Freigabe-Durchlauf auf dem aktuellen Projektstand abgeschlossen — alle geforderten Voll-Gate-Checks inkl. Typecheck/Lint/Edge/Jest/Docs/Contracts/Release/Legacy-Edges wurden real erneut ausgefuehrt und liefen lokal gruen; es gab keine reproduzierbaren Blocker und keinen notwendigen Codefix. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run typecheck:edge`, `npm run test:silent`, `npm run docs:lint`, `npm run docs:check:contracts`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_edge_helper_visibility.sh`, `bash scripts/check_edge_rate_limit_retention.sh`, `bash scripts/check_release_readiness.sh`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_legacy_disabled_edges.sh`.
- 2026-04-02: Patch 697: Reiner QA-/Verifikationslauf mit direkter Fehlerbehebungsbereitschaft abgeschlossen — kompletter Jest-Lauf und der geforderte Voll-Gate wurden real ausgefuehrt; es gab keine roten Tests und keinen reproduzierbaren Produkt-/Test-/Fixture-/Mock-Befund, daher war kein Codefix noetig. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run typecheck:edge`, `npm run test:silent`, `npm run docs:lint`, `npm run docs:check:contracts`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_edge_helper_visibility.sh`, `bash scripts/check_edge_rate_limit_retention.sh`, `bash scripts/check_release_readiness.sh`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_legacy_disabled_edges.sh`.
- 2026-04-02: Security/Auth/Edge-Haertungsscan (gezielter Fix) — `supabase/functions/github-workflow-logs/index.ts` validiert `runId` jetzt strikt als positive Ganzzahl statt als beliebige Number (zuvor waren negative/dezimale Werte moeglich und wurden via `Math.trunc(...)` implizit umgeschrieben); neue Invariant-Regression sichert den Guard dauerhaft ab. Validation-Stand: `npm run test:silent -- --runInBand __tests__/githubWorkflowLogs.helpers.test.ts __tests__/githubWorkflowLogs.security.invariants.test.ts __tests__/githubWorkflowLogs.runIdValidation.invariants.test.ts`.
- 2026-04-02: Patch 696: Deep-Scan-Voll-Gate erneut komplett lokal ausgefuehrt und gruen bestaetigt; `docs/reviews/Review.md` truthfulness-korrigiert (keine veraltete Restunsicherheit zu `npm run lint:ci`/komplettem Jest-Lauf mehr), Verifikationsliste auf den real gelaufenen Voll-Gate-Umfang gezogen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run typecheck:edge`, `npm run test:silent`, `npm run docs:lint`, `npm run docs:check:contracts`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_edge_helper_visibility.sh`, `bash scripts/check_edge_rate_limit_retention.sh`, `bash scripts/check_release_readiness.sh`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_legacy_disabled_edges.sh`.
- 2026-04-02: Doku-Konsolidierung nach Deep-Scan-/Cleanup-/Security-Runden — README/INDEX/OVERVIEW/PROJECT_CONTEXT/SYSTEM_README/TESTING/FRESH-CHECKOUT/REVIEW/TODO auf einen kleineren kanonischen Stand gezogen; redundante Root-Zusammenfassung, `docs/README_EXTENDED.md` und die stale `docs/05-contract-test-coverage-matrix.md` entfernt; `docs/patches/README.md` als Archivhinweis erneuert. Validation-Stand: `node scripts/docsLint.js` + `node scripts/check_docs_contracts.js`.
- 2026-03-31: Patch 695: Kleiner Follow-up auf Patch 694 — `utils/syntaxValidator.ts` behandelt Delimiter in Template-Literal-Ausdruecken `${...}` jetzt ebenfalls als echten Codepfad, statt sie pauschal mit dem Template-Text zu ueberspringen; eine neue Regression deckt den zuvor noch moeglichen False-Negative-Fall mit unausgeglichenen Klammern innerhalb von `${...}` ab. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 694: Kritischer Pass-53-Nachzug schliesst zwei belastbare Deep-Scan-Funde — `utils/syntaxValidator.ts` ignoriert Klammern in Strings/Regex/Kommentaren jetzt korrekt und prueft Import-Nutzung ausserhalb der Importzeilen, waehrend `supabase/functions/_shared/auth.ts` fuer Auth-/Durable-Rate-Limit-Reads einen gemeinsamen 8s-Timeout-Wrapper nutzt und `rateLimit(...)` bei grossem lokalen Keyset konservativ veraltete Eintraege bereinigt; fokussierte Regressionen decken Syntax-Validator-False-Positives und Timeout-Signals ab. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 693: Kritischer Follow-up-53-Verifikationslauf bestaetigt, dass nach Patch 692 kein reproduzierbarer Produkt-/CI-Befund und keine neue Kern-MD-/Patch-/Checklog-Drift vorliegen; 53 schliesst damit bewusst als kleiner Truthfulness-/SoT-Patch statt als kuenstlich erzwungener Bugfix-/Test-Fix-Block. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 692: Bedingter Docs-/History-Hygiene-Check nach der Stabilitaetsentscheidung aus Patch 691 bestaetigt, dass keine neue Kern-MD-/Patch-/Checklog-Drift entstanden ist; README/TODO/INDEX/TESTING/FRESH-CHECKOUT sowie Patchlog und Checklog wurden explizit auf denselben Patchstand gezogen. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 691: Deep-Scan-/Stabilitaetsentscheidung nach Patch 690 umgesetzt — erneuter Scan bestaetigt, dass ausserhalb von Docs/Checklog/Review/Historie aktuell keine `any`-Reste mehr offen sind; README/TODO/INDEX/TESTING/FRESH-CHECKOUT/Review/Hotspots wurden auf denselben Patchstand gezogen, und die naechsten Schritte sind jetzt bewusst als Stabilitaetsfenster statt weiterer kuenstlicher Refactor-Wellen formuliert. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 690: Docs-/History-Kompaktion im README und den Kern-MDs umgesetzt — das README fuehrt den aktuellen Stand wieder kompakt statt mit doppelten/ueberlappenden Recent-Bloecken, verweist fuer die append-only Historie klar auf `docs/patches/PATCHLOG_ROOT.md` und `PROJECT_CHECKLOG.md`, und `docs/INDEX.md`, `docs/TESTING_GUIDE.md`, `docs/FRESH_CHECKOUT_GREEN_PATH.md` sowie `docs/TODO.md` stehen wieder auf demselben Patchstand. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 689: Invariant-/String-Hygiene helper-first nachgezogen — `__tests__/helpers/invariantSnippetHelpers.ts` baut verbotene `any`-Snippets fuer Repo-Source-Invariants jetzt ohne rohe `"as any"`-/`catch (...: any)`-String-Literale, `ciLitePatch.invariants`, `ciLiteHeaderWorkflow.invariants`, `patch483.githubReposScreen.step8.invariants`, `patch462.githubReposScreen.restFixes.invariants` und `patch570.typeContracts.invariants` nutzen den gemeinsamen Repo-Reader statt lokaler `fs/path`-Duplikate, und der produktive Code bleibt unveraendert. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 688: Rebaseline-/Deep-Scan-Durchlauf nach Patch 687 abgeschlossen — Kern-MDs/Review/Hotspots stehen wieder auf demselben Patchstand, der Rest-`any`-Debt wird explizit als Test-/Docs-/Historien-Thema gefuehrt, und `README.md` hat keinen ueberlappenden Recent-Patch-Block mehr. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 687: UI-/Workflow-Testwelle helper-first umgesetzt — `App.test.tsx` nutzt jetzt getypte Children-Props statt `Navigator`-/`Screen`-/`NavigationContainer`-Komponenten mit `any`, `smoke.test.ts` verwendet explizite Mock-Typen fuer AsyncStorage und SecureStore statt `let ...: any`, und `invariants.strings.test.ts` vergleicht die kanonischen Workflow-Dateinamen ohne `as any`-Casts. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 686: Contract-/Invariant-Testwelle helper-first umgesetzt — `patch570.typeContracts.invariants.test.ts` und `ciLitePatch.invariants.test.ts` lesen Repo-Quellen jetzt gemeinsam ueber `readRepoText(...)`, `supabaseErrorSanitization.test.ts` validiert sanitisierten Transport-Output ueber Unknown-/Record-/Array-Reader statt `as any`, und `savePreview.authCorsAndTypecheck.invariants.test.ts` ersetzt die `globalThis.Deno as any`-Pfade durch einen kleinen Runtime-Helper und repariert den verwaisten letzten `it(...)`-Block. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 685: Kritischer Review-/SoT-Follow-up nach den Wellen 27-44 umgesetzt — `ciAutoFix.managedWorkflows.test.ts`, `useCiLiteWorkflow.behavior.test.tsx` und `useChatAIFlow.inputValidation.test.tsx` nutzen jetzt helper-/type-first Mock-/Config-/Result-Reader statt lokaler `any`-Bruecken; ausserdem stehen `docs/INDEX.md`, `docs/TESTING_GUIDE.md` und `docs/FRESH_CHECKOUT_GREEN_PATH.md` wieder auf demselben Patchstand wie README/TODO/Checklog. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 684: Refactor-Durchlauf 44 (diagnostic/patch/invariant test debt cleanup) umgesetzt — `diagnosticFixResultContract.test.ts`, `preflight.lockfileConsistency.test.ts`, `repoSyncOrchestration.test.ts` und `lib/__tests__/fileWriter.test.ts` nutzen jetzt helper-first getypte Preflight-/ProjectFile-Fixtures und `findProjectFile(...)` statt lokaler `as any`-/`(f: any)`-Muster; Produktcode bleibt unveraendert. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 683: Post-wave Deep Scan + Stabilisierungsrunde abgeschlossen — README/TODO/Checklog sowie `docs/INDEX.md`, `docs/TESTING_GUIDE.md` und `docs/FRESH_CHECKOUT_GREEN_PATH.md` stehen wieder auf demselben Patchstand; der Deep Scan bestaetigt, dass ausserhalb von Tests/Docs/Historie keine `any`-Reste mehr uebrig sind, und `docs/04-risk-hotspots.md` / `docs/reviews/Review.md` priorisieren den verbleibenden Debt jetzt ehrlich als Test-/Fixture-/Mock-Thema. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 681: Refactor-Durchlauf 41 (UI/screen test debt cleanup) umgesetzt — `oneClickDeploy.test.tsx`, `githubReposScreen.list.test.tsx`, `chatScreenAttachmentNotice.test.ts`, `ConfirmChangesModal.review.test.tsx` und `connectionsScreen.statusCardSemantics.test.tsx` arbeiten jetzt helper-first ohne lokale `any`-Casts fuer Build-Profile, Deploy-Steps, Attachment-Metadaten, OrchestratorResult, Repo-List-Props und Animated-Mocks; Produktcode bleibt unberuehrt. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 680: Refactor-Durchlauf 40 (preflight/security/patch contract test cleanup) umgesetzt — `lib/__tests__/fixSafety.test.ts`, `__tests__/patchFingerprint.test.ts` sowie die fokussierten `preflight.*`-Vertragstests nutzen jetzt helper-first getypte `PreflightPatch`-/`ProjectFile`-Helper statt lokaler `as any`-Patch-/Datei-Casts; Produktcode bleibt unberuehrt. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 679: Post-test-debt Deep Scan + SoT-Rebaseline abgeschlossen — ausserhalb von Tests/Docs/Historie sind keine `any`-Reste mehr bestaetigt; `docs/INDEX.md`, `docs/TESTING_GUIDE.md` und `docs/FRESH_CHECKOUT_GREEN_PATH.md` wurden auf denselben Patchstand wie README/TODO/Checklog gezogen, und die naechsten Schritte 40–42 sind jetzt nach echten Test-/Fixture-Clustern statt blindem Weiterzaehlen priorisiert. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 678: Refactor-Durchlauf 38 (targeted contract/mock cleanup wave 4) umgesetzt — `auth.failClosedAndDurableRateLimit.test.ts` nutzt getypte `fetch`-Spies ohne `"fetch" as any`, `edgeErrorResponseContracts.test.ts` liest Response-Bodies helper-first ueber einen `JsonRecord`-Reader statt `Promise<any>`, `githubFiles.contracts.test.ts` arbeitet ohne `response(...) as any`, und kleine Restcasts in `bridgeValidation.test.ts` sowie `chatHistoryMigration.test.ts` wurden ueber typed AsyncStorage-Helper bzw. `unknown`-/Record-Narrowing ersetzt. Produktcode bleibt unberuehrt. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 677: Refactor-Durchlauf 37 + Deep-Scan-Nachzug umgesetzt — neue Test-Helper (`makePreflightPatch`, `makePreflightResult`, `makeProjectRef`) entkoppeln Patch-/Diagnostic-Fix-Tests helper-first von lokalen `as any`-Fixtures; `patchEngine.applyOrder`/`deleteRemovesFile`/`idempotency`/`upsertOverwrites`/`jsonMergePreservesSiblings2` laufen ohne Datei-/Patch-Casts, `useDiagnosticFixRunner.fixSemantics` und `diagnosticSmartFix.fixableOnly` nutzen getypte Projekt-/Patch-/Result-Factories statt `projectRef as any`-/`mountedRef as any`-/`fix.patch as any`-Pfaden, und der Deep-Scan-Nachzug synchronisiert die Header-Staende von `docs/INDEX.md`, `docs/TESTING_GUIDE.md` und `docs/FRESH_CHECKOUT_GREEN_PATH.md`. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 676: Refactor-Durchlauf 36 (targeted test utilities + notification/retry fixtures) umgesetzt — `lib/__tests__/notificationService.test.ts` nutzt jetzt einen getypten Internal-View plus `jest.Mocked<typeof Notifications>` statt `notificationService as any`-/Mock-`any`-Zugriffen, `lib/__tests__/retryWithBackoff.test.ts` kapselt retry-Tagging helper-first ueber `RetryableTestError`, und `__tests__/useNotifications.permissions.regression.test.tsx` mockt `initialize`/`getPushToken` direkt ohne Spread-`any[]`. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 675: Refactor-Durchlauf 35 (test/fixture typing cleanup wave 2) umgesetzt — Build-/Readiness-/Diagnostics-Testcluster nutzt jetzt `makeProjectData(...)`, `makeProjectFile(...)` und `createMountedRef(...)` helper-first statt lokaler `ProjectData as any`-/`ProjectFile as any`-/`mountedRef as any`-Fixtures; `buildReadiness*`- und `buildStartService.readinessContract`-Tests lesen AsyncStorage-Mocks direkt, `diagnosticRunners.repoSync.test.ts` nutzt getypte Helper, und `lib/__tests__/buildStartService.integration.test.ts` arbeitet ohne Signatur-/Session-Casts. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 674: Refactor-Durchlauf 34 (test/fixture typing cleanup wave 1) umgesetzt — neue Test-Helper (`findCheckById`, `makeProjectFile`, `pluckIds`) reduzieren die ersten fokussierten Diagnostics-/Pipeline-/ProjectFile-`any`-Reste helper-first, `appInfoSecureBackup.test.ts` nutzt jetzt `AIConfig` plus Union-Narrowing statt `baseConfig: any` / `restored as any`, und mehrere Pipeline-/Diagnostics-Tests lesen Checks/Fixtures ohne lokale `any`-Casts. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 673: Refactor-Durchlauf 33 (tooling script cleanup) umgesetzt — `scripts/patch_edge_forge_getbytes_v20.sh` ersetzt den letzten Non-Test-`any`-Rest ausserhalb von Docs/Historie durch einen getypten Callback-Vertrag fuer den `forge.random.getBytes`-Patch, erkennt alten wie bereits modernisierten Zielcode idempotent und aendert den Android-Keystore-Forge-Patchzweck nicht. Validation-Stand: `bash scripts/check_patch_docs_sync.sh` + `node scripts/docsLint.js`.
- 2026-03-31: Patch 672: Refactor-Durchlauf 32 (generic-any follow-up in productive helpers) umgesetzt — `screens/AppStatusScreen/hooks/appStatusHelpers.ts` liest `app.json` jetzt helper-first ueber lokale Unknown-/Record-Reader statt `safeJsonParse<any>`, und `lib/diagnostics/buildPipelineDiagnostics.ts` nutzt im expo-dev-client-Check `readJsonFile<unknown>` + `readStringDeps(...)` statt `readJsonFile<any>`; fokussierte Regressionen decken Non-Object-/Malformed-JSON-Pfade ab, App-Status-/Diagnostics-/Build-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 671: Refactor-Durchlauf 31 (diagnostic checks JSON typing) umgesetzt — `lib/diagnostics/checks/qualityAndCompat.ts`, `packageAndEntry.ts` und `configAndProfiles.ts` nutzen jetzt kleine Unknown-/Record-Reader statt `parseJson<any>(...)`; Check-/Read-/Autofix-Semantik bleibt unveraendert, fokussierte Regressionen decken Non-Object-/Missing-Profile-/Missing-Dependency-Pfade ab.
- 2026-03-31: Patch 670: Refactor-Durchlauf 30 (navigation typing cluster) umgesetzt — `screens/ChatScreen/hooks/useChatScreen.ts` nutzt jetzt getypte Chat-Route-/Navigation-Parameter statt `useNavigation<any>()` / `useRoute<any>()`, `screens/DiagnosticScreen/index.tsx` verwendet einen getypten `DiagnosticRoute`-Vertrag plus `NavigationProp<ParamListBase>`, und `screens/EnhancedBuildScreen/index.tsx` verwendet `NavigationProp<ParamListBase>` statt `useNavigation<any>()`; Screen-/Flow-/Build-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 669: Refactor-Durchlauf 29 (cross-screen small typing follow-up) umgesetzt — `screens/AppStatusScreen/components/OverviewSection.tsx` nutzt jetzt eine schmale `ProjectData`-View statt `projectData: any`, `screens/AppStatusScreen/hooks/useAppStatusScreen.ts` fuehrt keinen irrefuehrenden `any`-Kommentar mehr, `screens/CodeScreen/index.tsx` tipisiert den `beforeRemove`-Event sauber, `screens/CredentialsWizardScreen/components/KeystoreStatusSection.tsx` nutzt einen getypten Ionicons-Namen statt `icon: any`, `screens/EnhancedBuildScreen/hooks/useOneClickDeploy.ts` haertet die Build-/Secret-Sync-Fehlerpfade ueber `unknown` + `getOneClickDeployErrorMessage(...)`, und `shared/types/build.ts` fuehrt `raw` nur noch als `unknown`; Verhalten/Build-/Wizard-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 668: Refactor-Durchlauf 28 (GitHubRepos component typing follow-up) umgesetzt — `screens/GitHubReposScreen/index.tsx`, `components/DiffFilesSection.tsx`, `LocalRemoteDiffSection.tsx`, `PullPreviewModal.tsx` und `SecretsSection.tsx` nutzen jetzt keine lokalen `catch (...: any)`-/`remote: any[]`-Pfade mehr; Fehlertexte laufen helper-first ueber `getErrorMessage(...)`, der Pull-Preview-Vertrag ist auf `ProjectFile[]` gezogen, und der doppelte TODO-Eintrag fuer Durchlauf 28 wurde im selben Patch konsolidiert. Pull/Push-/Sync-/EAS-Orchestrierung bleibt unveraendert.
- 2026-03-31: Patch 667: Refactor-Durchlauf 27 (Diagnostic component typing follow-up) umgesetzt — `screens/DiagnosticScreen/styles.ts` exportiert jetzt `DiagnosticScreenStyles`, und `FixRunModal.tsx`, `HeaderSection.tsx`, `PreviewModal.tsx`, `ProgressBar.tsx` sowie `hooks/useDiagnosticScreen.ts` nutzen keine lokalen `styles: any` / `anim: any` / `navigation?: any` / `catch (e: any)`-Pfade mehr. Diagnostics-/Runner-/Upload-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 666: Refactor-Durchlauf 26 (AppInfo component typing follow-up) umgesetzt — `screens/AppInfoScreen/componentTypes.ts` buendelt jetzt den lokalen AppInfo-Component-Vertrag (`AppInfoScreenStyles`, API-Key-Config und schmale ProjectData-Views), und `ActiveApiKeysSection.tsx`, `ApiBackupSection.tsx`, `AppSettingsSection.tsx`, `BackupPassphraseModal.tsx`, `ProjectInfoSection.tsx`, `SecureBackupSection.tsx` und `TemplateInfoSection.tsx` nutzen keine lokalen `styles: any` / `projectData: any`-Props mehr. Backup-/Import-/Passphrase-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 665: Refactor-Durchlauf 25 (diagnostic typing helper-first) umgesetzt — `components/diagnostics/SeverityBadge.tsx` nutzt jetzt einen getypten Ionicon-Namen statt `icon: any`; `screens/DiagnosticScreen/index.tsx` liest Fail-Summaries helper-first ueber `diagnosticScreenDisplayHelpers.ts` statt lokaler `r: any`-Filter/Mappings; `useDiagnosticUpload.ts`, `useDiagnosticCiAutofix.ts` und `diagnosticRunners.ts` nutzen jetzt `unknown` + `diagnosticErrorHelpers.ts` statt `catch (...: any)`; `lib/diagnostics/buildPipelineDiagnostics.ts` vervollstaendigt den in Patch 664 referenzierten `getDiagnosticErrorMessage(...)`-Helper sauber. Diagnostics-/Upload-/Pipeline-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 664: Refactor-Durchlauf 24 (productive typing follow-up) umgesetzt — `project/services/templateLoader.ts` normalisiert Template-Dateien jetzt ueber sichere Unknown-Reader statt `file: any`/`files: any[]`; `lib/diagnostics/smartPatch.ts` nutzt fuer Parse-/Merge-Fehler einen kleinen `unknown`-basierten Error-Helper statt `catch (e: any)`; `lib/diagnostics/buildPipelineDiagnostics.ts` liest den Repo-Secret-Listen-Fehlerpfad jetzt ueber `unknown` + `getDiagnosticErrorMessage(...)`; `supabase/functions/github-workflow-logs/helpers.ts` wertet den 404-Run-Status helper-first ueber `asRecord(...)`/`asString(...)` statt `runJson: any` aus. API-/Build-/Logs-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 663: Refactor-Durchlauf 23 (remaining productive typing follow-up) umgesetzt — `lib/validators.ts` liest die Back-compat-Groessenkonstanten jetzt ueber kleine Validation-Reader statt `cfg: any`; `supabase/functions/github-workflow-runs/index.ts` nutzt fuer Request-Body und GitHub-JSON jetzt Record-/String-/Number-Narrowing statt `body: any` / `json: any`; `supabase/functions/k1w1-handler/helpers.ts` extrahiert sichere Gemini-/Anthropic-Textteil-Reader statt lokaler `part: any`-Mappaths. API-/KI-/Workflow-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 662: Refactor-Durchlauf 22 (edge/remaining typing follow-up) umgesetzt — `infra/github/branchOps.ts::getBranches` liest Branch-Listen jetzt ueber einen sicheren JSON-Array-Reader statt Direkt-Cast, `infra/github/workflows.ts` mappt Workflow-/Run-/Job-Antworten helper-first ueber kleine Record-/Array-Reader statt lokalem `any`-/JSON-Parse-Rauschen, und `supabase/functions/android-keystore-generate/helpers.ts` nutzt fuer den `node-forge`-Loader jetzt einen minimal getypten Runtime-Adapter statt `any`-Import/Callback; API-/Workflow-/Keystore-Vertraege bleiben unveraendert.
- 2026-03-31: Patch 661: Refactor-Durchlauf 21 (logger/github response typing helper-first) umgesetzt — `lib/logger.ts` nutzt jetzt `unknown[]` statt `any[]`, und `infra/github/compare.ts`, `infra/github/user.ts`, `infra/github/secrets.ts` lesen GitHub-Responses ueber die erweiterten `githubResponseHelpers` statt lokaler `: any`-Pfade; der fokussierte Helper-Test deckt den neuen Record-Array-Reader ab, API-/Fehlervertraege bleiben unveraendert.
- 2026-03-31: Patch 660: Refactor-Durchlauf 20 (JSON typing/helper-first) umgesetzt — `lib/diagnostics/templates/jsonUtils.ts` fuehrt jetzt `JsonRecord`, `isJsonRecord` und `getErrorMessage` als engeren Shared-Vertrag; `patchAppJson`, `patchPackageJson` und `patchEasJson` nutzen lokale getypte Nested-Objekte statt impliziter `any`-Caller-Pfade, und fokussierte Regressionen sichern Normalisierung/Fallback-Semantik ohne Template-/Checklist-Vertragsaenderung.
- 2026-03-31: Patch 659: Refactor-Nachzug 19.1 umgesetzt — `screens/AppInfoScreen/hooks/importExportErrorHelpers.ts` erkennt Abbruchpfade jetzt robust ueber deutsches `abgebrochen` und englische Cancel-Varianten (`cancelled`/`canceled`); der fokussierte Helper-Test deckt diese Faelle ab, und `docs/04-risk-hotspots.md` fuehrt den aktuellen `: any`-Reststand fuer den naechsten Follow-up ehrlich weiter.
- 2026-03-31: Patch 658: Refactor-Durchlauf 19 (Import/Export typing helper-first) umgesetzt — `screens/AppInfoScreen/hooks/importExportHelpers.ts` nutzt fuer Export-/Import-/Encrypted-Backup-Catches jetzt `unknown` statt `error: any`; der neue pure Helper `screens/AppInfoScreen/hooks/importExportErrorHelpers.ts` zentralisiert Fehlermeldungs-Fallback und konservative Abbrucherkennung, ohne Import-/Export-Vertragsaenderung.
- 2026-03-31: Patch 657: Refactor-Durchlauf 18 (infra/typing helper-first) umgesetzt — neue `infra/github/githubResponseHelpers.ts` kapselt sichere GitHub-Response-Reads (`readJsonRecordSafe`, Message-/String-/SHA-Helper); `infra/github/branchOps.ts` und `infra/github/repos.ts` nutzen damit keine lokalen `: any`-JSON-Pfade mehr fuer Branch-/Repo-Responses, ohne API-/Workflow-Vertragsaenderung.
- 2026-03-31: Patch 656: Durchlauf-17-Refactor umgesetzt — im Diagnostic-Hotspot wurden Anzeige-Formatter fuer Issue-/Single-/Batch-Fix-Ergebnisdetails in `fixRunnerDisplayHelpers.ts` ausgelagert und `useDiagnosticFixRunner.ts` darauf umgestellt; als kleiner Opportunitaetsblock wurden `infra/github/tokenStore.ts` Catch-Pfade auf `unknown` gehaertet und `lib/diagnostics/templates/jsonUtils.ts` Eingabeparameter von `any` auf `unknown` gezogen, ohne Flow-/Vertragsaenderung.
- 2026-03-31: Patch 655: Durchlauf-16-Refactor umgesetzt — im Build-Hotspot wurde das Status-Emoji/-Label-Mapping in `resolveBuildStatusPresentation(...)` zentralisiert; kein Polling-/Dispatch-/Readiness-Umbau.
- 2026-03-31: Patch 654: Durchlauf-15-Refactor umgesetzt — im Connections-Hotspot wurde der Alert-/Hinweis-Textpfad in `resolveConnectionsAlertNotice(...)` zentralisiert und in Link/Create-Pfaden genutzt; kein Auth-/Dispatch-/Polling-Umbau.
- 2026-03-31: Patch 653: Planungs-Durchlauf fuer sichere helper-first Refactors konkretisiert — fuer die naechsten kleinen Laeufe sind jetzt drei risikoarme Kandidaten mit Reihenfolge fixiert (Connections Alert-Resolver, Build Statuslabel-Mapping, Diagnostic Subtitle-Formatter), jeweils explizit ohne Auth-/Dispatch-/Polling-Umbau.
- 2026-03-31: Patch 652: Durchlauf-14-Refactor umgesetzt — im CI-Lite-Hotspot wurde die Busy-State-Aggregation in `resolveCiLiteBusyState(...)` ausgelagert; Hook-Verhalten bleibt gleich, aber Statuszusammenfuehrung ist zentralisiert und testbarer.
- 2026-03-31: Patch 651: Durchlauf-13-Refactor umgesetzt — im Connections-Hotspot wurde die EAS-Link-Startmessage in `resolveEasLinkWorkflowStartMessage(...)` ausgelagert; Hook-Verhalten bleibt gleich, aber projectId-vs-init-Message ist zentralisiert und testbarer.
- 2026-03-31: Patch 650: Durchlauf-12-Refactor umgesetzt — im CI-Lite-Hotspot wurde die Completion-Error-Textauswahl in `resolveCiLiteCompletionErrorText(...)` ausgelagert; Hook-Verhalten bleibt gleich, aber Workflow-vs-Hydrated-Fallback-Mapping ist zentralisiert und testbarer.
- 2026-03-31: Patch 649: Durchlauf-11-Refactor umgesetzt — im Connections-Hotspot wurde die Link-Selection-Precheck-Entscheidung in `resolveLinkExistingSelectionPrecheck(...)` ausgelagert; Hook-Verhalten bleibt gleich, aber Token/Repo/Branch-Alerts sind zentralisiert und testbarer.
- 2026-03-31: Patch 648: Durchlauf-10-Refactor umgesetzt — im CI-Lite-Hotspot wurde die Lookup-Failure-Label-Auswahl in `resolveCiLiteLookupFailureLabel(...)` ausgelagert; Hook-Verhalten bleibt gleich, aber Chain-vs-Default-Label ist zentralisiert und testbarer.
- 2026-03-31: Patch 647: Durchlauf-9-Refactor umgesetzt — im Connections-Hotspot wurde die Status-Flag-Ableitung in `resolveConnectionsStatusFlags(...)` ausgelagert; Hook-Verhalten bleibt gleich, aber Token-/Edge-/Supabase-/Link-/EAS-Mapping ist zentralisiert und testbarer.
- 2026-03-31: Patch 646: Durchlauf-8-Refactor umgesetzt — im CI-Lite-Hotspot wurde das Hydrated-`StepInfo`-Mapping in `resolveHydratedCiLiteStepInfo(...)` ausgelagert; Hook-Verhalten bleibt gleich, aber die Snapshot->Status-Matrix ist zentralisiert und testbarer.
- 2026-03-31: Patch 645: Durchlauf-7-Refactor umgesetzt — im CI-Lite-Hotspot wurde die Pending-Run-Statuszeile in `resolveCiLitePendingRunMessage(...)` ausgelagert; der Hook-Flow bleibt verhaltensgleich, aber Chain-Waiting-/job_id-Text ist zentralisiert und testbarer.
- 2026-03-31: Patch 644: Durchlauf-6-Refactor umgesetzt — im CI-Lite-Hotspot wurde das Artifact-Request-Mapping in `resolveCiLiteArtifactRequest(...)` ausgelagert; `useCiLiteWorkflow` bleibt verhaltensgleich, aber Artifact-Name/-Pfad fuer CI-Lite vs. Autofix sind zentralisiert und testbarer.
- 2026-03-31: Patch 643: Durchlauf-5-Refactor umgesetzt — im CI-Lite-Hotspot wurde das Lookup-Failure-Mapping in `resolveCiLiteLookupFailureMessage(...)` ausgelagert; `useCiLiteWorkflow` bleibt verhaltensgleich, aber der Branching-Block fuer `ambiguous`/`contract_mismatch`/`timeout` ist zentralisiert und testbarer.
- 2026-03-31: Patch 642: Durchlauf-4-Refactor umgesetzt — im Connections-Hotspot wurde der EAS-Precheck in `resolveEasTestPrecheck(...)` ausgelagert; `testEas` bleibt verhaltensgleich, aber lesbarer/testbarer, und fokussierte Helper-/Screen-Tests sind gruen.
- 2026-03-31: Patch 641: Durchlauf-3-Refactor umgesetzt — CI-Lite-Lookup-Diagnose-Merge als pure Helper-Funktion (`mergeWorkflowRunLookupDiagnosis`) zentralisiert, Inline-Komplexitaet im Hook reduziert und mit fokussierten Helper-Tests abgesichert; keine Flow-/Contract-Aenderung.
- 2026-03-31: Patch 640: Durchlauf-2-Refactor und Review-Fixes umgesetzt — Connections-Hotspot nutzt fuer EAS-Verify nun den pure Helper `resolveEasProjectVerification(...)` statt inline Mapping, mit erweiterten Helper-Tests; Review-Hinweise aus PR 507 nachgezogen (historische TODO-Patch-IDs erhalten, `as any`-Vollscan in Risk-Hotspots auf 341 korrigiert).
- 2026-03-30: Patch 639: Refactor-Durchlauf 1 auf dem Deep-Scan-Stand umgesetzt — zwei kleine pure-logic-Extraktionen in Hotspot-Hooks ohne Flow-Umbau: CI-Lite-Artifact-JSON-Parsing in `useCiLiteWorkflowHelpers.ts` zentralisiert und Diagnostic-Fix-Preview-Entry-Building in `useDiagnosticFixRunnerHelpers.ts` ausgelagert; beide Bereiche mit fokussierten Helper-Tests regressionsgesichert.
- 2026-03-30: Patch 638: vollstaendigen Deep-Scan-Follow-up abgeschlossen — reale Header-Drift zwischen Kern-MDs und restlicher SoT behoben (`docs/INDEX.md`/`docs/TESTING_GUIDE.md`/`docs/FRESH_CHECKOUT_GREEN_PATH.md` jetzt einheitlich), Typing-/Debt-Lage neu inventarisiert (`as any`/`: any` mit Runtime-vs-Test-Trennung) und in `docs/04-risk-hotspots.md` dokumentiert, Deep-Scan-Review um ein zweites evidenzbasiertes Addendum erweitert; keine neuen Features, keine Runtime-/Contract-Aenderung.
- 2026-03-30: Patch 637: PR-506-Doku-/Scope-Drift konsolidiert — Kern-MDs (`docs/INDEX.md`, `docs/TESTING_GUIDE.md`, `docs/FRESH_CHECKOUT_GREEN_PATH.md`) auf Stand Patch 636 gezogen, `docs/reviews/Review.md` im Hauptteil inhaltlich mit TODO-Realitaet synchronisiert (Trust-Follow-up/CS-REST-001 nicht mehr als offen), `docs/04-risk-hotspots.md` um den Patch-636-Testaudit-Hinweis ergaenzt, und `AGENTS.md` minimal, aber verbindlich fuer Doku-/SoT-Abgleich nachgeschaerft (Kern-MD-Pflichtcheck + Abschlussbericht-Pflichtangaben).
- 2026-03-30: Patch 636: finaler Test-Audit-Nachzug fuer Korrektheit/Determinismus — `__tests__/buildReadinessGate.ciLiteFreshness.test.ts` nutzt jetzt eine feste Zeitbasis (`FIXED_NOW`) plus `Date.now`-Spy statt verstreuter Echtzeitaufrufe, wodurch die Freshness-Assertions reproduzierbarer werden; die Deep-Scan-Review wurde um einen expliziten Test-Audit-Abschnitt (Priorisierung, Begründung, konkrete Fixoptionen/Fixbeispiel) erweitert. README/TODO/Patchlog/Patchnote sind auf denselben Stand synchronisiert.
- 2026-03-30: Patch 635: letzter kleiner Follow-up aus dem Helper-Refactor-Block plus Deep-Scan-/Docs-Sync — `useGitHubReposScreen::refreshSyncStatus` behandelt den Precheck-Fall `invalid_repo` jetzt wie einen expliziten Fehlerstatus (Sync-Lampe bleibt nicht mehr stale auf alten Vergleichswerten stehen), `__tests__/useGitHubReposScreenHelpers.test.ts` deckt den `invalid_repo`-Zweig gezielt ab, und die Deep-Scan-Review (`docs/reviews/Review.md`) wurde um einen aktuellen Addendum-Block mit Ampel-Priorisierung (P0/P1/P2), Refactoring-/Dead-Code-Kandidaten inkl. konkreter Datei-/Stellenhinweise und Fixoptionen erweitert. Patch-/TODO-/README-Sync auf denselben Stand gezogen.
- 2026-03-30: Patch 634: PR-504-Review-Regressionsfix fuer `materializeProjectFiles(...)` umgesetzt — `lib/projectMaterializer.ts` prueft Datei-Eintraege jetzt zuerst auf brauchbare Objektkandidaten und ruft `readProjectFileContent(...)` erst danach auf; dadurch werden `null`/primitive/malformed `project.files`-Eintraege aus Storage-/Hydration-Pfaden wieder fail-safe ignoriert statt bei `file.content` zu crashen. Neuer Regressionstest `__tests__/projectMaterializer.failSafe.regression.test.ts` deckt den Crash-/Skip-Vertrag explizit ab; README/INDEX/TODO/Patchlog/Patchnote auf denselben Stand synchronisiert.
- 2026-03-30: Patch 633: weiterer kleiner `as any`-Abbau im Test-Scope — `lib/__tests__/tokenEstimator.test.ts` und `lib/__tests__/validators.test.ts` entfernen lokale Casts auf `any` (Provider-Literale und ZIP-Import-Testdaten nun typkonform); codefokussierter Scan (ohne `docs/**`/`README.md`/`PROJECT_CHECKLOG.md`) sank von 165 auf 150 (`-15`).
- 2026-03-30: Patch 632: Durchlauf 6 schliesst die letzten echten Code-`as any`-Reste ausserhalb von Doku/Test — die zwei verbliebenen UI-Hilfsskripte `scripts/ui/k1w1_ui_polish_templates.sh` und `scripts/ui/apply_ui_polish_fix_dev_toggle_v2.sh` nutzen jetzt keine `as any`-Fragmente mehr in ihren generierten TS-Strings; codefokussierter Scan (ohne `docs/**`/`README.md`) sank von 167 auf 165 (`-2`) (zusätzlich ohne `PROJECT_CHECKLOG.md`).
- 2026-03-30: Patch 631: Durchlauf 5 fuer `as any` mit weiterem UI-/Style-/Interop-Fokus — Glow-/Icon-/Template-/WebView-Casts in `ChatHeaderActions`, `PulseDot`, `CiLiteHeaderButton` (styles + StatusIndicators), `EnhancedBuildScreen`, `WebCodeEditor`, `ChecklistSection` und `templateFiles` entfernt; codefokussierter Scan (ohne `docs/**`/`README.md`) sank von 187 auf 178 (`-9`).
- 2026-03-30: Patch 630: weiterer `as any`-Durchlauf mit kleinem UI-/Interop-Scope — `CustomHeader`, `CustomDrawer`, `FileItem`, `FixRunModal`, `DiffFilesSection`, `WebCodeEditor`, `ChecklistSection` und `templateFiles` entfernen lokale Casts ohne Verhaltensumbau; codefokussierter Scan (ohne `docs/**`/`README.md`) sank von 197 auf 187 (`-10`).
- 2026-03-30: Patch 629: Durchlauf 3 fuer `as any` mit kleinem UI-/Glue-Scope ohne Refactor — `polyfills.ts`, `CredentialsWizardScreen`, `useDiagnosticScreen`, `WorkflowRunDetailModal`, `ApiKeysSection` und `LocalRemoteDiffSection` entfernen verbleibende lokale Casts (`globalThis`, `run`, `PROVIDER_METADATA`, file content access) ohne Verhaltensumbau; codefokussierter Scan (ohne `docs/**`/`README.md`) sank von 208 auf 197 (`-11`).
- 2026-03-30: Patch 628: Durchlauf 2 zum `as any`-Abbau auf aktuellem Stand — verbleibende A-Hotspots in `lib/notificationService.ts` (`Constants as any`), `supabase/functions/github-workflow-logs/index.ts` (`e as any`) und `supabase/functions/create_codesandbox/helpers.ts` (`(err as any).message`) wurden durch enge Unknown-/Record-Narrowings ersetzt; codefokussierter Scan (ohne `docs/**`/`README.md`) sank von 212 auf 208 (`-4`).
- 2026-03-30: Patch 627: erster konservativer `as any`-Abbau auf aktuellem Branch-Stand mit Fokus auf produktive Runtime-/Edge-Helper ohne Vertragsumbau — `k1w1-handler` Request-Parsing nutzt Record-Narrowing statt `body as any`, `android-keystore-generate` ersetzt `supabase as any` in `ensureBucketExists(...)` durch einen engen Query-Typ, Template-/Materializer-Helfer lesen Datei-Content ohne `(f as any)?.content`, und `repos.ts` dedupliziert ohne `(r as any)?.id`; neuer Scanstand 285 (vorher 291, netto -6).
- 2026-03-30: Patch 626: enger Cleanup-/Hardening-Nachzug auf aktuellem Branch-Stand — `lib/secretRedaction.ts` entfernt den verbleibenden Runtime-`as any`-Cast in `replaceAllSafe(...)` (gleiches Verhalten, engerer String-Replace-Typvertrag), `docs/TODO.md` bleibt die einzige aktive Restpunkt-SoT; historische Parallel-Listen wurden spaeter entfernt, `docs/TODO.md` schliesst CS-REST-001 mit dem bereits vorhandenen `testEas`-Busy-Guard-/Feedback-Stand final als erledigt, und `docs/04-risk-hotspots.md` zieht die offene Any-Rangfolge ohne `secretRedaction` nach.
- 2026-03-30: Patch 625: Deep-Scan-P1-Follow-up im engen Scope umgesetzt — neuer zentraler Fresh-Checkout-Green-Path (`docs/FRESH_CHECKOUT_GREEN_PATH.md`) dokumentiert inkl. Voraussetzungen, Pflichtkommandos (`npm ci`, `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`), erwarteter Signale und externem `npm warn Unknown env config "http-proxy"`-Noise; `docs/TESTING_GUIDE.md` auf denselben realen Verifikationspfad aktualisiert (historische/umgebungsfremde Schritte entfernt); `screens/ConnectionsScreen/hooks/useConnectionsScreen.ts::testEas` nutzt jetzt `withBusyGuard` plus explizites Busy-Feedback (`Bitte warten`) statt stillem Return bei Busy-Kollision; `lib/diagnostics/ciAutoFix.ts` reduziert den naechsten Runtime-`as any`-Hotspot via `toErrorMessage(error: unknown, ...)`; fokussierte Invariant-Regession fuer `testEas`-Busy-Semantik ergänzt.
- 2026-03-30: Patch 624: live-vs-repo-Drift fuer den bereits ausgefuehrten P0-RLS-/Policy-Fix kanonisiert — neue Migration `20260330000000_p0_rls_policy_fix_lint_and_native_sync.sql` zieht exakt den Live-SQL-Stand nach: auf `public.lint_jobs` wird die fehlkonfigurierte PUBLIC-Policy `"Service role full access lint_jobs"` entfernt und eine explizite deny-Policy fuer `anon`/`authenticated` gesetzt; auf `public.native_sync_jobs` und `public.native_sync_reports` wird RLS aktiviert und jeweils dieselbe fail-closed deny-Policy gesetzt. Keine weiteren Tabellen/Objekte, keine Produktcode-Aenderung.
- 2026-03-30: Patch 623: berechtigtes Review-Feedback zu Patch 622 am Regressionstest geschlossen — der bisherige Non-ASCII-Fall lag nur in `user_metadata.display_name`, waehrend die fuer den Role-Match relevanten Quellen (`role`, `app_metadata.role`) ASCII blieben; dadurch konnte der alte Decoderpfad (`atob` + direktes `JSON.parse`) weiter gruen bleiben. Der Test nutzt jetzt bewusst einen Unicode-Rollenclaim (`build_ädmin`) im Shared-Auth-Pfad und prueft sowohl `getJwtPayload(req)?.role` als auch den finalen `requireJwtRole(... allowedRoles)`-Match. Damit waere pre-fix ein Mojibake-/Mismatch-Fehler aufgetreten (rot), waehrend der aktuelle UTF-8-Decoder korrekt gruen ist. Kein Produktcode wurde angefasst.
- 2026-03-30: Patch 622: verbleibenden Live-RBAC-Drift im Shared-Auth-JWT-Payload-Decode geschlossen — Root-Cause war ein Encoding-Drift in `decodeJwtPayload(...)`: JSON wurde nach `atob(...)` ohne UTF-8-Decoding geparst. Enthielt der verifizierte JWT in anderen Claims Non-ASCII-Zeichen (z. B. `user_metadata`), konnte der lokale Payload-Parse fehlschlagen; der finale Rollenvergleich fiel dann auf `auth/v1/user.role` (haeufig `authenticated`) zurueck und blockte trotz korrektem `role=build_admin` faelschlich mit `Forbidden ... role is not allowed`. Fix: UTF-8-sicheres Decode (`Uint8Array` + `TextDecoder`) vor `JSON.parse`, plus Regressionstest mit Unicode-Claim. Fail-closed-Vertrag bleibt strikt auf `service_role|build_admin`.
- 2026-03-30: Patch 621: verbleibenden Timing-/Flaky-Testrestpunkt in `lib/__tests__/retryWithBackoff.test.ts` robust geschlossen — Root-Cause war keine fehlerhafte Backoff-Runtime, sondern eine fragile Realzeit-Messung (`Date.now()` + Real-Timer) im Exponential-Backoff-Test. Unter langsamer/lastiger Laufumgebung konnte der erste Retry-Abstand durch Scheduler-/Event-Loop-Drift deutlich >1500ms liegen, obwohl der berechnete Delay weiterhin korrekt 1000ms war. Der Test validiert jetzt deterministisch mit Fake-Timern den fachlichen Vertragskern (kein Retry vor 1000ms, zweiter Retry erst nach weiteren 2000ms), ohne Timeout-Kosmetik und ohne Runtime-Verhaltensaenderung in `retryWithBackoff`.
- 2026-03-29: Patch 620: verbleibenden serverseitigen JWT-/RBAC-Drift im Shared-Auth-Role-Read-Pfad geschlossen — Root-Cause war die falsche Priorisierung `user.role` vor JWT-Claim-Auswertung: bei verifiziertem Operator-JWT (`role=build_admin`) lieferte `GET /auth/v1/user` haeufig dennoch `role: "authenticated"`, wodurch `allowedRoles` (`service_role|build_admin`) faelschlich ablehnten. `requireJwtRole(...)` nutzt jetzt nach erfolgreicher Verifikation primaer den verifizierten JWT-Claim (`role`, danach `app_metadata.role`), faellt erst danach defensiv auf verifizierte User-Felder zurueck, und behaelt fail-closed bei fremden Rollen bei. Nebenbei sind die zwei Edge-Typecheck-Restpunkte in `_shared/auth.ts` final typisiert (`res.json().catch((): unknown => null)`, explizites Union-Narrowing `verified.ok === false`).
- 2026-03-29: Patch 619: strukturierter `as any`-Audit mit priorisiertem Risikoabbau — repo-weites Inventar (260 Treffer) wurde in A/B/C/D klassifiziert; Schwerpunkt auf produktiven Runtime-/Validation-/Config-/Normalizer-/Diagnostics-/Archive-Pfaden statt UI-/Test-/Interop-Aktionismus. Gezielt entfernt/ersetzt wurden die risikoreichsten, klein behebbaren Casts in `lib/validators.ts`, `lib/supabase.ts`, `lib/supabaseEdge.ts`, `lib/normalizer.ts`, `lib/diagnostics/buildPipelineDiagnostics.ts`, `project/services/projectArchiveService.ts`, `screens/ConnectionsScreen/utils/validation.ts` und `config.ts` (Guards + enge Runtime-Types, keine grossen Refactors). Offene Restpunkte bleiben dokumentiert in `docs/04-risk-hotspots.md`; Patch-Doku/TODO/README/Patchlog sind synchron.
- 2026-03-29: Patch 618: strukturierter Hook-Refactoring-Audit ohne Funktionsumbau — repo-weites Hook-Inventar (use*.ts/use*.tsx) nach Groesse/Seiteneffekten/Kopplung klassifiziert und in A/B/C/D priorisiert. Hoechste Risiken: `useGitHubReposScreen`, `useCiLiteWorkflow`, `useConnectionsScreen`, `useCredentialsWizardScreen` sowie `useDiagnosticFixRunner`; sensible, aktuell stabilisierte Vertrags-Hooks (`usePreview`, `useEnhancedBuildScreen`, `useGitHubActionsLogs`) sind explizit als „nicht jetzt gross anfassen“ markiert. `docs/04-risk-hotspots.md` dokumentiert konkrete Schnittkandidaten und eine ehrliche Reihenfolge fuer kommende kleine Refactor-Schritte; README/TODO/Patchlog sind synchronisiert.
- 2026-03-29: Patch 617: offenen Supabase-/Operator-Runbook-Restpunkt aus der unklaren TODO-Zone in einen operativen Vertrag ueberfuehrt — `docs/06-build-readiness.md` enthaelt jetzt die verbindliche Setup-Reihenfolge (Operator-Claim -> scoped Keys -> Secrets -> DB/Storage/Functions -> Repair/Dispatch -> Live-Test) plus Troubleshooting fuer typische Setup-vs-Code-Symptome; `docs/TODO.md` markiert den Punkt als geschlossen und trennt externe Betriebsrestpunkte explizit von Repo-Code. `docs/EDGE_FUNCTIONS_STATUS.md`, `docs/04-risk-hotspots.md`, `docs/01-state-contract.md` und `README.md` wurden konsistent nachgezogen; `scripts/check_workflow_edge_contracts.sh` validiert die neuen Runbook-Anker.
- 2026-03-29: Patch 616: globale Warnungsunterdrueckung in der zentralen App-Initialisierung auf ein bewusstes Minimum reduziert — `App.tsx` entfernt die breiten Ignore-Regeln fuer `Require cycle:` und `VirtualizedLists should never be nested`, damit Architektur-/Listenwarnungen im Dev-Betrieb wieder sichtbar bleiben. Als einzig bewusst akzeptierte globale Regel bleibt die eng begrenzte Reanimated-Dev-Noise-Warnung `Sending \`onAnimatedValueUpdate\` with no listeners registered.` mit expliziter Entfernungsbedingung im Code-Kommentar. Neue Invariant `__tests__/patch616.logboxWarningVisibility.invariants.test.ts` sichert den Vertrag regressionsfest (kein `ignoreAllLogs`, keine rueckkehrenden breiten Ignore-Strings).
- 2026-03-29: Patch 615: Preview-Clientpfad vom stillen Legacy-Admin-Key-Rettungsanker getrennt — `hooks/usePreview.ts` blockiert den normalen Supabase-Preview-Flow jetzt fail-closed, solange kein expliziter Operator-/Maintenance-Schalter gesetzt ist (`EXPO_PUBLIC_ENABLE_LEGACY_PREVIEW_OPERATOR_MODE=true`); dadurch liest der Standard-Nutzerpfad keinen Legacy-Key mehr. Legacy-`save_preview` bleibt nur als bewusst aktivierter Operator-Compat-Pfad erhalten, mit ehrlicher Fehlersemantik im UI. `hooks/previewHelpers.ts` kapselt den Operator-Flag-Guard, `__tests__/usePreview.serverContract.test.tsx` deckt Standard-vs-Operator-Vertrag ab, und neue Invariant `__tests__/patch615.previewLegacyOperatorBoundary.invariants.test.ts` plus `scripts/check_workflow_edge_contracts.sh` sichern die explizite Gate-Semantik regressionsfest.
- 2026-03-29: Patch 614: Build-Screen-Filter-Truthfulness korrigiert — in `useEnhancedBuildScreen` faellt ein aktiver Profilfilter bei null Matches nicht mehr still auf alle Workflow-Runs zurueck (`return list.length > 0 ? list : runs` entfernt). Die Filterlogik liefert im aktiven Filterfall jetzt strikt nur echte Treffer (`[]` bei none), und der Screen zeigt dazu einen ehrlichen Empty State (`Keine Builds für dieses Profil gefunden (Filter: <profil>).`). Neue fokussierte Regressionen (`__tests__/runFilterState.test.ts`) plus Invariant-Update (`__tests__/invariants.selection.test.ts`) sichern den No-Fallback-Vertrag regressionsfest.
- 2026-03-29: Patch 613: Workflow-Dispatch-Semantik strikt von Bootstrap/Repair getrennt — `infra/github/workflows.ts::triggerWorkflow(...)` und `supabase/functions/github-workflow-dispatch` fuehren bei `404` (fehlender Workflow) keine stillen Repo-Mutationen mehr aus (kein `createOrUpdateFile`, kein on-the-fly Workflow-Write/Retry-Bootstrap). Stattdessen liefern beide Pfade klaren `missing_workflow`-Kontext mit fail-closed Hinweis auf explizite Repair-/Provisioning-Flows. Neue Vertrags-Tests/Invariants und `scripts/check_workflow_edge_contracts.sh` sichern regressionsfest, dass normaler Dispatch mutation-free bleibt.
- 2026-03-29: Patch 612: Build-Start fail-closed bei vorgelagertem Repo-Sync/Push-Fehler gehaertet — im Pfad `startBuildJob(...)` fuehrte ein `out_of_sync`-Push-Fehler bisher nur zu Warn-Logging, danach liefen Workflow-Autofix und Build-Dispatch trotzdem weiter. Der Push-Schritt bricht jetzt hart mit klarer Operator-Fehlermeldung ab (`Build abgebrochen: Lokale Aenderungen konnten nicht erfolgreich ins Ziel-Repo gepusht werden.`); dadurch werden nach Push-Fail weder `autoFixCIWorkflows(...)` noch Supabase-Dispatch/Run ausgeloest. Integrations-Tests sichern explizit: Push-Fail => kein Workflow-Autofix, kein Dispatch, kein Teil-Erfolg.
- 2026-03-29: Patch 611: finalen Audit-/Preflight-/Runbook-Schritt fuer den extern provisionierten `build_admin`-Vertrag umgesetzt — operatornahe Caller-/Wizard-Fehltexte nennen jetzt explizit, dass normale eingeloggte Nutzer ohne externen `build_admin`-Claim fail-closed blockiert sind; `runBuildPipelineDiagnostics` und Build-Readiness-Doku enthalten einen klaren Operator-Preflight/Troubleshooting-Runbook-Abschnitt (Voraussetzung, erwarteter Claim, betroffene Flows, Fehlersymptome, Pruefschritte), und Script-/Invariant-Checks sichern die harte externe Provisioning-Aussage regressionsfest ab.
- 2026-03-29: Patch 610: kleiner Merge-Readiness-Integrationssync fuer die PR-Kette 480-484 — `docs/EDGE_FUNCTIONS_STATUS.md` fuehrte nach Patch 609 im Kopf noch `Stand: ... (Patch 608)` trotz bereits dokumentiertem Patch-609-Endvertrag. Header auf `(Patch 609)` korrigiert; keine Code-/Contract-Aenderung. Validation-Stand: `bash scripts/check_workflow_edge_contracts.sh` + `bash scripts/check_edge_helper_visibility.sh` + `bash scripts/check_patch_docs_sync.sh` + `npm run typecheck` + `npm run lint:ci` + `npm run test:silent`.
- 2026-03-29: Patch 609: finalen Cleanup-/Sunset-Schritt fuer den Legacy Edge Admin Key Compat-Vertrag umgesetzt — Audit ueber alle `get/save/deleteLegacyEdgeAdminKey`-Fundstellen klassifiziert produktive, migrationsnahe und dokumentative Reste. Runtime-Pfade fuer Keystore-Readiness sind jetzt scoped-only (`useCredentialsWizardScreen` und `signingKeyGate` ohne Legacy-Fallback), `SecretsSection` zaehlt fehlenden Legacy-Key nicht mehr als lokalen Runtime-Blocker, und verbleibende Legacy-Nutzung bleibt explizit auf eng begrenzte Compat-Faelle (`usePreview`, `k1w1-handler`, Backup/Restore/AppInfo, optionaler Repo-Secret-Sync) markiert. Neue Invariant `patch609.legacyEdgeAdminSunsetBoundaries` plus erweiterter `check_workflow_edge_contracts` sichern den Sunset-Endzustand regressionsfest.
- 2026-03-29: Patch 608: finalen Cleanup der Config-Source-of-Truth fuer `android-keystore-export` abgeschlossen — Audit ueber `supabase/config.toml`, die funktionslokale Export-Config und bestehende Keystore-Checks zeigte keinen akuten Widerspruch (`verify_jwt=true` an beiden Stellen), aber einen verbleibenden Doppel-SoT-Driftpunkt. Die lokale Datei `supabase/functions/android-keystore-export/config.toml` wurde entfernt, sodass fuer `android-keystore-export` jetzt derselbe eindeutige Endvertrag wie fuer `android-keystore-generate`/`android-keystore-status` gilt: einzige fail-closed SoT ist `supabase/config.toml` mit `verify_jwt=true`. `scripts/check_workflow_edge_contracts.sh` sowie die Invariants `__tests__/patch599.keystoreConfigSot.invariants.test.ts` und `__tests__/patch549.keystoreExportJwtRbac.invariants.test.ts` erzwingen den Zustand regressionsfest (keine lokale Keystore-Config mehr fuer export/generate/status). Validation-Stand: fokussierte Contract-Checks/Invariants plus Typecheck/Lint/Tests gruen.
- 2026-03-28: Patch 607: finalen Cleanup der toten CI-bearer-Helperlogik in `supabase/functions/_shared/auth.ts` abgeschlossen — repo-weites Audit ueber Edge-Routen/Helper/Tests/Checks zeigt keinen produktiven CI-bearer-Caller mehr. Deshalb wurden `isScopedCiBearerRequest(...)`, `ciBearerSecretEnv`-/`allowCiBearer`-Branches in `requireScopedEdgeAuth(...)` sowie die generischen Legacy-Helfer `requireServiceRoleBearer(...)`, `requireAdminKeyOrServiceRoleBearer(...)` und `hasServiceRoleSecretConfigured()` entfernt. Alle betroffenen Scoped-Auth-Callsites, Invariants und Contract-Checks sind auf den bereinigten Einzelvertrag gezogen (nur scoped admin-key, optional zusammen mit JWT fuer verify_jwt-Operatorrouten), inklusive Doku-/Patchsync. Validation-Stand: fokussierte Auth-/Contract-Tests plus `check_workflow_edge_contracts`, `check_edge_helper_visibility`, `check_patch_docs_sync`, Typecheck/Lint/Tests gruen.
- 2026-03-28: Patch 606: verbleibenden CI-bearer-Vertrags-/Ops-Drift fuer workflow-/build-/artifact-nahe Edge-Routen konsistent geschlossen — Audit ueber `supabase/config.toml`, die sechs Workflow-/Build-/Artifact-Routen, Auth-Helper und aktuelle Smoke-/Ops-Skripte zeigt: unter `verify_jwt=true` ist ein reiner CI-bearer-Pfad kein eigenstaendiger realer Betriebsweg mehr. Endzustand daher bewusst ohne CI-bearer: die betroffenen Routen laufen jetzt einheitlich mit `allowCiBearer: false`, ohne `ciBearerSecretEnv`/`isScopedCiBearerRequest(...)`, und erzwingen durchgaengig denselben Operator-Vertrag (`Authorization: Bearer <jwt>` + `x-k1w1-admin-key`, role `service_role|build_admin`). UI-/Hint-/Secret-Texte, Contract-Checks (`scripts/check_workflow_edge_contracts.sh`), Invariants (`patch415`, `patch553`) und Patch-Doku sind auf diesen Einzelvertrag nachgezogen; kein Zombie-Dualpfad bleibt konserviert. Validation-Stand: fokussierte Vertrags-Checks/Invariants + Typecheck/Lint/Tests gruen.
- 2026-03-28: Patch 605: Operator-/RBAC-Vertragsrest fuer `build_admin` final geklaert und operativ geschlossen — Repo-Audit ueber Auth/JWT/Edge/Caller/Docs zeigt keinen internen Claim-Mapping-/Grant-Pfad; `build_admin` ist damit ein bewusst externer Supabase-Provisioning-Claim (role/app_metadata.role ausserhalb dieses Repos). App-Caller-/Wizard-Fehltexte nennen den externen Provisioning-Ursprung jetzt explizit, `runBuildPipelineDiagnostics` fuehrt einen sichtbaren Operator-Claim-Precondition-Check, `scripts/check_workflow_edge_contracts.sh` erzwingt die neuen Vertragsformulierungen in Code + Docs, und neuer Invariant-Test `__tests__/patch605.buildAdminProvisioningContract.invariants.test.ts` blockt Rueckdrift. Validation-Stand: fokussierte Operator-Contract-Tests/Checks + Typecheck/Lint/Tests gruen.
- 2026-03-28: Patch 604: RBAC-/JWT-Vertragswiderspruch zwischen App-Callern und gehaerteten workflow-/build-/artifact-/keystore-Routen geschlossen — Endvertrag bleibt fail-closed Operator-only (`service_role|build_admin`) gemaess `WORKFLOW_OPERATOR_ALLOWED_ROLES`/`PRIVILEGED_OPERATOR_ALLOWED_ROLES` in `supabase/functions/_shared/auth.ts`; App-Caller-/Wizard-Fehltexte behaupten nicht mehr `JWT role=authenticated`, relevante Vertrags-Tests nutzen neutrale Operator-JWT-Mocks, neuer Invariant-Test (`__tests__/patch604.workflowOperatorCallerContract.invariants.test.ts`) plus erweiterter `scripts/check_workflow_edge_contracts.sh` blocken Rueckdrift. Validation-Stand: fokussierte RBAC-Tests/Checks + Typecheck/Lint/Tests gruen.
# PROJECT_CHECKLOG
- 2026-03-28: Patch 603: echten Guard-Misconfiguration-Bug in `supabase/functions/test` fail-closed behoben — der scoped Auth-Aufruf setzt jetzt verpflichtend `scope: "test"`, `adminSecretEnv: "K1W1_EDGE_ADMIN_KEY"`, `allowAdmin: true`, `allowCiBearer: false`, sodass die Legacy-Teststub-Route nicht mehr in `500` vorzeitig abbricht, sondern den beabsichtigten Disabled-Vertrag `410 legacy_test_route_disabled` liefert. `scripts/check_workflow_edge_contracts.sh` prueft die konkrete Konfiguration jetzt explizit; Invariants `edgeCorsRequestBound` und `patch603.legacyTestRouteAuthContract` decken den Fix regressionsfest ab.
- 2026-03-28: Patch 602: verbleibenden JWT-/Ref-Vertragsbruch in `scripts/ci-lite-smoke.sh` fail-closed geschlossen — workflow-/build-/artifact-nahe Smoke-Calls senden jetzt verpflichtend `Authorization: Bearer <jwt>` (`K1W1_EDGE_WORKFLOW_JWT`) plus `x-k1w1-admin-key` (`K1W1_EDGE_WORKFLOW_ADMIN_KEY`), `ci-lite-smoke.sh` verlangt einen expliziten `<ref>` ohne stillen `main`-Fallback, `scripts/ci-lite-env-load.sh` validiert den JWT-Vertrag, und `scripts/check_workflow_edge_contracts.sh` + neuer Invariant-Test `__tests__/patch602.ciLiteSmokeJwtRefContract.invariants.test.ts` sichern die Vertragskanten regressionsfest.
- 2026-03-28: Patch 601: letzten Rest-/Compat-Cleanup fuer generische Edge-Admin-Key-Pfade abgeschlossen — App-/Diagnostics-/Wizard-/Secrets-Caller verwenden jetzt explizite Legacy-Compat-Helper (`get/save/deleteLegacyEdgeAdminKey`) statt generischer `get/save/deleteEdgeAdminKey`-Namen; `supabase/functions/test/index.ts` ist als alte Testflaeche nun fail-closed gehaertet (`requireScopedEdgeAuth(... K1W1_EDGE_ADMIN_KEY)`, danach immer `410 legacy_test_route_disabled`); neue Vertrags-Guards decken die Grenzen ab (`__tests__/legacyEdgeAdminCompat.invariants.test.ts`, `__tests__/edgeCorsRequestBound.invariants.test.ts`, `bash scripts/check_workflow_edge_contracts.sh`).
- 2026-03-28: Patch 600: verbleibenden Workflow-Admin-Key-Drift in CI-/Smoke-Skripten fail-closed gehaertet — `scripts/ci-lite-env-load.sh` und `scripts/ci-lite-smoke.sh` lesen fuer workflow-/build-/artifact-nahe Aufrufe jetzt ausschliesslich `K1W1_EDGE_WORKFLOW_ADMIN_KEY` (kein stiller `ADMIN_KEY`-/`K1W1_EDGE_ADMIN_KEY`-Fallback mehr), Fehlermeldungen nennen den dedizierten Vertragswert explizit, und `scripts/check_workflow_edge_contracts.sh` plus neuer Invariant-Test `__tests__/patch600.workflowAdminScriptContract.invariants.test.ts` verhindern Regressionen.
- 2026-03-28: Patch 599: Android-Keystore-Config-Split-Brain fail-closed bereinigt — fuer `android-keystore-generate` und `android-keystore-status` ist `supabase/config.toml` jetzt die einzige `verify_jwt`-SoT (`true`), die widerspruechlichen funktionslokalen Config-Dateien mit `verify_jwt = false` wurden entfernt, und `scripts/check_workflow_edge_contracts.sh` plus neuer Invariant-Test `__tests__/patch599.keystoreConfigSot.invariants.test.ts` verhindern Regressionen (inkl. Guard, dass keine lokalen Keystore-Config-Schattenquellen mehr existieren).
- 2026-03-28: Patch 598: Naechsten Legacy-Guard-/Surface-Reduction-Schritt umgesetzt — generisches `requireAdminKey(...)` in `supabase/functions/_shared/auth.ts` akzeptiert jetzt ausschliesslich `K1W1_EDGE_ADMIN_KEY` (kein stiller `SIGNING_ADMIN_KEY`-Fallback mehr), neuer dedizierter `requireSigningAdminKey(...)` trennt signing-spezifische Nutzung vom Allzweck-Guard, und verbleibende Legacy-Routen (`k1w1-handler`, `create_codesandbox`, `save_preview`, disabled lint/native-sync Stubs) wurden auf expliziten `requireScopedEdgeAuth(... adminSecretEnv: "K1W1_EDGE_ADMIN_KEY", allowCiBearer: false)`-Vertrag gezogen. Betroffene Invariants/Checks (`patch415`, `patch416`, `patch514`, `savePreview.authCorsAndTypecheck`, `check_edge_helper_visibility`, `check_legacy_disabled_edges`, `check_workflow_edge_contracts`) sowie Status-/Risk-/Readiness-/Patch-Doku sind synchronisiert.
- 2026-03-28: Patch 597: Credentials Wizard fuer Android-Keystore-Status/Generate auf den finalen scoped Caller-Vertrag gezogen — lokaler Key-Store im Wizard ist jetzt primaer `androidKeystoreExportAdminKey` (Legacy-`edge_admin_key_v1` nur noch kontrollierte Compat-Lesung), `invokeEdgeJson(...)` sendet fuer `android-keystore-status`/`android-keystore-generate` verpflichtend `Authorization: Bearer <Supabase user JWT>` plus `x-k1w1-admin-key`, und UI-/Status-Texte benennen den dedizierten Keystore-Key statt generischem Edge-Key. Contract-Absicherung: aktualisierte Wizard-/localAdminKey-Tests plus erweiterter `check_workflow_edge_contracts.sh`-Guard fuer JWT+Scoped-Key Header und dedizierte Keystore-Key-APIs.
- 2026-03-28: Patch 596: scoped-key-Vertrag in Settings/Connections/Secrets/Diagnostics konsolidiert — `useConnectionsScreen` + `TokensCard` fuehren getrennte lokale Felder fuer Workflow Admin Key, Android Keystore Export Admin Key und Legacy Edge Admin Key (compat), inklusive getrenntem Laden/Speichern/Validieren. `ConnectionsScreen`-Sync-Summary und `GitHubReposScreen/SecretsSection` entfernen den alten Spiegel-Text (ein lokaler Edge-Key speise alles) und beschreiben scoped Workflow-/Keystore-Vertraege getrennt. `buildPipelineDiagnostics` prueft jetzt lokale Workflow-Readiness (`local.workflowAdminKey`, fail bei fehlendem Wert), lokale Keystore-Readiness (`local.androidKeystoreExportAdminKey`, warn bei fehlendem Wert) und markiert Legacy-Key nur noch als Compat-Signal (`local.legacyEdgeAdminKey`, warn wenn gesetzt). Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_eas_manual_trigger_controls.sh`, `bash scripts/check_eas_production_credentials.sh`, `bash scripts/check_eas_strict_lockfile_policy.sh`, `bash scripts/check_edge_helper_visibility.sh`, `bash scripts/check_k1w1_handler_providers.sh`, `bash scripts/check_supabase_deploy_workflow.sh`, plus fokussierte Jest-Tests fuer Connections/Secrets/Diagnostics.
- 2026-03-28: Patch 595: npm-Warnung `Unknown env config "http-proxy"` gezielt stabilisiert und klassifiziert — reproduzierbar bei `npm run test:silent`, `npm run lint:ci`, `npm run typecheck`; Analyse von `npm config list -l`, `env` und Repo-Scan zeigt: Warnung kommt aus externer Runner-Umgebung (Proxy-Env inkl. `npm_config_http_proxy`/`npm_config_https_proxy`), nicht aus Repo-Dateien (`.npmrc` fehlt, keine entsprechenden Exporte in Scripts/Workflows). Daher kein Produkt-/Tooling-Codefix auf Verdacht; stattdessen klare Dokumentation + neuer Invariant-Test `__tests__/npmProxyWarning.source.invariants.test.ts` gegen deprecated npm proxy env-Injection aus dem Repo. Drift-Checks `check_eas_manual_trigger_controls.sh` und `check_eas_strict_lockfile_policy.sh` bleiben gruen.
- 2026-03-28: Patch 594: verbleibende Workflow-/Template-Drifts in den EAS-Checks gezielt behoben — Ursache war eine veraltete Check-SoT: `check_eas_manual_trigger_controls.sh` und `check_eas_strict_lockfile_policy.sh` prueften Teile des Vertrags direkt in `lib/diagnostics/workflowTemplates.ts`, obwohl die echte Inhalts-SoT fuer EAS/Triggered-Workflows in `shared/workflows/easBuildReleaseWorkflowTemplates.ts` und `shared/workflows/k1w1TriggeredBuildWorkflowTemplate.ts` lebt (Diagnostics ist nur Wiring-/Alias-Layer). Beide Scripts validieren jetzt Live-Workflows + Shared-SoT + Diagnostics-Wiring konsistent; zusaetzlicher Invariant-Test `__tests__/easWorkflowManualLockfileChecks.invariants.test.ts` schuetzt gegen Rueckdrift. Validation-Stand: gezielte Drift-Checks (`check_eas_manual_trigger_controls`, `check_eas_strict_lockfile_policy`, `check_managed_workflows`, `check_workflow_template_drift`, `check_patch_docs_sync`) + `npm run typecheck` + `npm run lint:ci` + `npm run test:silent`.
- 2026-03-28: Patch 593: lokale scoped Admin-Key-Zonen als klaren Primarvertrag nachgeschaerft — `infra/github/tokenStore.ts` migriert den lokalen Workflow-Key kontrolliert einmalig aus `edge_admin_key_v1` (nur wenn `workflow_admin_key_v1` fehlt) und stellt explizite Legacy-Compat-APIs (`get/save/deleteLegacyEdgeAdminKey`) bereit. Backup-/Import-Compat in `lib/appInfoScopedBackup.ts` spiegelt Legacy-`edgeAdminKey` nicht mehr still in Keystore-/Signing-Zonen, sondern nur noch in `workflowAdminKey` + `legacyEdgeAdminKey`; `useAppInfoScreen` und `autoSyncRepoSecrets` nutzen dafuer explizite Legacy-Reads/Writes. Validation-Stand: fokussierte Jest-Tests (`tokenStore.edgeAdminKey`, `appInfoSecureBackup`, `useCiLiteWorkflow.behavior`, `useGitHubActionsLogs.contract`, `buildPollingService`, `buildStartService.readinessContract`) sowie `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_workflow_edge_contracts.sh`.

Kurzlog für den laufenden Stand. Detailhistorie bleibt im Patchlog.

## Zuletzt geprüft / aktualisiert

- 2026-03-28: Patch 592: gezielte Test-/Warning-Stabilisierung fuer driftende Vertragsinvariants nach Hardening 586–591. `__tests__/ciLiteHeaderWorkflow.invariants.test.ts` bindet den Header-Running-Vertrag jetzt an die echte SoT `deriveCiLiteHeaderState(...)` in `useCiLiteWorkflowStatusHelpers.ts` statt an einen veralteten Inline-String aus `useCiLiteWorkflow.ts`. `__tests__/patch403.workflowContracts.invariants.test.ts` prueft CI-/EAS-Vertraege jetzt dort, wo die Template-SoT real lebt (`shared/workflows/managedWorkflowTemplates.ts`, `shared/workflows/easBuildReleaseWorkflowTemplates.ts`) und sichert zusaetzlich die Alias-/Import-Verdrahtung in Infra/Edge/Diagnostics. Ergebnis: bekannte Fremdfails behoben, Vertraege nicht aufgeweicht, Full Checks gruen; verbleibende `npm warn Unknown env config "http-proxy"`-Warnung ist reproduzierbar, aber Umgebungskonfiguration statt Repo-Code.
- 2026-03-28: Patch 591: letzter Hardening-/Bereinigungsblock konsolidiert — `GitHubContext` fuehrt keine konkurrierende persistente SoT mehr fuer Repo/Branch (`k1w1_github_active_repo`/`k1w1_github_active_branch` werden nicht mehr geschrieben und beim Hydrieren bereinigt), `activeRepo`/`activeBranch` sind nur noch vom Projektstand `projectData.linkedRepo`/`linkedBranch` abgeleitet, und Repo-/Branch-Schreibpfade in `useGitHubReposScreen` sowie AppInfo-Secure-Backup-Import laufen fachlich nur noch ueber `setLinkedRepo(...)`. `lib/appInfoScopedBackup.ts` schreibt jetzt `github.linkedRepo`/`github.linkedBranch` und migriert Legacy-Backups mit `github.activeRepo`/`activeBranch` kontrolliert. Fuer durable Rate-Limits bekommt `edge_rate_limit_events` eine echte Retention-Migration (`public.prune_edge_rate_limit_events(interval)` + nightly `pg_cron` Job `prune-edge-rate-limit-events-nightly`). `android-keystore-generate` entfernt den irrefuehrenden branch-Vertrag (kein `branch`-Parsing/-Validation/-Response mehr; Scope bleibt `repo + mode`). Relevante Tests/Checks/Docs wurden synchronisiert (`__tests__/githubContext.mirror.test.tsx`, `__tests__/appInfoSecureBackup.test.ts`, `__tests__/githubBranchRefHardening.contracts.test.ts`, `__tests__/edgeRateLimitRetention.invariants.test.ts`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_edge_rate_limit_retention.sh`, `bash scripts/check_patch_docs_sync.sh`).
- 2026-03-28: Patch 590: verbleibende stille Branch-/Ref-Fallbacks in tieferen Shared-Layern fail-closed entfernt — `infra/github/workflows.ts` verlangt jetzt fuer Dispatch explizit einen non-empty `ref` (`Explicit branch/ref is required.` statt `ref = "main"`), `infra/github/files.ts` entfernt die harten `"main"`-Fallbacks in branch-/ref-gebundenen Helfern (`resolveTargetBranch`, `createOrUpdateFile`, `deleteRepoFile`, `listRepoBlobEntries`), `infra/github/branchOps.ts::getDefaultBranch()` rät ohne Repo-Metadaten nicht mehr auf `"main"` (`Repository default_branch is missing.`), und `supabase/functions/android-keystore-generate/index.ts` entfernt `safeString(body?.branch) || "main"`. Relevante Contracts/Checks/Docs wurden synchronisiert (`__tests__/githubBranchRefHardening.contracts.test.ts`, `__tests__/githubFiles.contracts.test.ts`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_patch_docs_sync.sh`).
- 2026-03-28: Patch 589: Edge-Branch-Validation fuer `trigger-eas-build` fail-closed gehaertet — `validateTriggerBuildRequest(...)` verlangt serverseitig jetzt einen expliziten non-empty `branch` (fehlend/leer/Whitespace -> 400 Validation-Fehler), der Rueckgabevertrag ist fuer diese Route auf `branch: string` verpflichtend, `trigger-eas-build` entfernt die bisherige `branch ?? null`-Toleranz im Route-Eingang/Dispatch-Payload, und `isAllowedRef(...)` akzeptiert leere Refs nicht mehr (`false` statt stilles `true`). Shared-Layer-Default-Branch-/`main`-Fallbacks in tieferen Infra-Pfaden bleiben in diesem Schritt bewusst unangetastet. Validation-Stand: fokussierte Contract-Tests + `bash scripts/check_workflow_edge_contracts.sh` + `bash scripts/check_patch_docs_sync.sh` + `npm run typecheck` + `npm run lint:ci` + `npm run test:silent`.
- 2026-03-28: Patch 588: Keystore-Auth-Hardening fuer `android-keystore-generate` und `android-keystore-status` auf den dedizierten scoped Vertrag gezogen — beide Routen verwenden nun `requireScopedEdgeAuth(...)` mit `adminSecretEnv: "K1W1_EDGE_ANDROID_KEYSTORE_EXPORT_ADMIN_KEY"` (kein generischer `requireAdminKey(...)`-Primärpfad mehr), `allowCiBearer: false`, `allowJwtAuthHeaderWithAdmin: true` und erzwingen danach fail-closed JWT-RBAC nur fuer `service_role|build_admin` via neuem neutralen Shared-Helper `requirePrivilegedOperatorJwtRole(...)`; `android-keystore-export` bleibt als Referenzroute funktional unveraendert. `supabase/config.toml` setzt `verify_jwt=true` gezielt fuer generate/status, relevante Invariants/Contract-Skripte/Docs sind synchronisiert. Validation-Stand: fokussierte Keystore-/Auth-Invariants + `bash scripts/check_workflow_edge_contracts.sh` + `bash scripts/check_edge_helper_visibility.sh` + `bash scripts/check_patch_docs_sync.sh` + `npm run typecheck` + `npm run lint:ci` + `npm run test:silent`.
- 2026-03-28: Patch 587: Secret-Splitting fuer lokale Edge-Admin-Werte abgeschlossen — `infra/github/tokenStore.ts` trennt dedizierte SecureStore-Slots fuer `workflowAdminKey`, `androidKeystoreExportAdminKey`, `legacyEdgeAdminKey` und `signingAdminKey` (Legacy-Helper bleiben explizit kompatibel markiert); `lib/autoSyncRepoSecrets.ts` synchronisiert diese Keys bewusst getrennt auf `K1W1_EDGE_WORKFLOW_ADMIN_KEY`, `K1W1_EDGE_ANDROID_KEYSTORE_EXPORT_ADMIN_KEY`, `K1W1_EDGE_ADMIN_KEY` und `SIGNING_ADMIN_KEY` ohne stilles Spiegeln eines Einzelwerts. Workflow-/Build-/Artifact-Clientcaller (`buildStartService`, `buildPollingService`, `useCiLiteWorkflow`, `useGitHubActionsLogs`) nutzen lokal jetzt den dedizierten Workflow-Key. AppInfo-Secret-Backup/Import und `scripts/signing_secrets.sh` wurden auf die getrennten Secret-Zonen nachgezogen. Validation-Stand: fokussierte Jest-Tests + `bash scripts/check_patch_docs_sync.sh` + `npm run typecheck` + `npm run lint:ci` + `npm run test:silent`.
- 2026-03-28: Patch 586: workflow-/build-Operator-RBAC fail-closed gehaertet — die sechs privilegierten Workflow-/Build-/Artifact-Routen nutzen fuer den Non-CI-Bearer-JWT-Pfad jetzt zentral `requireWorkflowOperatorJwtRole(...)` aus `supabase/functions/_shared/auth.ts` mit `WORKFLOW_OPERATOR_ALLOWED_ROLES = ["service_role", "build_admin"]`; `authenticated` wird dort nicht mehr akzeptiert. Scoped Auth-Config bleibt unveraendert (`allowCiBearer: true`, `ciBearerSecretEnv: "K1W1_EDGE_WORKFLOW_CI_BEARER"`, `allowJwtAuthHeaderWithAdmin: true`, `adminSecretEnv: "K1W1_EDGE_WORKFLOW_ADMIN_KEY"`) und die `if (!usedCiBearer) { ... }`-Semantik bleibt erhalten. Direkt betroffene Contract-Checks/Invariants/Doku wurden synchronisiert. Validation-Stand: `npm run test:silent -- --runInBand __tests__/patch415.edgeAuthGuards.invariants.test.ts __tests__/patch553.workflowDispatchJwtRbac.invariants.test.ts`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_patch_docs_sync.sh`, `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `git diff --check`.
- 2026-03-28: Patch 585: letzter kleiner Doppel-Schritt ohne Hook-/Context-Redesign — `hooks/useChatAIFlow.ts` nutzt fuer den Input-Validation-Guard jetzt den extrahierten reinen Warning-Helper `getInputValidationMessage(...)` aus `hooks/chatAIFlowNoticeHelpers.ts`, und `contexts/GitHubContext.tsx` nutzt fuer linked repo/branch Mirror-Entscheidungen den reinen Guard-Helper `getLinkedMirrorUpdates(...)` aus `contexts/githubContextHelpers.ts`; Async-/Persistenz-/Mirror-Orchestrierung bleibt bewusst unveraendert. Erweiterte fokussierte Regressionen in `__tests__/chatAIFlowNoticeHelpers.test.ts` und `__tests__/githubContextHelpers.test.ts`, zusaetzlich laufen `__tests__/useChatAIFlow.inputValidation.test.tsx` und `__tests__/useChatAIFlow.validatorExplain.invariants.test.ts` weiter gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/chatAIFlowNoticeHelpers.test.ts __tests__/githubContextHelpers.test.ts __tests__/useChatAIFlow.inputValidation.test.tsx __tests__/useChatAIFlow.validatorExplain.invariants.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 584: zwei letzte kleine Pure-Logic-Extracts ohne Hook-Redesign — `hooks/useChatAIFlow.ts` nutzt fuer Builder-Non-OK-Fallback den neuen reinen Notice-Helper `hooks/chatAIFlowNoticeHelpers.ts` (`getBuilderFailureDetails`, `getBuilderFailureMessage`), und `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` nutzt fuer create/rename/delete-success Alerts den erweiterten Notice-Helper `getRepoSuccessNotice(...)` aus `screens/GitHubReposScreen/hooks/githubReposScreenNoticeHelpers.ts`; Async-/Dialog-/Busy-/Request-Orchestrierung bleibt bewusst unveraendert. Neue fokussierte Regression `__tests__/chatAIFlowNoticeHelpers.test.ts`, erweiterte `__tests__/githubReposScreen.noticeHelpers.test.ts`, bestehende `__tests__/useChatAIFlow.summary.regression.test.ts` und `__tests__/githubReposScreen.pullPushSemantics.test.ts` bleiben gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/chatAIFlowNoticeHelpers.test.ts __tests__/githubReposScreen.noticeHelpers.test.ts __tests__/useChatAIFlow.summary.regression.test.ts __tests__/githubReposScreen.pullPushSemantics.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 583: zwei kleine Pure-Logic-Extracts ohne Context-Redesign — `contexts/ProjectContext.tsx` nutzt fuer Datei-Delete/Filter jetzt den neuen reinen Helper `removeProjectFilesByPaths(...)` aus `contexts/projectContextHelpers.ts`, und `contexts/AIContext/index.tsx` nutzt fuer Provider-Auswahl + Default-Mode-Ableitung den neuen reinen Helper `buildProviderSelectionPatch(...)` aus `contexts/AIContext/helpers.ts`; Persistenz-/Rehydration-/Orchestrierungsfluss bleibt bewusst unveraendert. Erweiterte fokussierte Regressionen in `__tests__/projectContext.helpers.test.ts` und `__tests__/aiContext.helpers.test.ts`, bestehende `__tests__/aiContext.persistence.test.tsx` und `__tests__/aiContext.qualityMode.test.ts` bleiben gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/projectContext.helpers.test.ts __tests__/aiContext.helpers.test.ts __tests__/aiContext.persistence.test.tsx __tests__/aiContext.qualityMode.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 582: zwei kleine pure-logic-Extracts ohne Hook-Redesign — `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` nutzt fuer Delete-Confirm-Dialoge jetzt `githubReposScreenDialogHelpers.ts` (`getDeleteRepoConfirmDialog`, `getDeleteBranchConfirmDialog`) statt doppelter Inline-Textbildung, und `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` nutzt fuer die Header-Lampe den neuen pure Helper `deriveCiLiteHeaderState(...)` aus `useCiLiteWorkflowStatusHelpers.ts`; Polling-/Timer-/Dispatch-/Artifact- sowie Dialog-/Busy-Orchestrierung bleiben unveraendert. Neue fokussierte Regressionen: `__tests__/githubReposScreen.dialogHelpers.test.ts`, `__tests__/useCiLiteWorkflowStatusHelpers.test.ts`; bestehende `__tests__/githubReposScreen.pullPushSemantics.test.ts` und `__tests__/useCiLiteWorkflow.behavior.test.tsx` bleiben gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/githubReposScreen.dialogHelpers.test.ts __tests__/useCiLiteWorkflowStatusHelpers.test.ts __tests__/githubReposScreen.pullPushSemantics.test.ts __tests__/useCiLiteWorkflow.behavior.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 581: kleiner Entflechtungsschritt im Regressions-Hotspot `hooks/useChatAIFlow.ts` — das wiederholte Stage-/Result-Mapping fuer Validator-Fallback-Warnings und Source-Summary ist in den neuen lokalen pure Helper `hooks/chatAIFlowStageHelpers.ts` extrahiert (`getValidatorFallbackWarning`, `getSourceSummaryText`); Hook-Orchestrierung und aussen sichtbare Chat-/Stage-Semantik bleiben bewusst unveraendert. Neue fokussierte Regression `__tests__/chatAIFlowStageHelpers.test.ts`, nachgezogener Invariant-Test `__tests__/useChatAIFlow.validatorExplain.invariants.test.ts`, bestehender `__tests__/useChatAIFlow.summary.regression.test.ts` bleibt gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/chatAIFlowStageHelpers.test.ts __tests__/useChatAIFlow.validatorExplain.invariants.test.ts __tests__/useChatAIFlow.summary.regression.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 580: kleiner Entflechtungsschritt im Fix-Runner-Hotspot `screens/DiagnosticScreen/hooks/useDiagnosticFixRunner.ts` — wiederholtes Failure-/Result-Mapping fuer Step-Fehler und Apply-Fehler-Finish-Inputs ist in den neuen lokalen pure Helper `screens/DiagnosticScreen/hooks/fixRunnerResultHelpers.ts` extrahiert (`buildFailedStepPatch`, `buildApplyFailureResult`, `getErrorMessage`, `getFixRuntimeMeta`), der Hook bleibt Orchestrator ohne beabsichtigten Verhaltenswechsel bei `failed`/`blocked`/`pending_recheck`/`patch_applied` sowie `partial`/`localChangeApplied`. Neue fokussierte Regression: `__tests__/fixRunnerResultHelpers.test.ts`; bestehender `__tests__/useDiagnosticFixRunner.fixSemantics.test.tsx` bleibt gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/fixRunnerResultHelpers.test.ts __tests__/useDiagnosticFixRunner.fixSemantics.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 579: zwei kleine pure-logic-Extracts ohne Hook-/Context-Umbau — `useCiLiteWorkflow` nutzt fuer den Artifact-Fetch-Kontext jetzt den neuen Helper `buildArtifactFetchContextKey(...)` (`components/CiLiteHeaderButton/hooks/useCiLiteWorkflowHelpers.ts`) statt inline Eligibility-/Key-Bildung; `GitHubContext` nutzt fuer gespeicherte Recent-Repos-Rehydration den neuen Helper `normalizeStoredRecentRepos(...)` (`contexts/githubContextHelpers.ts`) fuer trim/string-only/filter-empty/dedupe/limit. Polling-/Artifact-Loop-Guard, Rehydration-/Persistenzfluss und oeffentliche APIs bleiben bewusst unveraendert. Neue fokussierte Regressionen: `__tests__/useCiLiteWorkflowHelpers.test.ts`, erweiterte `__tests__/githubContextHelpers.test.ts`; bestehende `__tests__/useCiLiteWorkflow.behavior.test.tsx` und `__tests__/githubContext.mirror.test.tsx` bleiben gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/useCiLiteWorkflowHelpers.test.ts __tests__/githubContextHelpers.test.ts __tests__/useCiLiteWorkflow.behavior.test.tsx __tests__/githubContext.mirror.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 578: kleiner Entflechtungsschritt im userkritischen Hook-Hotspot `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` — die bisher inline verteilte Result-/Notice-/Success-Textbildung fuer EAS-Link-Write-Outcomes und Secrets-Sync-Results ist als neue pure Helper-Datei `screens/GitHubReposScreen/hooks/githubReposScreenNoticeHelpers.ts` extrahiert (`getEasLinkWriteNotice`, `getSecretsSyncNotice`); Hook-Orchestrierung fuer Repo-Aktionen, Async-/Busy-/Dialog-Flow bleibt unveraendert. Neue fokussierte Regression `__tests__/githubReposScreen.noticeHelpers.test.ts`; bestehende Pull/Push-/EAS-Race-Regressionen bleiben gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/githubReposScreen.noticeHelpers.test.ts __tests__/githubReposScreen.pullPushSemantics.test.ts __tests__/githubReposScreen.easLinkStatusRace.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 577: kleiner Entflechtungsschritt im zentralen GitHub-Zustands-Hotspot `contexts/GitHubContext.tsx` — die doppelte Recent-Repo-Update-Logik (`dedupe + move-to-front + limit`) ist als pure Helper `mergeRecentRepo(...)` nach `contexts/githubContextHelpers.ts` extrahiert, und die wiederholte Linked-Repo/Branch-Trim-Normalisierung laeuft ueber `normalizeLinkedGitHubValue(...)`; Context-Orchestrierung fuer Rehydration/Persistenz/Mirror bleibt unveraendert. Neue fokussierte Regression `__tests__/githubContextHelpers.test.ts`, bestehender `__tests__/githubContext.mirror.test.tsx` bleibt gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/githubContextHelpers.test.ts __tests__/githubContext.mirror.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 576: kleiner Entflechtungsschritt im userkritischen Hook-Hotspot `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` — der inline Error-/Fallback-Block wurde als pure Helper `extractErrorMessage(...)` + `getErrorMessage(...)` nach `screens/GitHubReposScreen/hooks/githubReposScreenErrorHelpers.ts` extrahiert; Hook-Orchestrierung fuer Repo-Aktionen, Busy-/Dialog-/Async-Flow bleibt unveraendert. Neue fokussierte Regression `__tests__/githubReposScreen.errorHelpers.test.ts`, bestehende GitHubReposScreen-Regressionen fuer Pull/Push und EAS-Status-Races bleiben gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/githubReposScreen.errorHelpers.test.ts __tests__/githubReposScreen.easLinkStatusRace.test.tsx __tests__/githubReposScreen.pullPushSemantics.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 575: kleiner Entflechtungsschritt im CI-Lite-Hook-Hotspot `useCiLiteWorkflow` — pure Artifact-/Notice-Textbildung (`sanitizeArtifactDetail`, `getArtifactUiMessage`) in den neuen lokalen Helper `components/CiLiteHeaderButton/hooks/ciLiteWorkflowNoticeHelpers.ts` extrahiert, ohne Polling-/Timer-/Dispatch-/Artifact-Flow-Umbau; neue Helper-Regressionen plus bestehende `useCiLiteWorkflow`-Behavior-Tests sichern Message-/Detail-/Redaction-/Truncation-Semantik gegen Drift ab. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/ciLiteWorkflowNoticeHelpers.test.ts __tests__/useCiLiteWorkflow.behavior.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 574: kleiner Entflechtungsschritt im verbleibenden AIContext-Hotspot — `contexts/AIContext/index.tsx` nutzt fuer API-Key-Rehydration (SecureStore-vs.-Legacy-Migration) jetzt den neuen pure Helper `resolveRehydratedApiKeys(...)`, und `contexts/AIContext/helpers.ts` zentralisiert die wiederholte Key-Listen-Normalisierung ueber `normalizeApiKeys(...)` plus `hasAnyApiKeys(...)`; beabsichtigte Persistenz-/Fallback-Semantik bleibt unveraendert (SecureStore bleibt autoritativ, Legacy-Migration nur bei komplett leerem SecureStore). Neue fokussierte Regression `__tests__/aiContext.helpers.test.ts`, bestehende `__tests__/aiContext.persistence.test.tsx` und `__tests__/aiContext.qualityMode.test.ts` bleiben gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/aiContext.helpers.test.ts __tests__/aiContext.persistence.test.tsx __tests__/aiContext.qualityMode.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 573: kleiner Entflechtungsschritt im Regressions-Hotspot `hooks/useChatAIFlow.ts` — die gemischte Builder-/Validator-Result-Normalisierung inkl. Builder-Fallback-Fehlermapping wurde in den neuen lokalen pure Helper `hooks/chatAIFlowResultHelpers.ts` extrahiert (`normalizeResultFiles`, `readBuilderFilesOrThrow`); Hook-Orchestrierung und sichtbare Chat-/Status-/Error-Semantik bleiben bewusst unveraendert. Neue fokussierte Regression `__tests__/chatAIFlowResultHelpers.test.ts` deckt den extrahierten Vertrag inkl. fail-safe Preview-/Fallback-Fehlern ab, bestehende Invariants `__tests__/useChatAIFlow.validatorExplain.invariants.test.ts` und `__tests__/useChatAIFlow.inputValidation.test.tsx` bleiben gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/chatAIFlowResultHelpers.test.ts __tests__/useChatAIFlow.validatorExplain.invariants.test.ts __tests__/useChatAIFlow.inputValidation.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 572: kleiner, risikoarmer Entflechtungsschritt im `ProjectContext`-Hotspot — Rehydration-/Fallback-Normalisierung (`files`, `chatHistory`, `preferredPreviewMode`) und der wiederholte Basisaufbau neuer Projektobjekte sind jetzt als lokale pure Helper in `contexts/projectContextHelpers.ts` extrahiert (`normalizeLoadedProjectData`, `buildProjectForCreation`); `contexts/ProjectContext.tsx` bleibt Orchestrator ohne beabsichtigten Verhaltenswechsel bei Persistenz/Storage/lastModified/Chat-History. Neue fokussierte Regressionen in `__tests__/projectContext.helpers.test.ts`, bestehender `__tests__/projectContext.createNewProject.regression.test.tsx` weiterhin gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/projectContext.helpers.test.ts`, `npm run test:silent -- --runInBand __tests__/projectContext.createNewProject.regression.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 571: kleiner Entflechtungsschritt im verbleibenden Fix-Runner-Hotspot — `screens/DiagnosticScreen/hooks/useDiagnosticFixRunner.ts` nutzt fuer Step-Plan-Aufbau (Issue/Single/Batch) und wiederholte Step-Status-Updates jetzt den neuen lokalen pure Helper `screens/DiagnosticScreen/hooks/fixRunnerHelpers.ts` statt mehrfacher Inline-Bloecke; keine beabsichtigte Flow-/Semantikaenderung bei Statuswerten (`patch_applied`, `pending_recheck`, `failed`, `blocked`), Toast-Texten oder Modal-Abfolge. Neue fokussierte Regressionen (`__tests__/fixRunnerHelpers.test.ts`) sichern Plan-/Status-Vertrag, bestehender Hook-Semantiktest bleibt gruen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/fixRunnerHelpers.test.ts __tests__/useDiagnosticFixRunner.fixSemantics.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 570: zwei naechste kleine Typ-/Fehlervertrags-Restpunkte gezielt gehaertet — `screens/AppInfoScreen/hooks/useAppInfoScreen.ts` entfernt den weichen Root-Cast `projectData as any`, normalisiert Datei-/Asset-Zugriffe ueber lokale Guards (`toProjectFiles`, `isRecord`) und nutzt in Save-/Import-/Export-/Backup-Catches `unknown` plus `getErrorMessage(...)`/`isAbortLikeError(...)` statt unsicherer `error.message`-Direktzugriffe. `components/CiLiteHeaderButton/hooks/useCiLitePatch.ts` parst Patch-JSON jetzt via `parsed: unknown`, haertet Catch-Pfade mit lokalem `getErrorMessage(...)` und entfernt riskante `any`-Parse-/Catch-Ketten bei unveraenderter Patch-/Alert-Fallback-Semantik. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/ciLitePatch.invariants.test.ts __tests__/patch570.typeContracts.invariants.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 569: zwei kleine Typ-/Payload-Hotspots gezielt gehaertet — `components/ciLite/ciLiteUtils.ts` nutzt in `findWorkflowRunByJobId(...)` und `normalizePreflightPatch(...)` jetzt kleine Run-/Record-Guards (`unknown` + Narrowing) statt riskanter `any`-Objektzugriffe, bleibt aber im Matching-/Fallback-Vertrag unveraendert. `lib/appInfoBackup.ts` liest Backup-Importdaten (`config`, `apiKeys`, Provider, mode/quality/agentEnabled) ueber enge Helper statt weicher `as any`-Ketten und baut `validateApiBackupJson(...)` explizit ohne Blind-Cast auf; toleranter Import und bestehende Fallback-Semantik bleiben erhalten. Neue Regressionen sichern Legacy-Top-Level-Fallback + fail-safe Patch-Normalisierung sowie robuste Backup-Fallbacks bei unvollstaendigen/fremden Payloads. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/ciLiteStatus.test.ts __tests__/ciLiteArtifactParsing.test.ts __tests__/appInfoBackupPrivacy.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 568: zwei riskante Typ-/Fehlervertragsgrenzen gezielt gehaertet — `infra/github/files.ts` ersetzt unscharfe `any`-GitHub-Response-/Error-Payloadzugriffe durch kleine klare API-Typen plus `unknown`-sichere JSON-/Message-Guards (`readJsonSafe`, `pickGitHubMessage`), und `screens/DiagnosticScreen/hooks/useDiagnosticFixRunner.ts` haertet die zentralen Runner-Step-Catches auf `unknown` mit lokalen Error-/Meta-Narrowing-Helpern (`getErrorMessage`, `getFixRuntimeMeta`) ohne Flow-Umbau. Zusätzliche fokussierte Regressionen sichern fail-safe Unknown-Errors sowie unvollstaendige GitHub-Responses. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/githubFiles.contracts.test.ts`, `npm run test:silent -- --runInBand __tests__/useDiagnosticFixRunner.fixSemantics.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 567: riskanteste verbleibende `any`-Fehlervertraege in zwei Hotspots minimal gehaertet — `screens/GitHubReposScreen/hooks/useGitHubReposScreen.ts` und `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` nutzen in den zentralen Netzwerk-/Action-Catches jetzt `unknown` + lokale `getErrorMessage(...)`-Guards statt `any`-Property-Zugriffen. Kein Flow-/API-Vertragswechsel; gleiche UI-Fehlersemantik mit sicherem Fallback. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/githubReposScreen.easLinkStatusRace.test.tsx`, `npm run test:silent -- --runInBand __tests__/useCiLiteWorkflow.behavior.test.tsx`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 566: verbleibenden Triggered-Build-Template-Driftblock reduziert — neue Shared-SoT `shared/workflows/k1w1TriggeredBuildWorkflowTemplate.ts` enthaelt `WORKFLOW_K1W1_TRIGGERED_BUILD_TEMPLATE` als gemeinsame Stringquelle fuer den Live-Workflow `.github/workflows/k1w1-triggered-build.yml`; `lib/diagnostics/workflowTemplates.ts` exportiert `WORKFLOW_K1W1_TRIGGERED_BUILD` nur noch als Alias auf die Shared-Quelle statt eigener Inline-Kopie. Drift-/Managed-Checks (`scripts/check_workflow_template_drift.sh`, `scripts/check_managed_workflows.sh`) pruefen jetzt Shared==Live plus Diagnostics-Alias explizit auch fuer Triggered Build; fokussierte Invariants in `__tests__/patch414.workflowRefSot.invariants.test.ts` und `__tests__/patch417.ciWorkflowRefSot.invariants.test.ts` sind nachgezogen. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patch414.workflowRefSot.invariants.test.ts`, `npm run test:silent -- --runInBand __tests__/patch417.ciWorkflowRefSot.invariants.test.ts`, `npm run test:silent -- --runInBand __tests__/invariants.strings.test.ts`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_managed_workflows.sh`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 565: naechsten grossen EAS-/Release-Build-Template-Driftblock reduziert — neue Shared-SoT `shared/workflows/easBuildReleaseWorkflowTemplates.ts` enthaelt `WORKFLOW_EAS_BUILD_TEMPLATE` und `WORKFLOW_RELEASE_BUILD_TEMPLATE` als gemeinsame Stringquelle fuer Live-Workflows und Diagnostics-Alias (`lib/diagnostics/workflowTemplates.ts` exportiert beide nur noch ueber Shared-Imports statt eigener Kopie). Drift-/Managed-Checks (`scripts/check_workflow_template_drift.sh`, `scripts/check_managed_workflows.sh`) validieren jetzt Shared==Live plus Diagnostics-Alias explizit; fokussierter Invariant-SoT-Abgleich in `__tests__/patch414.workflowRefSot.invariants.test.ts` ist nachgezogen. Bewusst unveraendert bleibt `WORKFLOW_K1W1_TRIGGERED_BUILD` weiterhin lokal in Diagnostics. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patch414.workflowRefSot.invariants.test.ts`, `npm run test:silent -- --runInBand __tests__/invariants.strings.test.ts`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_managed_workflows.sh`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 564: naechsten Workflow-/Template-Fragmentierungsblock fuer EAS Link reduziert — `shared/workflows/easLinkWorkflowTemplate.ts` ist jetzt die gemeinsame string-SoT fuer den Diagnostics-EAS-Link-Export (`WORKFLOW_EAS_LINK` wird in `lib/diagnostics/workflowTemplates.ts` nur noch auf die Shared-Quelle verdrahtet), waehrend Live-Workflow `.github/workflows/eas-link.yml` und `templates/expo-sdk54-base.json` bewusst als getrennte Auslieferungsartefakte bestehen bleiben. Drift-/Managed-Guards pruefen den echten Endvertrag jetzt gegen die neue SoT (`check_workflow_template_drift.sh`, `check_managed_workflows.sh`), inkl. fokussierter Invariant-Anpassung in `__tests__/patch417.ciWorkflowRefSot.invariants.test.ts`. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patch417.ciWorkflowRefSot.invariants.test.ts`, `npm run test:silent -- --runInBand __tests__/invariants.strings.test.ts`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_managed_workflows.sh`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 563: Managed-Workflow-/Dispatch-Drift-Risiko fuer CI Lite/Autofix reduziert — `shared/workflows/managedWorkflowTemplates.ts` ist jetzt die gemeinsame SoT fuer die beiden managed CI-Lite-Workflows; `infra/github/workflowTemplates.ts` und `supabase/functions/github-workflow-dispatch/index.ts` nutzen nur noch diesen Shared-Export statt doppelter Inline-Template-Bloecke. Die Drift-/Managed-Workflow-Checks (`check_workflow_template_drift.sh`, `check_managed_workflows.sh`) und fokussierte Invariants (`patch399`, `patch414`) sind auf den Shared-SoT-Pfad nachgezogen; kein beabsichtigter Dispatch-/Bootstrap-/Retry-Verhaltenswechsel. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/patch399.managedWorkflowDrift.invariants.test.ts`, `npm run test:silent -- --runInBand __tests__/patch414.workflowRefSot.invariants.test.ts`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_workflow_edge_contracts.sh`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 562: Preview-Vertrag fail-closed nachgeschaerft — `hooks/usePreview.ts` faellt im normalen Supabase-Produktpfad bei Remote-/Admin-Key-/Payload-Problemen nicht mehr still auf lokalen HTML-/Eval-Fallback zurueck; lokaler Fallback bleibt nur im expliziten `preferredPreviewMode === "local"` erreichbar, und die stille Promotion `setPreferredPreviewMode("local")` wurde entfernt. `hooks/previewHelpers.ts`, `lib/sandpackBuilder.ts` und `screens/PreviewScreen/hooks/usePreviewScreen.ts` sprechen denselben Vertrag (lokaler `new Function(...)`-Pfad = expliziter Dev-/Best-Effort). Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/usePreview.serverContract.test.tsx __tests__/previewHelpers.test.ts __tests__/previewStatusBar.statusText.test.ts`, `npm run test:silent`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 561: Auth-Vertrag/Truthfulness final konsolidiert — `requireJwtRole(...)` bleibt fail-closed und trennt jetzt ehrlich `401` (ungueltig/nicht verifizierbar) von `500` (Server-Misconfig fuer JWT-Verifikation), `android-keystore-generate` bleibt bewusst admin-only (plus durable/local rate-limit, ohne JWT-Rollencheck), und workflow-/build-/artifact-Routen fuehren den scoped Admin+CI-Bearer-Dualpfad wieder explizit mit JWT-Rollencheck nur auf dem Non-CI-Bearer-Pfad. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/auth.failClosedAndDurableRateLimit.test.ts __tests__/patch415.edgeAuthGuards.invariants.test.ts __tests__/patch553.workflowDispatchJwtRbac.invariants.test.ts`, `npm run test:silent`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 560: Prompt-/Validator-Kontext ehrlich nachgeschaerft — `lib/promptEngine.ts` und `lib/aiContextBudget.ts` erweitern `collectFocusTerms(...)` um kleine englische Stopwords, damit einfache englische Requests weniger Rauschbegriffe in die Dateirelevanz tragen; `buildValidatorMessages(...)` enthaelt jetzt zusaetzlich ein Vollpfad-Manifest aller AI-Zielpfade, sodass der Validator auch bei budgetiertem JSON-Entwurf keine vorgeschlagenen Zielpfade still verliert (Inhalte bleiben bewusst budgetiert). Regressionen: `__tests__/promptEngine.contextPriority.test.ts` (English-stopword-Relevanz + Vollpfad-Manifest bei Budget-Trim), dazu `lib/__tests__/aiContextBudget.test.ts` und `__tests__/aiFlowPrivacyContract.test.ts`. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand __tests__/promptEngine.contextPriority.test.ts lib/__tests__/aiContextBudget.test.ts __tests__/aiFlowPrivacyContract.test.ts`, `npm run test:silent`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 559: ZIP-Import-/Persistence-Block minimal und ehrlich gehaertet — `infra/storage/persistenceHelpers.ts` nutzt fuer `readDirectoryRecursive(...)` jetzt einen rekursionsweiten gemeinsamen Dateizaehler, sodass `MAX_FILES_IN_ZIP` global ueber verschachtelte Verzeichnisse erzwungen wird; Ueberlauf oder uebersize Dateien brechen den Import frueh mit klarer Fehlermeldung ab statt still zu skippen. `infra/storage/projectPersistence.ts` wurde auf strict all-or-nothing gezogen (kein widerspruechlicher Warn-/Skip-Pfad mehr) und hat jetzt zusaetzlich einen kleinen Pre-unzip-Guard (leere/unlesbare ZIP-Groesse oder >25MB werden vor `unzip(...)` abgelehnt; ehrlich begrenzt auf Archivgroesse, keine Entry-Inspektion vor Entpacken). `lib/validators.ts` meldet invalid/leer konsistent als Gesamtfehler (`ZIP enthält ungültige Dateien (strict all-or-nothing)` / `ZIP enthält keine Dateien`). Regressionen: `lib/__tests__/validators.test.ts`, `__tests__/persistenceHelpers.readDirectoryRecursive.test.ts`, `__tests__/projectPersistence.zipImportContract.test.ts`. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent -- --runInBand lib/__tests__/validators.test.ts __tests__/persistenceHelpers.readDirectoryRecursive.test.ts __tests__/projectPersistence.zipImportContract.test.ts`, `npm run test:silent` (1 bekannter Timeout-Fail in `__tests__/localRemoteDiffSection.truthfulness.test.tsx`), `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-28: Patch 558: offene CI-Lite-Restpunkte in Artifact-Notice + Run-Lookup minimal geschlossen und nachgeschaerft — `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` ergaenzt Artifact-Hinweise um einen gekuerzten/sanitized Detailteil aus `artifactError` (Redaction fuer Bearer/Admin-Key/gh*-Token plus `github_pat_...`, begrenzte Laenge), sodass Workflow-/Run-Problem und Artifact-/Nachzug-Problem im Modal weiterhin getrennt und ehrlicher sichtbar bleiben. Das starre 2.5s-Intervall fuer Dispatch-/Chain-Run-Lookup bleibt durch lokalen Timeout-Backoff (`1200 -> 1800 -> 2600 -> 3500 -> 4500ms`) ersetzt und hat jetzt einen Generation-Guard gegen Stop/Unmount-Reschedule-Races; zusaetzlich verhindert ein Artifact-Kontext-Guard Refetch-Loops fuer denselben failed completed-run Kontext. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `npm run test:silent -- --runInBand __tests__/useCiLiteWorkflow.behavior.test.tsx __tests__/ciLiteHeaderButton.behavior.test.tsx __tests__/ciLiteHeaderWorkflow.invariants.test.ts`, `git diff --check`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-27: Patch 556: verbleibenden Workflow-Restblock `github-run-artifact-json` auf den kontrollierten JWT-/RBAC-Vertrag gehoben — Route laeuft jetzt mit `verify_jwt=true`, `requireScopedEdgeAuth` ohne CI-Bearer (`allowCiBearer: false`, `allowJwtAuthHeaderWithAdmin: true`) und serverseitigem JWT-Claim-Guard (`role in [service_role, authenticated]`). Der direkte Artifact-Caller in `useCiLiteWorkflow` sendet nun wie Runs/Logs/Dispatch JWT + `x-k1w1-admin-key`, und `useGitHubActionsLogs` blockiert Runs/Logs lokal fail-fast bei fehlendem Admin-Key statt optionale Header zu senden. Validation-Stand: `npm run typecheck`, `npm run edge:check`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_legacy_disabled_edges.sh`, `git diff --check`.
- 2026-03-27: Patch 555: naechsten kontrollierten JWT-/RBAC-Rollout fuer Build-Start/-Status umgesetzt — `trigger-eas-build` und `check-eas-build` laufen jetzt mit `verify_jwt=true`, serverseitigem JWT-Claim-Guard auf `role in [service_role, authenticated]` und scoped Workflow-Admin-Key als zweitem Gate (`x-k1w1-admin-key` / `K1W1_EDGE_WORKFLOW_ADMIN_KEY`), waehrend der route-spezifische CI-Bearer fuer genau diese zwei Routen deaktiviert wurde. Die App-Caller `startBuildJob` und `pollBuildStatusOnce` senden jetzt denselben JWT+Admin-Key-Vertrag und blockieren klar bei fehlendem User-JWT statt stiller Degradierung. Validation-Stand: `npm run typecheck`, `npm run edge:check`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_patch_docs_sync.sh`, `bash scripts/check_workflow_template_drift.sh`, `bash scripts/check_managed_workflows.sh`, `bash scripts/check_legacy_disabled_edges.sh`.
- 2026-03-27: Patch 554: den naechsten kontrollierten JWT-/RBAC-Rollout direkt neben dem Dispatch-Pilot fuer die Read-/Support-Routen umgesetzt — `github-workflow-runs` und `github-workflow-logs` laufen jetzt mit `verify_jwt=true`, serverseitigem JWT-Claim-Guard auf `role in [service_role, authenticated]` und scoped Workflow-Admin-Key als zweitem Gate (`x-k1w1-admin-key` / `K1W1_EDGE_WORKFLOW_ADMIN_KEY`), waehrend der route-spezifische CI-Bearer auf genau diesen beiden Routen deaktiviert wurde. Die Caller `useCiLiteWorkflow` und `useGitHubActionsLogs` senden fuer Run-/Log-Lookups jetzt JWT + Admin-Key im selben kontrollierten Vertrag wie Dispatch. Validation-Stand: `npm run typecheck`, `npm run edge:check`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_workflow_edge_contracts.sh`, `bash scripts/check_patch_docs_sync.sh`.
- 2026-03-27: Patch 553: naechsten kontrollierten JWT-/RBAC-Rollout auf `github-workflow-dispatch` umgesetzt und im Folge-Schritt verschaerft — Route laeuft mit `verify_jwt=true`, serverseitigem JWT-Claim-Guard auf `role in [service_role, authenticated]` (kein `anon`) und scoped Workflow-Admin-Key als zweitem Gate (`x-k1w1-admin-key` / `K1W1_EDGE_WORKFLOW_ADMIN_KEY`), waehrend der fruehere route-spezifische CI-Bearer fuer diesen Dispatch-Pfad bewusst deaktiviert bleibt. Der App-Caller (`useCiLiteWorkflow`) sendet fuer Dispatch jetzt einen echten Supabase-Session-JWT (`Authorization: Bearer <user-jwt>`, `role=authenticated`) plus Admin-Key; ohne gueltigen User-Login blockiert der Dispatch klar. Validation-Stand: `npm run typecheck`, `npm run edge:check`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_workflow_edge_contracts.sh`.
- 2026-03-27: Patch 552: gezielten JWT-/RBAC-Pilot fuer High-Impact-Edge umgesetzt — `android-keystore-export` ist jetzt auf `verify_jwt=true` plus serverseitigen `role=service_role`-Claim-Guard gehoben und behaelt den scoped Admin-Key-Gate als klaren zweiten Route-Check; dazu wurden die betroffenen Caller (`eas-build.yml`, `release-build.yml`) fuer den Keystore-Export auf `Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}` synchronisiert. `github-workflow-dispatch` bleibt bewusst secret-basiert, um den bestehenden App-/CI-Caller-Vertrag nicht blind zu brechen. Validation-Stand: `npm run typecheck`, `npm run edge:check`, `npm run lint:ci`, `npm run test:silent`.
- 2026-03-25: Patch 551: verbleibende Doku-/Patchlog-Drift nach den Modell-/Runtime-Merges minimal bereinigt — veraltete Verweise auf den zwischenzeitlichen `gpt-5-mini`/`gpt-4.1-nano`-Katalogstand korrigiert, den aktuellen Default-Stand (`gemini-3.1-flash-lite`/`gemini-3.1-pro`) in den Summary-Dokumenten klargezogen und `patch_429.md` als historisch ueberholt markiert, damit keine pre-merge Modellwahrheit mehr als aktueller Stand gelesen wird. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `git diff --check`.
- 2026-03-23: Patch 549: Modellkatalog-/Default-Sync aus dem damaligen Branch-Stand dokumentiert; die spaeteren Merges haben den sichtbaren Katalog erneut konsolidiert. Fuer den aktuellen Stand sind die aktiven IDs in `contexts/AIContext/models.ts` (z.B. OpenAI `gpt-5.4*`, Gemini `gemini-3.1-*`) und `shared/ai/providerDefaults.ts` massgeblich. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `git diff --check`.
- 2026-03-23: Patch 548: KI-/CI-Lite-Runtime-Vertrag auf den realen Serverstand gezogen — `contexts/AIContext/models.ts` und `supabase/functions/k1w1-handler/helpers.ts` verwenden wieder denselben Satz aktuell unterstuetzter Default-Modelle, `lib/orchestrator/index.ts` faengt `provider_env_missing` bzw. Modell-404/serverseitig fehlende Modelle ehrlich ueber same-provider- bzw. cross-provider-Fallbacks ab und meldet diesen Lauf inkl. Abort-Race-Fix sichtbar an den Chat zurueck, und `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` sowie die verwalteten Workflow-Templates verlangen fuer manuelle CI-Lite-Dispatches wieder strikt `ref` + `job_id` statt stiller Legacy-Fallback-Bindung; Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, dazu fokussiert `npm run test:silent -- --runInBand lib/__tests__/orchestrator.test.ts` und `npm run test:silent -- --runInBand __tests__/useCiLiteWorkflow.behavior.test.tsx`.
- 2026-03-23: Patch 547: den wieder aufgetretenen Default-Jest-Timeout in `__tests__/localRemoteDiffSection.truthfulness.test.tsx` minimal-konservativ geschlossen — der Context-Switch-Helfer wartet jetzt nicht mehr nur auf das auch ohne echten Reset oft schon sichtbare `Push (0)`, sondern auf den neuen Repo-/Branch-Labelzustand plus wieder freigegebenen Refresh-Button, bevor der zweite Refresh fuer den neuen Kontext ausgeloest wird; damit wird der stale alte Request nicht mehr mit einem zu fruehen zweiten Refresh verwechselt. Nebenbei dokumentieren README/Patchlog/Checklog den bereits gelandeten Modellkatalog-/Settings-Metadaten-Stand wieder konsistent. Validation-Stand: fokussierter Run `npm run test:silent -- --runInBand __tests__/localRemoteDiffSection.truthfulness.test.tsx`, dazu `npm run typecheck`, `npm run lint:ci`, `npm run test:silent`, `bash scripts/check_patch_docs_sync.sh` und `git diff --check`.
- 2026-03-23: Patch 546: roten CI-Rest aus PR #407 auf den tatsaechlichen Workflow-Lint-Fehler zurueckgefuehrt und lokal behoben — `scripts/check_patch_docs_sync.sh` brach ab, weil `README.md` noch `Zuletzt abgeschlossen: **Patch 544**` meldete, waehrend `PROJECT_CHECKLOG.md`/`docs/patches/PATCHLOG_ROOT.md` bereits auf 545 standen; README, Checklog, Patchlog und die neue Patch-Notiz sind jetzt wieder synchron, und `jest.setup.js` sammelt zusaetzlich `unhandledRejection` pro Test als Fail-fast-Guard ein. Validation-Stand: alle lokalen Workflow-Guard-Skripte sowie drei gruene `npm run test:silent`-Laeufe, `npm run typecheck`, `npm run lint:ci` und `git diff --check`.
- 2026-03-23: Patch 545: Testhygiene- und Isolationspass fuer parallele Jest-Laeufe umgesetzt — `jest.setup.js` reinitialisiert blockierte Netzwerk-Globals jetzt vor jedem Test und fuehrt nach jedem Test DOM-, Spy-, Mock- und Timer-Cleanup zusammen, `lib/__tests__/fetchWithTimeout.test.ts` sowie `lib/__tests__/retryWithBackoff.test.ts` binden ihre `fetch`-Mocks deterministisch pro Test, lokale Fake-Timer werden in den betroffenen AI-/Timeout-Regressionen explizit zurueckgesetzt, und `__tests__/usePreview.serverContract.test.tsx` hinterlaesst keinen geaenderten `EXPO_PUBLIC_SUPABASE_URL`-Prozesszustand mehr. Validation-Stand: fokussierte Regressionen plus drei gruene `npm run test:silent`-Laeufe sowie `npm run typecheck`, `npm run lint:ci` und `git diff --check`.
- 2026-03-23: Patch 544: `__tests__/localRemoteDiffSection.truthfulness.test.tsx` ist jetzt deterministisch parallel-stabil — der Repo-/Branch-Switch-Test wartet explizit auf den Context-Reset (`Push (0)`) bevor der zweite Refresh ausgelöst wird, und ein kleines `afterEach` raeumt Cleanup, Timer und Mock-State zwischen den Faellen konsequent auf. Validation-Stand: `npm run typecheck`, `npm run lint:ci`, `npm run test:silent` und `git diff --check` lokal komplett gruen.
- 2026-03-23: Patch 543: letzte bestaetigte App-Timeout-Restpunkte bereinigt — `project/services/buildPollingService.ts` nutzt jetzt den gemeinsamen App-Helper `lib/network/fetchWithTimeout.ts` statt lokaler `AbortController`-/Timer-Logik, `supabase/functions/preview_page/helpers.ts` enthaelt keinen toten `withTimeout(...)`-Rest mehr, und `screens/EnhancedBuildScreen/hooks/buildScreenHelpers.ts` behaelt nur den bewusst lokalen Gesamt-Deadline-Guard fuer bereits helper-geschuetzte GitHub-Calls (jetzt mit gemeinsamem `TimeoutError`-Typ statt ad-hoc `Error`). Validation-Stand: `npm run typecheck`, `npm run lint:ci` und `git diff --check` gruen; `npm run test:silent` lief lokal einmal in den isoliert ebenfalls gruenen `localRemoteDiffSection.truthfulness`-Timeout, waehrend die gezielten Netzwerk-Haertungs-Tests mit `--runInBand` voll gruen waren.
- 2026-03-23: Patch 542: letzte Timeout-/Leak-Follow-ups nach Patch 541 konsolidiert — `hooks/previewHelpers.ts`, `screens/CredentialsWizardScreen/hooks/credentialHelpers.ts`, `hooks/useGitHubActionsLogs.ts` und `supabase/functions/preview_page/index.ts` haengen jetzt ebenfalls am gemeinsamen `fetchWithTimeout(...)`-Vertrag statt an lokalen `AbortController`-/Timer-Duplikaten, `supabase/functions/github-workflow-logs/helpers.ts` leakt in Timeout-Fehlern keine signierte Download-URL mehr, und `supabase/functions/k1w1-handler/helpers.ts` nutzt fuer echte Provider-Upstreams ein gemeinsames 45s-Limit; fokussierte Invariants/Regressionen sichern Preview-, Wizard-, Logs- und Leak-Vertrag regressionsfest ab.
- 2026-03-23: Patch 541: verbleibende Netzwerk-Haertung fuer App-/Edge-Fetch-Pfade konsolidiert — gemeinsame AbortController-basierte Timeout-Helper decken jetzt `infra/github/*`, `useCiLiteWorkflow`, `useConnectionsScreen`, `retryWithBackoff` sowie die betroffenen GitHub-/CodeSandbox-/Provider-Edge-Pfade ab, echte Aborts werden nicht mehr still retried, `github-run-artifact-json` und der deploybare `test`-Endpoint sind rate-limitiert, und fokussierte Tests sichern Timeout-/Abort-/Screen-Pfade regressionsfest ab.
- 2026-03-23: Patch 540: den verbliebenen `useGitHubActionsLogs`-Pending-Guard-Race nach Patch 539 minimal geschlossen — `hooks/useGitHubActionsLogs.ts` bindet den Pending-Guard jetzt an die konkret aktive Request-Version statt an einen globalen Boolean, sodass der Selection-Reset alte Requests weiter korrekt abortet, deren spaetes `finally` aber keinen neueren laufenden Request fuer dieselbe neue Auswahl mehr freigeben kann; ein fokussierter Contract-Test deckt Abort-alt → neuer Selection-Fetch → blockierter Parallel-Refresh regressionsfest ab.
- 2026-03-22: Patch 539: die zwei echten `useGitHubActionsLogs`-Follow-up-Regressionen nach Patch 538 minimal geschlossen — `fetchLogs()` erhoeht `requestVersionRef` jetzt erst nach dem Pending-Guard eines wirklich gestarteten Requests, sodass ein zweiter `refreshLogs()`-Aufruf laufende Antworten nicht still verwirft oder `isLoading` haengen laesst, und der Selection-Reset fuer `[githubRepo, runId, workflowId]` laeuft nun vor dem Auto-Refresh-Effekt, damit der erste legitime Fetch nach einem Repo-/Run-Wechsel nicht sofort von seinem eigenen Reset abortet wird; fokussierte Contract-Tests decken beide Hook-Randfaelle regressionsfest ab.
- 2026-03-22: Patch 537: den verbleibenden echten `preview_page`-Timer-/Cleanup-Restpunkt minimal geschlossen — `fetchPreviewRecord()` zieht den `supabaseHeaders()`-/Partial-Env-Guard vor `withTimeout(8000)`, sodass bei gesetzter `PREVIEW_SUPABASE_URL` aber fehlendem `PREVIEW_SERVICE_ROLE_KEY` kein unnoetiger Pending-Timer/Abort-Callback offen bleibt; `preview_env_missing`, der browser-facing HTML-Fehlerpfad und der bestehende Preview-Vertrag bleiben unveraendert, und ein fokussierter Test sichert die Guard-vor-Timer-Struktur regressionsfest ab.
- 2026-03-22: Patch 536: die zwei echten `preview_page`-Review-Restpunkte nach PR 398 minimal geschlossen — der Partial-Env-Fall mit gesetzter `PREVIEW_SUPABASE_URL` aber fehlendem `PREVIEW_SERVICE_ROLE_KEY` wird im Lookup jetzt explizit als `preview_env_missing` statt `preview_db_error` klassifiziert, und browser-/QR-seitige Env-/DB-/Lookup-Fehler bleiben ueber denselben strukturierten Header-/HTML-Marker-/Status-Vertrag auf dem sicheren HTML-Fehlerpfad statt als rohes JSON im Browser; fokussierte Preview-Vertragstests decken Partial-Env, HTML-Content-Type und den unveraenderten Marker-/Status-Pfad ab.
- 2026-03-22: Patch 535: Preview-Edge-Fehlervertrag gezielt gehaertet — `save_preview` und `preview_page` liefern fuer klassifizierbare Env-/Payload-/DB-/Runtime-/Groessenfehler jetzt strukturierte Preview-Codes statt blindem Generic-500, waehrend `hooks/previewHelpers.ts` diese Codes ohne fragile Text-Matches in ehrliche sichere Clientmeldungen mappt; fokussierte Preview-Vertragstests decken Env-missing, invalid/empty payload, DB insert/select, `preview_page`-Runtime-Catch und den Client-Mapping-Pfad ab.

- 2026-04-03: Patch 736: Preview-Secret-Transport gehaertet — `save_preview` liefert neue Preview-URLs im Fragment-Format (`?transport=fragment#secret=...`), `preview_page` nutzt im Fragment-Modus ein Bootstrap-Handoff ueber `x-k1w1-preview-secret` statt Query-Secret und behaelt Legacy-`?secret=`-Links kompatibel; gezielte Contract-Regression in `__tests__/previewEdgeErrorContract.test.ts` ergaenzt.
- 2026-03-22: Patch 534: PreviewScreen-Layout-Restpunkte nach PR 396 gezielt geschlossen — `screens/PreviewScreen/PreviewScreen.tsx` nutzt fuer den Scroll-Hauptpfad jetzt eine explizite Fill-Struktur statt nur eines intrinsischen Kind-Containers, `screens/PreviewScreen/PreviewScreen.styles.ts` verankert Hauptinhalt und BottomBar wieder ueber flex-/auto-margin-orientierte Regeln, und der ScrollView faehrt den Bottom-Safe-Area-Vertrag jetzt bewusst ueber genau einen Pfad (`contentInsetAdjustmentBehavior="never"` + BottomBar-Inset-Padding) ohne iOS-Doppelabstand; fokussierte Layout-Regressionen decken Fill-Vertrag und Safe-Area-Vertrag ab.
- 2026-03-22: Patch 533: PreviewScreen-Mobilpfad auf dem aktuellen `codex`-Stand gehaertet — `App.tsx`/`CustomHeader.tsx`-Routing-Check bestaetigt den bestehenden Drawer-`Preview`-Pfad plus Fullscreen-Modal ohne zweite aktive Legacy-Implementierung, `screens/PreviewScreen/PreviewScreen.tsx` verankert Toolbar/Status/DeviceFrame/Footer jetzt in einer stabileren Hauptstruktur mit kleinem Renderanker, `PreviewToolbar`/`PreviewStatusBar` falten lange Texte und Actions auf schmalen Geraeten kompakt statt eine einzige fragile Zeile zu erzwingen, und fokussierte Preview-Tests sichern Narrow-Toolbar, gleichzeitige Struktur sowie sichtbare Fallback-Zustaende; nebenbei wurde der veraltete `Deno.env.get(...)`-Invariant in `__tests__/k1w1Handler.providers.invariants.test.ts` auf den laengst produktiven `getRuntimeEnv(...)`-Server-Secret-Vertrag nachgezogen.
- 2026-03-21: Patch 532: produktiven `k1w1-handler`-Fehlervertrag fuer klassifizierbare Provider-/Env-/Upstream-Faelle gehaertet — `supabase/functions/k1w1-handler/helpers.ts` mappt bestehende interne Fehler jetzt auf sichere Codes/Meldungen statt auf rohe Upstream-Texte, `supabase/functions/k1w1-handler/index.ts` gibt diese strukturiert zurueck, und `lib/orchestrator/k1w1Edge.ts` zeigt im Chat dadurch ehrliche Ursachen wie fehlende serverseitige Provider-Konfiguration, 401/403, 429, Modell-nicht-verfuegbar oder allgemeinen Upstream-Fehler statt blindem `Internal Server Error`.
- 2026-03-21: Patch 531: Runtime-Dependency-Diagnose-Autofix fuer reale Repo-Faelle verbreitert — `runtime-import-dependency-mismatches` nutzt jetzt exakte Versionen aus `package-lock.json` bzw. `npm-shrinkwrap.json` als sichere Auto-Fix-Quelle, faellt erst danach auf kleine Expo-Safe-Mappings zurueck und kennzeichnet verbleibende Faelle ehrlicher als „kein sicherer AutoFix“ statt implizit generischer Warnung.
- 2026-03-21: Patch 530: lokale Diagnose-/AutoFix-Preflight-Pruefung fuer offensichtliche Runtime-Import→Dependency-Mismatches ergaenzt; Expo-/React-Native-Runtime-Imports werden gegen `package.json` gespiegelt, sichere SDK-54-Faelle (`expo-linear-gradient`, `expo-blur`) koennen gezielt in `dependencies` nachgetragen werden, und fokussierte Jest-Tests sichern Diagnose-vs.-Autofix-Vertrag ab.
- 2026-03-21: Patch 528: kleiner CI-Lite-/Build-Readiness-Cleanup ohne Verhaltensaenderung — `lib/ciLitePersistence.ts` exportiert die kanonischen Persistenzgruende und die gemeinsame CI-Lite-Workflow-ID jetzt als kleine Source of Truth, `lib/buildReadiness.ts` mappt diese Gruende auf Readiness-Reason-Codes ohne verteilte Stringvergleiche, und fokussierte Jest-Tests fuer Persistenz-/Readiness-Blocker wurden per `it.each(...)` leicht entdupliziert, ohne Regression-Schutz oder Verhalten zu verlieren.
- 2026-03-21: Patch 526: Remote-Preview-Payload im echten App-Pfad minimal repariert — `hooks/usePreview.ts` baut den `save_preview`-Request jetzt nur noch fuer echte zulässige Projektdateien und sendet ihn per explizitem JSON-`fetch` mit `x-k1w1-admin-key`, `hooks/previewHelpers.ts` klassifiziert leere/weggefilterte Preview-Dateien sowie `files fehlt/leer` ehrlich als Payload-/Dateiursache statt Auth, und fokussierte Preview-Regressionen decken nicht-leeren `files`-Payload, ehrlichen Leerfall, unveraenderten Remote-Hauptpfad und den sekundaeren lokalen Fallback ab.
- 2026-03-21: Patch 525: Repo-Secrets-Refresh-/Load-Loop in `screens/GitHubReposScreen/components/SecretsSection.tsx` minimal behoben — `load()` haengt nicht mehr an `names`, sondern liest den fuer Recheck-/stale-Entscheidungen benoetigten „bereits verifizierte Namensliste vorhanden“-Status aus einem kleinen `hasVerifiedNamesRef`; dadurch bleibt der Initial-Load pro Repo-Kontext bei genau einem automatischen Request, manueller Refresh funktioniert weiter, Repo-Wechsel invalidieren alte Requests unveraendert, und fokussierte Jest-Regressionen decken Einmal-Load, Refresh, Request-Invalidierung sowie das Beibehalten der zuletzt verifizierten Namensliste bei Refresh-Fehlern ab.
- 2026-03-21: Patch 524: CI-Lite-Chain-Run-Korrelation deterministisch und final dokumentiert — `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` verlangt fuer den Autofix→CI-Lite-Folgepfad denselben expliziten `job_id`-Marker wie fuer manuelle `workflow_dispatch`-Runs, `sourceHeadSha` bleibt nur zusaetzlicher Guard fuer marker-passende Kandidaten, und die pure Matching-Logik liegt im finalen Branch-Stand in `components/CiLiteHeaderButton/hooks/workflowRunMatching.ts` statt als Test-Only-Export am Hook; gezielte Regressionen pruefen weiter head_sha-only-Nichtbindung, konkurrierende gleiche-SHA-Runs und den unveraenderten manuellen Pfad.
- 2026-03-21: Patch 523: GitHubRepoScreen Push-/Pull-Pfad minimal gehaertet — `infra/github/utils.ts` blockiert `..`-Traversal-Segmente jetzt in `normalizeRepoPath(...)`, `infra/github/files.ts` lehnt traversal-artige lokale Push-Pfade dadurch vor jedem GitHub-Write mit klarer Fehlermeldung ab und laesst den bestehenden Managed-Workflow-Guard unveraendert, waehrend `hooks/useGitHubRepos.ts` fuer den Pull-Pfad einen harten, ehrlichen Cap von 200 erlaubten Textdateien setzt und grosse Repos vor dem Blob-Load sauber mit Hinweis abbricht; gezielte Regressionen decken Traversal-Block, normalen Push, Pull-Cap und normalen Pull weiter ab.

- 2026-03-21: Patch 522: lokalen Edge-Admin-Key-Save-Flow im Credentials Wizard minimal gehaertet — `screens/CredentialsWizardScreen/hooks/useCredentialsWizardScreen.ts` speichert leer/Whitespace weiter als bewusstes Loeschen, blockiert nicht-leere formal ungueltige Werte jetzt vor `saveEdgeAdminKey(...)` mit kurzer UI-Meldung und nutzt dafuer denselben `isLikelyValidAdminKey(...)`-Contract wie die bestehende `missing`/`invalid`/`rejected`-Diagnose; fokussierte Wizard-Regressionen decken Delete/Invalid/Valid-Save ab, waehrend die Diagnose-Tests aus Patch 520/521 unveraendert gruen bleiben.

- 2026-03-21: Patch 521: lokalen Edge-Admin-Key-HTTP-Vertrag im Wizard-/Preview-/CI-Lite-Scope bereinigt — `screens/CredentialsWizardScreen/hooks/credentialHelpers.ts` sendet fuer lokale Admin-Key-Edge-Calls ueber `invokeEdgeJson(...)` keinen semantisch falschen `Authorization: Bearer ...`-Header mehr, sondern nur noch `x-k1w1-admin-key`; gezielte Regressionstests sichern den Header-Vertrag fuer Wizard, Remote-Preview und CI Lite, waehrend die bestehende ehrliche `missing`/`invalid`/`rejected`-Diagnose sowie der Fall „frischer 401 ueber alte Persist-Verifikation“ regressionsfest bleiben.

- 2026-03-21: Patch 520: lokale Edge-Admin-Key-Klassifizierung fuer Wizard/Preview/CI Lite ehrlich getrennt — `screens/CredentialsWizardScreen/utils/localAdminKey.ts` behandelt `missing` jetzt nur noch fuer wirklich leere lokale Keys, mappt 401/403-/`missing or invalid admin`-/Header-Fehler bei vorhandenem lokalem Key konsequent auf `rejected`, `hooks/usePreview.ts` + `hooks/previewHelpers.ts` melden denselben praezisen Grund auch im Remote-Preview-Fallback, und `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` blockiert Dispatch/Artifact-Pfade mit denselben ehrlichen lokalen Key-Hinweisen statt pauschalem „fehlt/falsch".

- 2026-03-20: Patch 519: AppInfo-Backup-/Import-Pfad fuer Secrets/Tokens fachlich sauber getrennt und gehaertet — API-/KI-Konfiguration bleibt ein eigener projektdateifreier Export, der alte irrefuehrende Klartext-"Full-Backup"-Pfad ist durch passwortgeschuetzte AES-GCM-Backups fuer `Secrets/Tokens/Connections` bzw. optional `AI-/KI-Konfiguration + Secrets/Connections` ersetzt, und neue Regressionen sichern Verschluesselung, Import mit korrekter/falscher Passphrase, Legacy-Klartext-Ablehnung sowie die Abwesenheit von Projektdateien/Chats.
- 2026-03-20: Patch 518: lokalen Edge-Admin-Key-Vertrag fuer Wizard/Preview/Build ehrlich synchronisiert — `screens/CredentialsWizardScreen/hooks/useCredentialsWizardScreen.ts` rehydriert den lokalen Key jetzt bei Fokus und nach Save erneut aus SecureStore, persistiert projektbezogen auch Wizard-State/Detail fuer spaetere Build-Gates, `screens/EnhancedBuildScreen/hooks/signingKeyGate.ts` sowie `useBuildPreconditions.ts`/`useOneClickDeploy.ts` kommunizieren fehlende bzw. abgelehnte lokale App-Keys als echten Blocker, und `hooks/previewHelpers.ts` meldet fehlenden/ungueltigen lokalen Admin-Key im Remote-Preview-Pfad explizit statt nur neutral-vage.

- 2026-03-20: Patch 517: Preview-Produktvertrag auf den bestehenden Expo-/WebView-Remote-Pfad als klare SoT geschaerft — `hooks/usePreview.ts`, `screens/PreviewScreen/hooks/usePreviewScreen.ts` und `screens/PreviewScreen/components/DeviceFrame.tsx` priorisieren und benennen die vertraute Remote-Preview jetzt noch expliziter als Primaerpfad, waehrend `lib/sandpackBuilder.ts` sowie die Preview-Status-/Vertragstests den lokalen HTML-/Eval-Weg sichtbar nur noch als Dev-/Best-Effort-Fallback einordnen.

- 2026-03-20: Patch 516: finalen toten Legacy-Key-Manager-Rest konservativ entfernt — `lib/SecureKeyManager.ts` und `lib/__tests__/SecureKeyManager.test.ts` hatten keine Runtime- oder fachlich noetigen Test-Imports mehr und sind geloescht, der bestehende Patch-513-Invariant blockiert nun auch die Rueckkehr dieser Datei regressionsfest, waehrend `docs/SYSTEM_README.md` die produktive KI-Vertragslage wieder ohne veraltete SecureKeyManager-/SecureTokenManager-Empfehlungen beschreibt.

- 2026-03-20: Patch 515: verbliebenen GitHub-PAT-Passthrough vom Client zu workflow-/logs-/run-bezogenen Edge-Pfaden entfernt — `hooks/useGitHubActionsLogs.ts` und `components/CiLiteHeaderButton/hooks/useCiLiteWorkflow.ts` senden fuer `github-workflow-runs`, `github-workflow-logs` und `github-workflow-dispatch` kein `githubToken` mehr im JSON-Body, die drei Edge-Entry-Points beziehen den Token jetzt ausschliesslich ueber `_shared/github.ts`, und gezielte Vertrags-/Invariant-Tests sichern serverseitige Shared-Token-Nutzung sowie unveraenderte Admin-/Workflow-/Logs-Guards regressionsfest.

- 2026-03-20: Patch 514: verbleibenden produktiven Build-/Preview-Env-Restpfad auf Shared-Helper-Linie geschlossen — `check-eas-build`, `preview_page/helpers.ts` und `save_preview` bleiben auf den bestehenden `_shared/auth.ts`-Gettern, `trigger-eas-build` zieht den GitHub-Token-Lookup in `_shared/github.ts` jetzt ebenfalls auf `getRuntimeEnv(...)` statt `Deno.env.get(...)`, und der Patch-514-Invariant sichert Shared-Helper-Nutzung plus unveraenderte Admin-/CI-/Preview-Guard-Vertraege regressionsfest.

- 2026-03-20: Patch 513: Key-/Token-Manager-Hygiene im Client nach der Edge-Proxy-Umstellung geklaert — `contexts/AIContext/index.tsx` spiegelt Provider-API-Keys nicht mehr in den alten `SecureKeyManager`, `lib/SecureTokenManager.ts` plus zugehoeriger Test sind entfernt, `lib/SecureKeyManager.ts` ist explizit als Legacy-/Test-Helper ohne produktive Runtime-Rolle markiert, und ein neuer Patch-513-Invariant blockiert sowohl direkte Runtime-Imports als auch eine Rueckkehr der entfernten Token-Manager-Datei regressionsfest.

- 2026-03-20: Patch 512: alte direkte Client-Provider-Altpfade im Orchestrator-Bereich entfernt — `lib/orchestrator/providers/*` und die nur noch daran haengenden Tests `lib/__tests__/openaiProvider.test.ts` / `lib/__tests__/geminiProvider.test.ts` sind weg, `lib/orchestrator/index.ts` bleibt produktiv ausschliesslich auf `invokeK1w1Handler(...)` verdrahtet, `SecureKeyManager` bleibt bewusst nur fuer lokalen Key-/Rotations-State in `contexts/AIContext` bestehen, und ein neuer Patch-512-Invariant blockiert die Rueckkehr direkter Client-Provider-Imports im Runtime-Scope regressionsfest.
- 2026-03-20: Patch 511: verbleibende produktive Supabase-Edge-Entry-Points auf native `Deno.serve(...)`-Bootstrap-Linie gezogen — `check-eas-build`, `trigger-eas-build`, `github-workflow-dispatch`, `github-workflow-runs`, `github-workflow-logs`, `k1w1-handler`, `save_preview`, `preview_page` und `create_codesandbox` importieren kein altes `std/http/server.ts` mehr, bereinigte Helper tragen keine toten `serve`-Reexports/-Imports weiter, und ein neuer Patch-511-Invariant haelt die Runtime-Hygiene gegen neue gemischte Serve-Pfade regressionsfest.
- 2026-03-20: Patch 510: Android-Keystore-Export-/Status-Secret-Hygiene auf dieselbe Shared-Helper-Linie wie Generate gezogen — `_shared/auth.ts` exponiert jetzt kleine Runtime-Getter fuer `SUPABASE_URL` und `SIGNING_MASTER_KEY`, `android-keystore-generate`, `android-keystore-export` und `android-keystore-status` lesen diese Werte nicht mehr parallel direkt per `Deno.env.get(...)`, und ein neuer Invariant-Test plus angepasste Guard-/Helper-Visibility-Regressionen halten Shared-Helper-Nutzung sowie unveraenderte Admin-/CI-Guards regressionsfest.
- 2026-03-20: Patch 509: verbleibende repo-weite Wildcard-CORS-Restpfade in Edge-Functions auf request-gebundene Shared-Header gezogen — die disabled Legacy-Stubs `check-lint`, `trigger-lint`, `check-native-sync`, `trigger-native-sync`, `native-sync-report`, `native-sync-report-ingest` bauen ihre lokalen 410-Antworten jetzt mit sichtbarem `status: 410` plus request-gebundenem `corsHeadersForRequest(req)` statt ueber `...corsHeaders`, waehrend `test` weiter request-gebunden ueber `jsonResponse({ ok: true }, req)` laeuft, und `__tests__/edgeCorsRequestBound.invariants.test.ts` sichert diese Restpfade zusammen mit Origin-Reflection, Preflight und den bereits gehaerteten Kernpfaden gegen Rueckfall auf Wildcard-CORS ab.
- 2026-03-20: Patch 508: Flaky-Truthfulness-Test fuer `LocalRemoteDiffSection` gezielt stabilisiert — die Komponente invalidiert laufende Diff-/Preview-Requests jetzt auch beim Unmount, damit verspaetete Promise-Ruecklaeufer nach Teardown keinen State mehr committen, und `__tests__/localRemoteDiffSection.truthfulness.test.tsx` haelt diesen Unmount-/Late-Resolve-Fall mit einer zusaetzlichen Regression explizit fest.
- 2026-03-20: Patch 507: DB-/Schema-Referenzlage fuer Public-Schema und Diagnostics-Vertraege bereinigt — `supabase/schema.public.sql` ist nicht mehr leer/irrefuehrend, sondern klar als abgeleitete Sekundaerreferenz markiert und snapshotet nur die aktuell relevanten Public-Vertraege (`diagnostic_uploads`, `diagnostics_reports`, `insert_diagnostic_upload(jsonb)`), waehrend ein neuer Invariant-Test den SoT-Verweis auf Migrations + bestehende DB-/RPC-Guards sowie den finalen bigint-/Grant-Stand regressionsfest haelt.
- 2026-03-20: Patch 506: Diagnostic-Upload-Auth-Vertrag auf den echten Clientpfad zurechtgezogen — `lib/diagnostics/diagnosticUploader.ts` beschreibt den Upload jetzt explizit als direkten RPC ueber den normalen Supabase-Client, waehrend `supabase/migrations/20260320000000_restore_insert_diagnostic_upload_anon_client_contract.sql` `insert_diagnostic_upload(jsonb)` fuer `anon`/`authenticated`/`service_role` wieder bewusst freigibt; gezielte Jest-/Invariant-Tests sichern direkten RPC-Pfad, finalen Grant-Stand und unveraenderte ID-/Payload-Form ab.
- 2026-03-20: Patch 505: Secret-Hygiene rund um Service-Role-/Signing-/Backup-Pfade bereinigt — `supabase/functions/android-keystore-generate` bezieht den serverseitigen Service-Role-Key jetzt ueber den bestehenden Shared-Auth-Helper statt ueber einen parallelen direkten Env-Lesepfad, waehrend `lib/storageKeys.ts` keinen normalen Client-Storage-Slot fuer `SUPABASE_SERVICE_ROLE_KEY` mehr exponiert und AppInfo-Backup-/Import-Cleanup stattdessen nur noch einen expliziten Legacy-Cleanup-Helper fuer alte AsyncStorage-Reste nutzt; fokussierte Invariants sichern Shared-Helper-Nutzung und das Entfernen des irrefuehrenden Client-Slots ab.
- 2026-03-22: Patch 538: kleiner Maintenance-/Robustheits-Block auf dem aktuellen `codex`-Stand geschlossen — GitHubContext spiegelt `projectData.linkedRepo` / `linkedBranch` ohne redundante Mirror-Re-Set-Schleifen, `useGitHubActionsLogs` haengt Workflow-Run- und Log-Fetches jetzt an einen echten AbortController-/Timeout-Vertrag inklusive Input-Invalidierung, und AIContext schreibt die redacted Config nur noch coalesced per Debounce nach AsyncStorage; gezielte Jest-Tests decken Mirror-, Abort- und Persistenzvertrag ab.
- 2026-03-20: Patch 504: Edge-CORS-Haertung im engen Scope umgesetzt — `k1w1-handler`, `github-workflow-runs` und der eng angrenzende geschuetzte `github-workflow-logs` erzeugen lokale Erfolgs-/Fehlerantworten jetzt ueber request-spezifische Shared-CORS-Header statt ueber Wildcard-`corsHeaders`, waehrend `handleCors(req)` das bestehende OPTIONS-/Preflight-Verhalten unveraendert beibehält; neue Invariants decken Origin-Reflection, Preflight-Max-Age und das Entfernen der Wildcard-Nutzung ab.
- 2026-03-20: Patch 503: Confirm-/Review-UX fuer KI-Aenderungen dateibezogen und diff-orientierter gemacht — `ConfirmChangesModal` nutzt die vorhandenen `changePreviews` jetzt als kompakte Datei-Review-Karten fuer neue/geaenderte Dateien, zeigt Builder-vs-Validator-Provenance ueber `finalFileSource`/`validatorState` ehrlich advisory an, und blendet uebersprungene bzw. geblockte Hinweise sichtbar ausserhalb der langen Summary ein; fokussierte Jest-Regressionen decken Vorschau, Delta-Ausschnitte, Provenance und den unveraenderten Accept/Reject-Flow ab.
- 2026-03-20: Patch 502: Prompt-/Write-Contract an die echte Chat-Ownership-Policy synchronisiert — `effectiveWritePolicy` nutzt jetzt reale Chat-Guard-Probes statt der groben Config-Allowlist als Source of Truth fuer Prompt-Hinweise, markiert kritische/manual-only bzw. baseline-verwaltete Pfade (`.github/workflows/*`, `supabase/*`, `templates/*`, `scripts/*`, `android/*`, `ios/*`, `package.json`, `app.config.*`, `eas.json` usw.) explizit als guarded/manual-only/read-only, und Planner/Builder/Validator betonen im Prompt jetzt sichtbar den priorisierten Snapshot statt Vollrepo-Sicht; fokussierte Jest-Regressionen sichern Policy-Sync, Snapshot-Ehrlichkeit und unveraenderte Ownership-Haerte ab.
- 2026-03-20: Patch 501: Produktiver Chat-Sendepfad gegen lokale Preview-/Privacy-Leaks gehaertet — `useChatAIFlow` validiert/sanitizt jetzt jeden echten KI-Request nochmals direkt vor Planner/Builder/Validator, lokale Meta-/Preview-Nachrichten werden mit `localOnly`/`metaCommand`/`containsFilePreview` sichtbar aber aus spaeterer Provider-History herausgefiltert, und `redactSecrets(...)` deckt zusaetzlich Cookie-/Credential-Muster fuer providergebundene Prompts ab; fokussierte Jest-Regressionen pruefen Sendepfad-Sanitizing, lokale-History-Filter und Secret-Redaction.
- 2026-03-20: Patch 500: Produktiver AI-Flow laeuft jetzt ueber Supabase Edge `k1w1-handler` statt ueber direkte Client->Provider-HTTP-Calls — `runOrchestrator(...)` nutzt `supabase.functions.invoke(...)` mit `x-k1w1-admin-key`, sendet nur noch `provider`/`model`/`quality`/`messages`, mappt `ok/content/error/model/provider` kompatibel auf `OrchestratorResult`, und fokussierte Jest-Regressionen decken Invoke-Wiring, No-Provider-Key-Containment, Response-Mapping sowie Timeout-/Abort-Pfade ab.
- 2026-03-20: Patch 499: GitHubReposScreen trennt Repo-Secret-Namen und lokale Laufzeit-Credentials jetzt sichtbar und ehrlich — `SecretsSection` zeigt fuer `EXPO_TOKEN` und `K1W1_EDGE_ADMIN_KEY` jeweils `Repo Secret` vs. `Lokaler App-Wert`, eine zusaetzliche Truthfulness-Summary warnt bei lokal fehlenden SecureStore-Werten trotz bestaetigter Repo-Secrets, und der CI-Lite-Modaltext sagt explizit, dass der lokale Edge Admin Key fuer App→Edge-/Dispatch genutzt wird; fokussierte Jest-Regressionen decken fehlenden/lokalen Edge-Key, EXPO_TOKEN-Trennung und die bestehende Repo-Secret-Anzeige ab.
- 2026-03-20: Patch 498: GitHubReposScreen `SecretsSection` auf denselben Repo-Secret-Verification-Contract wie Diagnostics gezogen — kleiner Shared-Helper mappt bestätigte GitHub-Secret-Namenslisten jetzt auf `verified`/`missing`, 401/403 auf `auth_error`, generische Load-Fehler auf `unknown`, und ein Recheck-Fehler nach bereits bestätigter Liste auf `stale`; die Section rendert damit ehrliche Summary-/Row-Copy statt `names=[]`-basiertem Missing-Fallback, und fokussierte Jest-Regressionen decken verified/missing/auth_error/unknown sowie die bestehende Erfolgsanzeige ab.
- 2026-03-20: Patch 497: Pipeline-Diagnostics Repo-Secret-Contract/Title-Truthfulness gehärtet — `listRepoSecretNames(...)` mappt `EXPO_TOKEN`, `SUPABASE_URL` und `SUPABASE_SERVICE_ROLE_KEY` bei erfolgreicher Namensliste jetzt explizit auf `verified` bzw. `missing` statt auf implizites `unknown`, Auth-/Permission-Probleme bleiben ausschließlich echte Fehlerpfade, und zustandsabhängige Secret-Titel/Fix-Hinweise behaupten keinen vorhandenen Status mehr ohne Bestätigung; fokussierte Jest-Regressionen für verified/missing/auth/title-semantics ergänzt.
- 2026-03-20: Patch 496: GitHubReposScreen EAS-Link-Statuschecks gegen stale Async und Race-Zustände gehärtet — ein kleiner Request-/Generation-Guard bindet jeden Check an `repo@@branch`, Repo-/Branch-Wechsel invalidieren laufende Checks und resetten die UI bewusst auf neutral/unknown, ältere Requests dürfen weder neuere manuelle Checks noch den Write→Recheck-Pfad überschreiben, und fokussierte Jest-Regressionen decken Stale-Context, Newer-Request-Wins, Write→Recheck, Neutral-Reset und Same-Context-Guard ab.
- 2026-03-20: Patch 495: `LocalRemoteDiffSection` Same-Context-Truthfulness für lokale Dateiänderungen gehärtet — ein kleiner lokaler Fingerprint bindet die geladene Diff-Wahrheit jetzt an den tatsächlichen lokalen Dateistand; bei Änderungen im selben Repo-/Branch-Kontext werden alte Diff-Items, Push-Selection, Inline-/Modal-Preview, Preview-Cache und laufende Requests ehrlich invalidiert/resetet, und die UI fordert explizit einen neuen Vergleich per Refresh.
- 2026-03-20: Patch 494: `LocalRemoteDiffSection` stale-async-/Cache-/Reset-Semantik gehärtet — Repo-/Branch-Wechsel invalidieren laufende Diff- und Preview-Loads jetzt per hartem Request-Guard, der Bereich resetet Items/Note/Selection/Inline-Expanded-State/Modal-Preview ehrlich auf neutral, und der Preview-Cache ist an den aktiven Repo-/Branch-Kontext gebunden statt alte Datei-/Diff-Wahrheit weiterzuverwenden; fokussierte Jest-Regressionen für stale Load, Cache-Invalidation, UI-Reset, Preview-Truth und Same-Context-Regression ergänzt.
- 2026-03-20: Patch 493: GitHubReposScreen EAS-Link-/Repo-Semantik gehärtet — neuer kleiner `easLinkContract` zentralisiert die Zustände `verified`, `workflow_missing`, `project_missing`, `project_invalid`, `project_mismatch`, `auth_error`, `unknown` und `pending_recheck`; der Repo-Status-Check validiert jetzt Workflow-Präsenz und erwartete `projectId` getrennt und ehrlich, Auth-/Permission-/temporäre Fehler verrutschen nicht mehr zu „ok/fehlt“, und der Link-Write signalisiert Erfolg erst bei echter Verifikation bzw. sonst explizit Re-Check-Pending; fokussierte Jest-Regressionen für Contract-, Mismatch-, Auth-, Link-Flow- und UI-Text-Semantik ergänzt.
- 2026-03-19: Patch 492: GitHubReposScreen Pull-/Push-/Sync-Wahrheit gehärtet — Pull-Apply nutzt jetzt einen kleinen gemeinsamen Semantik-Resolver (`applied`/`noop`/`partial`), awaited echte lokale Writes vor Sync-Signatur/Refresh/Success, setzt bei lokalem Apply-Fehler keine Success-/Sync-Folgeschritte mehr, behandelt `skipConflicts` explizit als teilweise statt vollständig synchron, und blockiert false-green Push-Erfolg bei leerer Auswahl oder fehlendem Branch; fokussierte Jest-Regressionen für Reihenfolge, Fehler, no-op, partial, Push-Guards und Erfolgs-Regression ergänzt.
- 2026-03-19: Patch 491: Diagnostics-Fix-Engine/UX semantisch gehärtet — kleiner gemeinsamer `fixResultContract` trennt jetzt `advisory_only`, `patch_applicable`, `patch_applied`, `workflow_dispatched`, `blocked`, `failed` und `pending_recheck`; leere/no-op oder guard-blockierte Patches werden nicht mehr als Erfolg klassifiziert, Teilfehler bleiben ausdrücklich teilweise/fehlgeschlagen, Workflow-only-Fixes erscheinen als gestartet/recheck-pending statt lokal behoben, und Badge-/Action-/Toast-Texte nutzen dieselbe ehrliche Semantik; fokussierte Jest-Regressionen für Contract-, Apply-, Partial-, Workflow- und UI-Text-Semantik ergänzt.
- 2026-03-19: Patch 490: CredentialsWizard-Trust-/Safety-/Status-Härtung — Wizard-Status nutzt jetzt einen kleinen gemeinsamen Contract auf Basis des bestehenden Verification-Contracts (`verified`, `missing`, `unknown`, `auth_error`, `generated_pending_verification`, `busy`); Generate setzt erst `generated_pending_verification`, Refresh/Recheck mappt Auth-/Permission-/temporäre Fehler nicht mehr auf "fehlt", ein erfolgreicher Nach-Check hebt sauber auf `verified`, und ein gemeinsamer Busy-Guard blockiert widersprüchliche Parallelaktionen; fokussierte Jest-Regressionen für Status-, Generate-, Recheck-, UI- und Busy-Semantik ergänzt.
- 2026-03-19: Patch 488: kleiner gemeinsamer `verificationContract` vereinheitlicht Secret-/Credential-/EAS-/Readiness-Semantik über Connections, Build-Readiness und Diagnostics; Auth/Permission/Unknown/Stale werden nicht mehr als „fehlt“ dargestellt, Diagnostics-Hints bleiben ehrlicher; Typecheck/Lint/volle Jest-Suite grün.
- 2026-03-19: Patch 487: zweite AI-Flow-Welle konservativ umgesetzt — `validateChatInput()` hängt nun im echten Sendepfad und nutzt sanitizte Eingabe weiter; Planner/Builder/Validator bekommen deterministisch budgetierten Kontext (History zuerst, dann Snapshot-Dateien, dann Ausschnitte); Confirm-Modal zeigt kompakte Inhaltsdeltas inkl. Datei-Vorschau/Before-vs-After; Validator-Herkunft/Fallback wird im UX explizit als advisory transparent gemacht; Typecheck/Lint/volle Jest-Suite grün.
- 2026-03-15: Patch 461: Chat-Restregressionen aus PR #272/#273 gemeinsam final geschlossen; Meta-/lokale Kommandos laufen weiterhin auf unverändertem Raw-Input, Attachment-only wird nicht still verworfen, und Pending-Plan-Handoff übernimmt bei leerem Raw den `aiInput`-Fallback; Invariant-Regression plus Workflow-/Typecheck-/Lint-/Tests grün.
- 2026-03-15: Patch 460: Chat-Regression nach PR #273 konservativ geschlossen — `useChatAIFlow.handleSendWithMeta(...)` bricht nur noch bei gleichzeitig leerem Raw-/AI-Input ab; Attachment-only-Sendefälle werden nicht mehr still verworfen, Meta-/lokale Kommandos laufen weiterhin ausschließlich auf unverändertem Raw-Input; gezielte Invariant-Regression ergänzt.
- 2026-03-15: Patch 457: Connections-Busy-Guard meldet Busy-Kollisionen jetzt explizit (dedizierter Error) statt mehrdeutigem `false`; Save/Test-Aktionen trennen Busy vs. echte Fehler sauber. `useChatAIFlow`-Pending-Plan-Guard wurde gezielt geprüft und per Invariant abgesichert.
- 2026-03-15: Patch 456: Chat-Drift-Digest RN-sicher dokumentiert — `lib/chatFlowStateGuards.ts` enthält jetzt eine explizite Guardrail-Notiz gegen Node-`crypto` im App-Runtime-Pfad (`useChatAIFlow`), damit Metro-Bundles keine Node-Core-Module auflösen müssen.
- 2026-03-15: Patch 455: ConnectionsScreen-Restpunkte konservativ gehärtet — Supabase-ANON-Key jetzt SecureStore-basiert (inkl. Legacy-Migration), Busy-/Hydration-Guards blockieren parallele Save/Test-Runs, `testExpo` ohne Token-Persistenz-Side-Effect, EAS-Link-Lampe nicht mehr optimistisch grün; flow-nahe Invariants + Storage-Tests ergänzt.
- 2026-03-15: Patch 454: OneClickDeploy-Testflake in `__tests__/oneClickDeploy.test.tsx` gezielt stabilisiert (deterministischer `act`-Press + Microtask-Flush, AsyncStorage-Default-Resolves, striktes Cleanup/Timer-Clear); kein Produktcode-Umbau.
- 2026-03-15: Patch 453: Misstrauisches KI-/Chat-Nachaudit — echte Restlücke im Non-JSON-Fehlerpfad geschlossen (`output_text` wird im Normalizer als Response-Text durchgereicht), gezielte Regressionstests ergänzt; Full-Suite im Audit-Lauf grün.
- 2026-03-15: Patch 451: PreviewScreen/PreviewFullscreen-Restprobleme konservativ behoben — `usePreview` nutzt content-basierte Fingerprints (Hot-Reload erkennt nun auch inhaltliche Same-Length-Edits), PreviewScreen nutzt dieselbe WebView-Crash-Recovery wie Fullscreen (inkl. one-shot Auto-Reload), und abgelaufene Supabase-URLs werden im normalen PreviewScreen nicht mehr blind in die WebView gegeben; Preview-Helper/Types dedupliziert, `previewFiles`-Dependency auf `projectData?.files` verengt, lokale HTML-Fallback-Transienz im UI klarer benannt.
- 2026-03-15: Patch 450: CustomHeader-/CI-Lite-Restfix konservativ umgesetzt — `useGitHubActionsLogs` resetet `workflowRun`/`logs` bei Input-Wechsel und verwirft verspätete Antworten per Request-Key-Guard; CI-Lite-Persistenz speichert nur noch für den aktiven CI-Lite-Run-Kontext (`workflowRun.id===runId` + Repo/Branch-Guard), Doppeltap-Dispatch ist blockiert, `head_sha` im `WorkflowRun`-Typ ergänzt, Artifact-JSON lokal typisiert, Patch-Sync ohne `as any` + `syncPatchToGitHub` via `useCallback` stabilisiert; gezielte Regressionen ergänzt.
- 2026-03-15: Patch 449: EnhancedBuildScreen-Restpunkte gezielt behoben — OneClickDeploy entfernt den redundanten Vorab-Push (SHA-sichere Reihenfolge bleibt im `startBuildJob`), `canStartBuildUi` wurde auf state-basiertes Inflight-Flag umgestellt, unnötige `openRunDetails`-Ref-Dependencies bereinigt und Build-/Logs-`WorkflowRun`-Typing inkl. `event` vereinheitlicht (`LogsAnalysisSection` ohne `any`).
- 2026-03-15: Patch 448: DiagnosticScreen-Restpunkte gezielt behoben — progressive Preflight-Progress nutzt wieder Severity-`stage`; „KI-Fix verfuegbar“ wird nicht mehr bei `pass`-Checks angezeigt; flow-nahe Typing-/Hook-Cleanups (`projectData`-Casts, Runner-`files`-Typing, Fix-Runner-Signaturen, Dependency-Vollständigkeit) plus fokussierte Regressionstests ergänzt.
- 2026-03-15: Patch 447: kleiner Edge-Typing-Follow-up umgesetzt; `_shared/auth` + `_shared/cors` lesen Runtime-Env jetzt ohne `globalThis as any`, `_shared/validation` reduziert verbleibende `any` in Trigger-/Dispatch-Validierung und `parseJsonBody` lehnt nicht-Objekt-JSON nun explizit mit Testabdeckung ab.
- 2026-03-15: Patch 446: Build-Start-Type-Safety gezielt nachgeschärft; riskante `any`-Zugriffe auf Edge-Invoke-Payload in `buildStartService` durch lokales Payload-Narrowing ersetzt, unnötigen `pushFilesToRepo`-Cast entfernt und mit Regressionstests (`job.id` + error-shaped payload) abgesichert.
- 2026-03-15: Patch 445: `save_preview`-Headerkonsistenz zu Auth-/Rate-Limit-Fehlerpfaden zusätzlich per Invariants abgesichert; `_shared/auth` nutzt nun Deno/Node-kompatiblen Env-Lookup statt harter Deno-Referenzen, wodurch der lokale Typecheck wieder grün läuft.
- 2026-03-15: Patch 444: `save_preview`-Restinkonsistenz bei CORS-/Security-Headern reduziert; lokale Erfolgs-/Fehlerantworten auf `_shared/cors` ausgerichtet, damit sie konsistenter zu Auth-/Rate-Limit-Fehlerpfaden reagieren; gezielte Invariant-Tests ergänzt.
- 2026-03-15: Patch 443: `k1w1-handler` Provider-Härtung für Restfälle umgesetzt — Anthropic verhindert leeres `messages`-Array bei reinen `system`-Prompts, Gemini mappt `system` explizit via `systemInstruction` und nutzt nicht-leeren `contents`-Fallback; doppeltes Gemini-Nullish-Coalescing entfernt, Invariant-Tests ergänzt.
- 2026-03-15: Patch 442: Build-Status-/Phasen-Feintuning im EnhancedBuildScreen umgesetzt; aktiver Lauf, letzte bekannte Build-Daten und Auswahl sauberer getrennt, aktive Phase expliziter markiert sowie Run/Artefakt/Download-Labels auf aktuellen vs. vergangenen Kontext geschärft (ohne Architekturänderung).
- 2026-03-15: Patch 441: konservatives Mikro-UX-Finetuning der Kernpfade Build/Diagnosis/Preview umgesetzt; Build- und Diagnose-CTAs alltagsnäher benannt, Preview-Statussprache auf Live/Fallback/Fehler vereinheitlicht, bestehende Readiness-/Guard-Logik unverändert gelassen.
- 2026-03-15: Patch 440: konservatives UX-/Flow-Feintuning (Build/Diagnosis/Preview/Connections/Credentials/Chat) mit Fokus auf klarere Status- und Fallback-Semantik ohne Architekturumbau; ergänzende Regression für Preview-Status-Text.
- 2026-03-15: Patch 438: Fragile Cross-Boundary-Imports in produktiven Supabase-Workflow-Edges entfernt (`../../../shared/constants/github.ts`); `GITHUB_API_BASE` edge-nah in `_shared/github.ts` verankert und per Invariant-Test gegen Regress abgesichert.
- 2026-03-15: Patch 437: Doppelte Preview-Migration (`20251226140000_fix_previews.sql`/`20251226160000_fix_previews.sql`) als byte-identische Redundanz bestätigt; spätere Migration als explizites Legacy-No-op markiert (kein History-Delete), um Doppel-Ausführung in frischen Setups und Wartungsirritation zu vermeiden.
- 2026-03-15: Patch 436: Migrations-/RPC-Hygiene für `insert_diagnostic_upload` finalisiert; historischen UUID-/Spalten-Drift (`repo/branch/mode/platform/report/meta`) explizit eingeordnet, finalen `jsonb -> bigint`-Vertrag per Abschlussmigration reassertet und mit Invariant-Test abgesichert.
- 2026-03-14: Patch 435: Großer Supabase-E2E-Vertragscheck (Preview, Workflow-Dispatch/Runs/Logs/Artifacts, Signing/Keystore, k1w1-Handler) finalisiert; verbleibende Operator-Schritte explizit dokumentiert und `github-run-artifact-json` ZIP-Pfadnormalisierung für `\`-Separatoren korrigiert (stabileres Artifact-JSON-Mapping).
- 2026-03-14: Patch 434: Supabase-E2E-Contract-Audit abgeschlossen; zentrale Supabase-Edge-Function-Konstanten für Signing/Preview/AI vervollständigt und Keystore-Wizard-Calls auf SoT-Constants migriert (Hardcode-Drift entfernt), Invariant-Tests ergänzt.
- 2026-03-14: Patch 433: End-to-End-Supabase-Contract-Audit über produktive Edge-Flows; stilles 200/ok:false-Drift-Risiko im Credentials-Wizard behoben (`invokeEdgeJson` mapped jetzt konsistent auf Fehlerzweig), Regressionstest ergänzt.
- 2026-03-14: Patch 432: Ownership-Audit-Härtung — zentrale Guard-Regeln für Template/Baseline vs. Chat vs. Diagnosis/Autofix eingeführt; Chat-Writeback auf kritische/template-nahe Pfade blockiert, Diagnosis/Autofix auf kuratierte Pfade begrenzt; Regressionstests für Ownership-Konflikte ergänzt.
- 2026-03-14: Patch 431: System-Audit-Fix — `diagnostic_last_ok` für Diagnosis→Build-Flow auf Repo/Branch scoped gemacht (legacy fallback bleibt), damit keine falsche Freigabe zwischen unterschiedlichen Selektionen entsteht.
- 2026-03-14: Patch 430: Live-Reachability-Audit für Groq/Gemini/OpenAI/Anthropic/HuggingFace mit realen Smoke-Requests; in dieser Umgebung alle Provider wegen fehlender Secrets nur als `missing_secret` klassifizierbar. k1w1-Handler-Fehlerverträge um provider+model-Kontext erweitert, Groq-Fallback gibt resolved Modell-ID zurück.
- 2026-03-14: Patch 429: KI-/Provider-Konfigurationsaudit — k1w1-Edge-Defaultmodelle auf app-konsistente aktuelle IDs aktualisiert (Gemini 2.5, Anthropic datierte IDs, HF Qwen), Groq-"groq/"-Kompatibilitätsfallback ergänzt, Invariant-Tests erweitert.
- 2026-03-14: Patch 428: Zweiter Korrektheits-Check durchgeführt; rote Suite auf `__tests__/oneClickDeploy.test.tsx` durch konservative Timeout-Stabilisierung im Test behoben (kein Produktionscode geändert).
- 2026-03-14: Patch 427: Supabase-Function-Flow-Audit — Keystore-Wizard-Edge-Aufrufe mit hartem Timeout und ehrlicher HTTP-Fehlerextraktion (JSON error/message + Details-Fallback) gehärtet.
- 2026-03-14: Patch 426: Chat/KI-Änderungsfluss konservativ gehärtet — Pending-Änderungen werden vor Persistenz auf den aktuellen Projektstand rebased; Zustandsdrift wird transparent im Chat signalisiert; Guard-Regressionstests ergänzt.
- 2026-03-14: Patch 425: Diagnosis/Autofix/One-Click-Kette konservativ gehärtet — One-Click blockiert jetzt explizit bei fehlender/grenzwertiger Readiness (Diagnostic, CI-Lite, Repo/Branch-Match, Freshness); mode-abhängige Pipeline-Filterlogik testbar gemacht.
- 2026-03-14: Patch 424: Supabase-Preview produktseitig als bevorzugter Browser-/WebView-Weg geschärft; klare URL-/Expiry-/Fallback-Kommunikation und QR-Aktion für bestehende Preview-URL ergänzt.
- 2026-03-14: Patch 423: Machbarkeits-Audit für Expo Web / QR-Web-Preview abgeschlossen; Empfehlung B (gezielte Vorarbeiten vor offizieller Aktivierung), Supabase-URL-Preview kurzfristig realistischer.
- 2026-03-13: Patch 422: E2E-Traceability gehärtet — Build-History-Updates nutzen für laufende Jobs eine job-gebundene Snapshot-Selektion (Repo/Branch/Profil) statt potenziell driftendem UI-Zwischenzustand.
- 2026-03-13: Patch 421: Build-Transparenz gehärtet — effektive Repo/Branch/Profil-Werte werden im Start-/Statusfluss konsistent in `currentBuild` + Historie getragen und im Build-Screen explizit angezeigt.
- 2026-03-13: Patch 420: Guard-/Invariant-Härtung gegen implizite Workflow-Ref-Fallbacks (`github.ref`, `github.ref_name`, `github.head_ref`, `default_branch`) in managed Deploy/Build-Flows; CI/CI-Lite-Ausnahme explizit abgesichert.
- 2026-03-12: Patch 419: Follow-up auf PR #206; `[EDGE_FUNCTIONS_STATUS](docs/EDGE_FUNCTIONS_STATUS.md)` im Doku-Index wiederhergestellt und Patch-Benennung konsistent gehalten.
- 2026-03-12: Patch 418 V1 — Core-Doku auf Post-417-Realität gezogen, offene Restpunkte zentral in `docs/TODO.md` gesammelt, MD-/Notes-Cleanup als nächster Schritt fixiert.
- 2026-03-12: Patch 417 V18 — versehentlich committetes Patch-Artefakt aus dem Repo-Root entfernt, Patch-Bundle-/Patch-Datei-Artefakte per `.gitignore` gegen Re-Commit abgesichert.
- 2026-03-12: Patch 416 — stillgelegte Legacy-Lint-/Native-Sync-Edges auch in `supabase/config.toml` deaktiviert; Guard-/Invariant-Coverage ergänzt.
- 2026-03-11: Patch 414 V13 — Ref-SoT-Invariant robust gemacht (inkl. escaped Template-Dekodierung); dokumentierte branch-basierte CI-Lite-Ausnahme bewusst beibehalten.
- 2026-03-10: Patch 413 — restliche stille Repo-/Branch-Fallbacks in Build/Repo/Diagnostics/Diff/CI-Lite-Pfaden entfernt; Regression-Coverage ergänzt.
- 2026-03-10: Patch 412 — privilegierte Supabase-Funktionen gehärtet (`search_path`, `PUBLIC`-Execute-Revoke) + Guard-/Invariant-Coverage.
- 2026-03-10: Patch 411 V7 — Supabase-Deploy auf `workflow_dispatch` + expliziten `ref` gehärtet; `_shared`-/Single-Function-Guards und Migrations-Policy synchronisiert.
- 2026-03-09: Patch 410/410B — Edge-Auth-Pfade getrennt, explizite CI-Bearer-Guards ergänzt, Service-Role-Handhabung aus Client-Pfaden entfernt.
- 2026-03-09: Patch 409/408 — Upload-ID-Vertrag als opaque string stabilisiert; Build-Job-Vertrag auf positive numerische `jobId` ausgerichtet.

- 2026-03-21: Patch 527: CI-Lite-Persistenz auf repo-/branch-scoped Snapshot-Vertrag gehärtet — neuer `ci_lite_snapshot::<repo>::<branch>`-Schluessel ist die bevorzugte Wahrheit fuer Header + Build-Readiness, `readPersistedCiLiteSelection(...)` liest scoped zuerst und nutzt flache `CI_LITE_LAST_*`-Keys nur noch als Legacy-Fallback; fokussierte Jest-Regressionen decken scoped Read, Mismatch, kaputte Persistenz, Legacy-Fallback und den scoped Write-Pfad ab.

- 2026-03-21: Patch 529: CI-Lite-Run-Lookup fuer Legacy-`workflow_dispatch`-Workflows gehaertet — exakter `job_id`-Marker bleibt der bevorzugte Trefferweg, sichere Fallback-Tiers ueber `head_sha`/frische Branch-Kandidaten verhindern falsche Generic-Timeouts, und mehrdeutige Kandidaten liefern nun explizite Contract-/Ambiguitaetsdiagnosen statt falscher Bindung.

## Hinweise

- 2026-03-20: Patch 517: Preview-Produktvertrag weiter auf den bestehenden Expo-/WebView-Remote-Pfad fokussiert; lokale HTML-/Eval-Vorschau ist jetzt in Hook-, UI-, Builder- und Test-Semantik explizit nur noch Dev-/Best-Effort-Fallback.
- Vollständige Historie: `docs/patches/PATCHLOG_ROOT.md`.
- Operative Restliste / Follow-ups: `docs/TODO.md`.
- 2026-03-15: Patch 459: Chat-Regression aus PR #272 konservativ geschlossen — Meta-/lokale Kommandos werden wieder gegen unveränderten Raw-Input geprüft; Attachment-Hinweis wird erst nach Command-Routing für den normalen AI-Request angehängt; gezielte Invariant-Regression ergänzt; Workflow-/Typecheck-/Lint-/Tests grün.
- 2026-03-15: Patch 458: ChatScreen/Chat-Flow Restpunkte konservativ geschlossen: ehrlicher Attachment-Hinweis (kein verdeckter Dateiinhalt-Anspruch), Retention-Pruning direkt beim Chat-Append, Blur-Cleanup für Pending/Modal/Streaming-State sowie typing-/import-Hygiene in chatAIFlow-Types; Verifikation mit Workflow-Checks + Typecheck/Lint/Test vollständig grün.

- 2026-03-23: Patch 550: PR-408-Review-Follow-up gegen den aktuellen Branch-Stand abgeschlossen — App/Edge teilen jetzt eine gemeinsame Provider-Default-Quelle, Anthropic-Quality zeigt/runtime nutzt `claude-sonnet-4-20250514`, Groq/Gemini-Defaults bleiben bewusst auf ihren runtime-gueltigen IDs, und sichtbare Runtime-Fallback-Hinweise werden per `runtimeNote`-Meta-Filter nicht mehr in spaetere Planner/Builder/Validator-Prompts uebernommen.
- 2026-03-28: Patch 557 Notification-Push-Haertung umgesetzt: `notificationService` nutzt nur noch fachlich korrekte Expo/EAS-`projectId`-Quellen (kein owner/placeholder), Expo Push Token wird nicht mehr geloggt, und `useNotifications.requestPermissions()` aktualisiert `pushToken`/`isInitialized`/`hasPermissions` konsistent auch nach spaeterem Permission-Grant/-Revoke; Regressionstests fuer projectId-Fallback und Hook-State-Sync ergaenzt.
- 2026-03-28: Patch-557-Follow-up abgeschlossen: `notificationService.initialize()` setzt `permissionGranted`/`expoPushToken` konsistent; explizit getestete Fehler-/Abbruchpfade sind denied, Outer-Init-Error, Token-Fetch-Error und Android-FCM-Skip. `lib/__tests__/notificationService.test.ts` sichert zudem die `projectId`-Prioritaet (`easConfig` > `expoConfig.extra.eas` > `manifest2.expoClient.extra.eas`) regressionsfest ab.
